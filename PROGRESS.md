# Progress log

## 2026-08-07 — vm-call-by-name (implement)

**Задача:** узкий публичный API в `bsl-vm` для вызова процедуры/функции
модуля по имени из рантайма, поверх машинерии изолированного исполнения
`Выполнить`/`Вычислить`.

**Изменённые файлы:** только `crates/bsl-vm/src/lib.rs`.

- Новая `pub fn call_module_function(program, stack, name, args) ->
  Result<(BslValue, Vec<BslValue>), RtError>` рядом с `run_dynamic_snippet`.
  Вход обёрнут в `DynamicDepthGuard::enter()`; имя ищется регистронезависимо
  (`to_uppercase`) по `program.function_names`, попадание `i` даёт чанк
  `chunks[i + 1]`. Стек кадра — `[арг0..аргN-1]` + `push_own_registers`, за
  регистрами копия модульного блока, `Program` поднимается с тем же
  содержимым и `module_base` за регистрами, исполнение через `drive`.
  После возврата модульный блок переносится обратно в `stack` вызывающего,
  а слоты параметров `0..n_params` отдаются вторым элементом пары.
- Неизвестное имя и несовпадение арности — `RtError::DynamicError` с русским
  текстом (перехватываемо `Попытка`), новых вариантов `RtError` не заводил.
  Рассогласованная `Program` (имя есть, чанка нет) и короткий `stack` дают
  `RtError::InvalidBytecode` через `at`/`reg_load`. Голых `unwrap()`/`expect()`
  вне тестов не добавлено.
- Шесть юнит-тестов из ACCEPTANCE в том же `mod tests`, плюс два хелпера
  (`compile_src`, `module_state` — прогоняет верхний уровень через `drive`
  и отдаёт его финальный стек, то есть настоящее состояние модуля).

**Команды и результаты:**

- `cargo build -p bsl-vm` — PASS.
- `cargo test -p bsl-vm call_module_function` — PASS, 6/6 (все шесть тестов
  из ACCEPTANCE существуют и проходят).
- `cargo test --workspace` — PASS, падений и warning'ов нет (в том числе
  `bsl-cli`: конформанс, бандлы, JIT-сверка).
- `cargo fmt --all -- --check` — PASS.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — PASS
  (первый прогон падал на `[`run_dynamic_snippet`]` — ссылка из публичной
  документации на приватный элемент; ссылка снята, текст остался).
- `cargo clippy --workspace --all-targets -- -D warnings` — **НЕ ЗАПУЩЕН**:
  `cargo clippy` на этой машине отсутствует (`error: no such command:
  clippy`; системный Rust 1.97.1 из Fedora, пакет `clippy` не установлен,
  rustup нет). Ставить пакет не стал. Вместо него прогнал
  `cargo build --workspace --all-targets` — ноль warning'ов, но это только
  слой rustc, не clippy-линты. Clippy на этом изменении остаётся
  непроверенным; ревьюеру стоит прогнать его отдельно.

**Замечания по семантике (не платформенные, замера не требуют):**

- Аргументы передаются по значению: у рантайма нет слота вызывающего,
  который можно было бы алиасить (в отличие от `Instr::Call`, где
  `param_aliases` указывает в кадр вызывающего). Поэтому запись в параметр
  без `Знач` наблюдаема только через возвращаемые финальные значения слотов
  — это и есть будущий канал для `Отказ` на этапе json-callbacks.
- Арность обязана совпадать РОВНО. Проверено по коду: `bsl-sema` добивает
  список аргументов до `n_params` маркером `RExpr::Skipped`
  (`resolver.rs:869`), кодоген превращает его в `LoadSkipped`, а значение по
  умолчанию вычисляет пролог самой функции (`compile_param_defaults`). То
  есть вызывающий этот API может передать `BslValue::Skipped` и получить
  дефолт — отдельного механизма не нужно, но и «передать меньше аргументов»
  нельзя.
- `Program` клонируется целиком на каждый вызов (иначе не поменять
  `module_base`, а `drive` берёт `&Program`) — ровно то же делает
  `run_dynamic_snippet`. На этапе json-callbacks, где вызов идёт на каждый
  элемент документа, это может оказаться заметной стоимостью; сейчас не
  измерено и в задачу не входило.
- Маркеров `НЕ ИЗМЕРЕНО` не добавлял: API внутренний, платформой напрямую
  не наблюдаем, наблюдаемое поведение появится на следующем этапе.
  Реестр `open_questions.rs` не трогал.

**Неизвестное:** поведение при вызове функции модуля, содержащей
`Выполнить`/`Вычислить`, отдельным тестом не покрыто (в ACCEPTANCE такого
пункта нет). По коду путь корректен — `aliased` в `run_dynamic_snippet`
считается как `scope_id == 0 && module_base == 0`, а здесь `scope_id =
func_id >= 1`, так что фрагмент пойдёт неалиасной веткой, — но это
рассуждение, а не прогон.
2026-08-07T10:13:27Z  FAIL  vm-call-by-name  fable=PASS opus=n/a  panel=1

## 2026-08-07 — vm-call-by-name (implement, итерация 2)

**Задача:** достройка, а не переделка. Реализация `call_module_function`
из итерации 1 принята обоими ревьюерами по существу (прошлый FAIL был
формальным: вердикт в `REVIEW_OPUS.md` был оформлен как `# VERDICT: PASS`
и не распознался арбитром). По плану закрывался единственный оставшийся
пробел — пункт «Неизвестное» прошлой записи: вызов по имени функции,
которая внутри себя исполняет `Выполнить`/`Вычислить`.

**Изменённые файлы:** `crates/bsl-vm/src/lib.rs` — только `mod tests`,
один новый тест. Реализация (`call_module_function`, её doc comment,
сигнатура) не тронута ни на строку; `git diff --stat` по не-тестовой части
файла пуст.

- Новый тест `call_module_function_with_dynamic_eval_inside`: модуль с
  `Перем Счетчик` (инициализирован в 4 на верхнем уровне) и функцией
  `Крутить()`, которая делает `Выполнить("Счетчик = Счетчик + 1")` и
  возвращает `Вычислить("Счетчик * 10")`. Проверяются оба конца цепочки:
  возвращаемое значение равно 50 (значит `Вычислить` увидел запись,
  сделанную предыдущим `Выполнить`, а не исходное значение слота) и
  `stack[0]` вызывающего равен 5 после возврата (значит мутация из
  вложенного фрагмента пережила обе границы). Тест бьёт ровно две
  вложенности `call_module_function` → `run_dynamic_snippet`: пропади
  перенос модульного блока на любой из них, падает один из двух assert'ов.
- Нагруженность теста проверена мутацией, а не рассуждением. Временно
  выключал обратный перенос модульного блока по очереди на каждой границе
  и прогонял тесты, потом восстанавливал: без переноса в
  `call_module_function` новый тест падает на `stack[0]` (`left: Number(4)`
  вместо 5), вместе с ним падает и старый
  `call_module_function_mutates_module_var`; без переноса в
  `run_dynamic_snippet` падает ТОЛЬКО новый тест, на возвращаемом значении
  (`left: Number(40)` вместо 50) — то есть внутреннюю границу из семи
  тестов держит он один. Итоговый диф `crates/bsl-vm/src/lib.rs` — ровно
  +30 строк в `mod tests`, обе мутации откачены.

**Команды и результаты:**

- `cargo build -p bsl-vm` — PASS.
- `cargo test -p bsl-vm call_module_function` — PASS, **7/7**: шесть тестов
  прошлой итерации плюс новый.
- `cargo test --workspace` — PASS, 0 failed во всех бинарях (в bsl-vm стало
  186 тестов), warning'ов нет.
- `cargo fmt --all -- --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**.
  Отдельно отмечу расхождение с прошлой записью: тогда clippy на машине не
  было (`no such command: clippy`), теперь он есть (clippy 0.1.97) и
  проходит чисто по всему workspace. То есть заменитель из ACCEPTANCE
  (`cargo build --workspace --all-targets`) не понадобился, и оговорка
  «clippy на этом изменении остаётся непроверенным» из итерации 1 снята —
  весь код, включая `call_module_function`, теперь clippy-чист.

**Замечания:**

- Дефекта переноса модульного блока тест не вскрыл — прошёл с первого
  прогона, правки в реализации не потребовалось. Рассуждение из прошлой
  записи (`aliased = scope_id == 0 && module_base == 0`, а здесь
  `scope_id = func_id >= 1`, значит фрагмент идёт неалиасной веткой)
  подтвердилось прогоном.
- Маркеров `НЕ ИЗМЕРЕНО` не добавлял и `open_questions.rs` не трогал —
  по тем же основаниям, что и в итерации 1: API внутренний, платформой
  напрямую не наблюдаем. Новых кандидатов на OpenQuestion-ID эта итерация
  не породила.

**Неизвестное:**

- Тест берёт фрагмент, обращающийся к МОДУЛЬНОЙ переменной. Случай, когда
  фрагмент внутри вызванной по имени функции объявляет НОВОЕ имя, не
  покрыт — но там поведение задано общим правилом `run_dynamic_snippet`
  (имя, впервые созданное внутри `Выполнить`, вызов не переживает; это
  измерено на 8.3.27 и зафиксировано комментарием в коде), отдельной
  специфики у вызова по имени здесь нет.
- Стоимость `program.clone()` на каждый вызов по-прежнему не измерена
  (перенесено из итерации 1, в задачу не входило).
2026-08-07T10:26:06Z  PASS  vm-call-by-name  panel=1

## 2026-08-07 — json-callbacks (implement, итерация 1)

**Задача:** функции преобразования (`ЗаписатьJSON`) и восстановления
(`ПрочитатьJSON`) поверх `call_module_function`; снять заглушку
«появятся позже».

**Порядок работы — сначала платформа.** До первой строки Rust сделано
десять прогонов `run-on-1c.sh` (~40 с каждый). Вся семантика ниже —
ИЗМЕРЕНА на 8.3.27, не выведена из статьи. Первый прогон умер молча
(«модуль не скомпилировался») на параметре с именем `Знач` — тот же класс
грабель, что и `И` из AGENTS.md.

**Изменённые файлы:**

- `crates/bsl-rt/src/json.rs` — типы `JsonCallByName`/`JsonConvertFn`/
  `JsonRestoreFn`; `read_json` получил `restore`, `write_json` —
  `convert` вместо `has_convert_fn`; `build_value` разделён на
  `build_value` (зовёт колбэк) и `build_raw_value`; `serialize` переведён
  на `SerializeCtx` и получил `prepare`/`serialize_member`/
  `serialize_converted` под `Отказ`.
- `crates/bsl-rt/src/builtin.rs` — `reject_unsupported_json_callback_args`
  удалена; разбор аргументов вынесен в `read_json_builtin`/
  `write_json_builtin` (общие для пути с VM и без), плюс `callback_name` и
  `name_list_arg`; новый модуль тестов `json_callback_tests` (18 тестов).
- `crates/bsl-rt/src/lib.rs` — реэкспорт новых имён.
- `crates/bsl-vm/src/lib.rs` — `call_builtin_with_format` получила
  `program`/`stack` и ветки `ReadJson`/`WriteJson` с замыканием над
  `call_module_function`; 6 сквозных тестов.
- `crates/bsl-vm/src/jit/mod.rs` — шим передаёт те же два аргумента
  (одна строка): перехват обязан быть общим, иначе разъедутся.
- `tests/conformance/fixtures/json-callbacks.{bsl,expected,platform.txt}` —
  новая фикстура, 74 строки, `.expected` снят прогоном на платформе.
- `tests/conformance/fixtures/json-dates.{bsl,expected,platform.txt}` —
  правка одной пробы, см. «Что пришлось тронуть рядом».

**Что измерено (8.3.27, модуль формы, функции с `Экспорт`):**

- **Колбэк работает только при ОБЕИХ заданных позициях.** С
  `МодульФункции... = Неопределено` (или опущенным) платформа функцию не
  ищет ВООБЩЕ: запись падает тем же «Значение содержит данные недопустимых
  типов», что и без имени, чтение молча проходит мимо. Пустое имя — то же
  самое. Имя не строкой — «Несоответствие типов (параметр номер '4')».
  Отсюда убран разный текст ошибки при `has_convert_fn`.
- Модуль формы принимается только как `ЭтотОбъект` И только для функций с
  `Экспорт`: без `Экспорт` — «Метод 'Преобразовать' не найден». Не-объект в
  этой позиции — «Несоответствие типов (параметр номер '5')».
- **Функция преобразования** `(Свойство, Значение, ДополнительныеПараметры,
  Отказ)`, РОВНО четыре параметра. Зовётся ЛЕНИВО — только на значении, для
  которого своего представления нет (на числе и на `Дата` не зовётся ни
  разу). `Свойство` — имя свойства для члена структуры/соответствия,
  `Неопределено` для элемента массива и верхнего уровня; при
  `СериализовыватьМассивыКакОбъекты` — индекс СТРОКОЙ (`"1"`), то есть
  функция видит документ таким, каким он пишется. Несериализуемый КЛЮЧ
  соответствия до функции не доходит вовсе.
- **`Отказ` убирает значение целиком:** свойство исчезает из объекта,
  элемент — из массива (`[1,3]`, а не дыра), на верхнем уровне не пишется
  ничего и `Закрыть()` отдаёт пустую строку.
- **`Отказ` читается по обычным правилам УСЛОВИЯ языка.** Четырнадцать
  проб: отказом стали `Истина`, `1`, `-1`, `"да"`; не стали `Ложь`, `0`,
  `""`, `"   "`, `"абв"`, `Неопределено`, `Null`, пустая и непустая дата,
  пустой и непустой массив, `Тип("Строка")`. Это в точности
  `BslValue::as_condition` с подавленной ошибкой — включая нетривиальную
  часть про строки. Гипотезы «`<> Ложь`» и «`ЗначениеЗаполнено`» обе
  ОТВЕРГНУТЫ замером (первая — на `0`/`""`, вторая — на `Ложь`).
- Возвращённое значение сериализуется обычными правилами; если оно САМО
  снова несериализуемо — повторного вызова НЕТ, обычная ошибка типа (ровно
  один вызов). Но возвращённый КОНТЕЙНЕР обходится как обычно, и на
  несериализуемом внутри него функция зовётся снова.
- **Функция восстановления** `(Свойство, Значение, ДополнительныеПараметры)`,
  РОВНО три параметра. Зовётся для КАЖДОГО значения документа в ОБРАТНОМ
  порядке (дети раньше родителя), корень — последним с `Свойство =
  Неопределено`; элементы массивов — тоже с `Неопределено`. На документе из
  фикстуры это 20 вызовов; их порядок и есть таблица из статьи, снятая
  прогоном. Скаляр и пустой документ на верхнем уровне тоже получают вызов.
- Непустой `ИменаСвойствДляФункцииВосстановления` сужает вызовы до
  перечисленных имён НА ЛЮБОЙ ГЛУБИНЕ и ОТМЕНЯЕТ вызов на корне; пустой
  массив равносилен отсутствию фильтра. Сравнение РЕГИСТРОЗАВИСИМОЕ
  (`["БЭ"]` не ловит свойство `бэ`).
- **Функция восстановления имеет приоритет над
  `ИменаСвойствСоЗначениямиДата`:** свойство, которое ей достаётся,
  приходит СЫРОЙ строкой и датой не становится; свойство, до неё не
  дошедшее, разбирается в дату как раньше.
- Исключение изнутри любого колбэка выпускается наружу как есть.
- Повторный вход в тот же `ЧтениеJSON`/`ЗаписьJSON` изнутри колбэка —
  ошибка платформы («Недопустимое состояние потока чтения JSON», «Неверный
  порядок записи JSON»), не тихое продолжение. Здесь это тоже ошибка, а не
  паника: разборщик и приёмник ВЫНИМАЮТСЯ из своих `RefCell` на время
  обхода (иначе пользовательский код изнутри колбэка ронял бы процесс
  паникой `RefCell` мимо `Попытка`) и возвращаются на место при любом
  исходе.

**Фикстура и `ЭтотОбъект`.** Платформе для колбэка обязателен модуль, а
`ЭтотОбъект` этот интерпретатор не знает вовсе. Фикстура берёт его через
`Вычислить("ЭтотОбъект")` внутри `Попытка` — санкционированный AGENTS.md
приём для конструкции, которой на одной из сторон нет: на платформе
возвращается форма, здесь — `Истина`, и обе стороны передают в позицию
модуля НЕ `Неопределено`, чего для вызова достаточно. Реализация
принимает в этой позиции любое значение, кроме `Неопределено`, и всегда
ищет функцию в модуле исполняемого скрипта: своей системы модулей нет,
воспроизводить различие «не тот тип» / «нет такого метода» нечем. Это
осознанное отклонение, а не открытый вопрос — поведение платформы
измерено и записано в doc comment `callback_name`.

**Что пришлось тронуть рядом (обе правки — следствие замера, не рефакторинг):**

- `is_date_property` сравнивал имена ЧЕРЕЗ `to_uppercase`. Замер: платформа
  РЕГИСТРОЗАВИСИМА и здесь (`["СОЗДАНО"]` не превращает `создано` в дату).
  Функция стоит ровно в том месте, которое переписано под приоритет
  функции восстановления, и оставить рядом две разные дисциплины сравнения
  было нельзя. Исправлено, поведение зафиксировано фикстурой.
- `json-dates.bsl`: проба полной арности вызывала
  `ПрочитатьJSON(Ч, Ложь, Неопределено, Неопределено, "ИмяФункции")` и
  ждала `<ошибка>`. Замер показал, что ошибка была НЕ про колбэк:
  платформа различает ПРОПУЩЕННЫЙ аргумент и явное `Неопределено` в
  `ОжидаемыйФорматДаты` — второе даёт «Несоответствие типов (параметр
  номер '4')». Этого различия здесь нет: резолвер добивает необязательные
  позиции встроенного вызова именно `Неопределено`, и отвергать его
  значило бы сломать все вызовы с пропущенным форматом. Проба переписана
  на пропуск аргумента (`, ,`), `.expected` переснят прогоном на
  платформе — разошлась ровно одна строка из 49, остальные 48 совпали
  байт в байт (заодно подтверждение, что зона машины не поехала).

**Команды и результаты:**

- `cargo build -p bsl-rt -p bsl-vm` — PASS.
- `cargo test -p bsl-rt json_callback` — PASS, 18/18.
- `cargo test -p bsl-vm json_callback` — PASS, 6/6.
- `cargo test -p bsl-cli` — PASS (конформанс с новой фикстурой, бандлы,
  round-trip байт-кода, JIT-сверка).
- `cargo fmt --all -- --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — PASS.
- `cargo test --workspace` — PASS, 679 тестов, 0 failed (в том числе
  `open_questions_registry_matches_source_markers`).
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — PASS
  (три ссылки на приватные элементы сняты).
- Вывод фикстуры совпал с платформой ПОЛНОСТЬЮ, все 74 строки, и
  отдельно проверен `--jit`-прогон — тоже байт в байт.

**Новых `НЕ ИЗМЕРЕНО` не заведено, реестр не тронут.** Всё, что решалось,
закрыто замером; ни одного решения «рассуждением» в этой правке нет.

**Найдено попутно, НЕ исправлено (кандидаты в backlog, каждое —
самостоятельная задача со своим замером):**

- `ЗаписатьJSON` с ЧИСЛОВЫМ ключом `Соответствие`: платформа отвергает —
  «Недопустимый тип значения ключа элемента соответствия», а здесь ключ
  печатается своим строковым видом (`json.rs`, ветка `BslObject::Map`;
  комментарий там выведен рассуждением, а не замером). К колбэкам
  отношения не имеет, трогать в этой задаче не стал.
- `ЧтениеJSON.Закрыть()` не реализован (`метод «Закрыть» не применим`),
  хотя платформа его принимает; из-за этого фикстура источник не
  закрывает.
- `Строка(ТипЗнч(Новый ТаблицаЗначений))` — «ТаблицаЗначений» здесь и
  «Таблица значений» на платформе; в фикстуре тип несериализуемого
  значения поэтому не печатается.
- `ОписаниеОшибки()` не реализован — в фикстуре все ветки исключений
  печатают фиксированное `<ошибка>`.

**Неизвестное:**

- Значения, возвращённые колбэком, приходят из вложенного `drive` со
  СВОИМ `RuntimeShapes` (см. `call_module_function`). Имя поля, впервые
  созданное ВНУТРИ колбэка динамически (не литералом), во внешней таблице
  имён отсутствует; тестом это не покрыто и не измерено — ограничение
  унаследовано от `run_dynamic_snippet`, а не внесено этой правкой.
- Стоимость `program.clone()` на каждый вызов колбэка не измерена, а
  вызовов теперь по одному на значение документа: на большом документе с
  функцией восстановления это может быть заметно. В задачу не входило,
  бенчмарка на этот путь нет.
2026-08-07T13:55:07Z  FAIL  json-callbacks  fable=PASS opus=FAIL  panel=1

## 2026-08-07 — json-callbacks (implement, итерация 2, фикс по REVIEW_OPUS.md)

**Задача:** минимальное исправление трёх пунктов «## Дефекты» из
`REVIEW_OPUS.md` (VERDICT: FAIL). Содержательная реализация колбэков итерации
1 не трогалась: ни одной правки исполняемого кода в этой итерации нет.

**Изменённые файлы:**

- `tests/conformance/fixtures/json-dates.bsl` — в КОНЕЦ добавлена проба
  «регистр имени в `ИменаСвойствСоЗначениямиДата`»: два `Попытка`-блока на
  одном документе `{"создано":"2014-05-10T13:14:15"}` и одном формате
  `ФорматДатыJSON.ISO`, различающиеся только регистром имени в списке
  (`"создано"` — контроль, `"СОЗДАНО"` — собственно проба). Контрольная
  строка добавлена сверх буквы плана: без неё исход «Строка» на верхнем
  регистре нельзя отличить от «ISO в этой позиции вообще не разбирается».
- `tests/conformance/fixtures/json-dates.expected`,
  `json-dates.platform.txt` — переснято прогоном
  `./tests/conformance/measure/1c/run-on-1c.sh
  tests/conformance/fixtures/json-dates.bsl` на 8.3.27 на этой же машине;
  `.expected` — побайтовая копия `.platform.txt`.
- `crates/bsl-rt/src/builtin.rs` — переписан блок комментария над
  `BuiltinFn::ReadJson => (1, 8)` / `WriteJson => (2, 6)` (только
  комментарий, кортежи арности не менялись).
- `crates/bsl-rt/src/json.rs` — только doc comment `needs_convert`.

**Замер (дефект 2). Платформа подтвердила регистрозависимость.**

Новый `.platform.txt` отличается от прежнего РОВНО двумя добавленными
строками (`diff` — `49a50,51`), ни одна из 49 прежних строк не поехала:
канарейка сессии (часовой пояс, та же машина) чистая.

```
Дата: 10.05.2014 13:14:15      <- список ["создано"], регистр совпал
Строка: 2014-05-10T13:14:15    <- список ["СОЗДАНО"], регистр разошёлся
```

То есть замер итерации 1 верен, `is_date_property` откатывать не пришлось:
сравнение остаётся точным, и теперь оно ЗАКРЕПЛЕНО фикстурой, а не одним
комментарием. Невакуумность проверена прямо: временный откат на
`n.to_uppercase() == p.to_uppercase()` роняет `cargo test -p bsl-cli
--test conformance` двумя тестами (`conformance_fixtures_match_oracle_output`
и `fixtures_produce_the_same_output_when_run_from_printed_bytecode`) на
строке 51 — «ожидалось "Строка: ...", получено "Дата: ..."». Код после
проверки восстановлен.

**Поправка к записи итерации 1** (журнал append-only, старую запись не
редактирую): формулировка «Исправлено, поведение зафиксировано фикстурой»
была НЕТОЧНОЙ. Фикстурой (`json-callbacks.bsl:263`, `.expected:65`) была
закреплена регистрозависимость фильтра
`ИменаСвойствДляФункцииВосстановления`, а НЕ списка
`ИменаСвойствСоЗначениямиДата`; регрессия последнего к `to_uppercase`
прошла бы тогда незамеченной. Ревьюер прав. Списком дат этот пробел закрыт
только сейчас, пробой выше.

**Дефект 1 — комментарий `arity_range`.** Сняты все четыре опровергнутых
утверждения: «колбэков в языке ещё нет», «ИЗМЕРЕНО: `ПрочитатьJSON`
отвергает непустую пятую позицию», «пятая и шестая позиции `ЗаписатьJSON`
остались немедленной ошибкой», ссылка на снятую семантику
`has_convert_fn`/`unsupported_value_error`. Новый текст: обе полные арности
поддержаны целиком, хвостовые позиции — функции восстановления и
преобразования с параметрами (разбор в
`read_json_builtin`/`write_json_builtin`, вызов через контекст VM в
`call_builtin_with_format`). Метка ИЗМЕРЕНО оставлена ровно на двух фактах,
подтверждённых этой серией проб: лень вызова функции преобразования и
немедленная ошибка на ЯВНОМ `Неопределено` в четвёртой позиции
`ПрочитатьJSON` при принимаемом ПРОПУСКЕ той же позиции.

**Дефект 3 — doc comment `needs_convert`.** Обещание компиляторной защиты
сужено до правды: исчерпывающесть гарантирована только по `BslValue`; ветка
`BslValue::Object` — негативный `matches!`, поэтому новый вариант
`BslObject` компилятор не остановит и он консервативно уйдёт в
«несериализуемые» (тот же дефолт, что давний `_ =>` в `serialize`).
Разворачивать `matches!` в исчерпывающий матч по всем вариантам
`BslObject` в этой итерации намеренно НЕ стал — это рост объёма против
минимального фикса; кандидат в backlog.

**Команды и результаты:**

- `cargo build -p bsl-rt` — PASS.
- `diff json-dates.expected json-dates.platform.txt` — PASS (пусто).
- `grep -n "СОЗДАНО" json-dates.bsl` — PASS (строка 315).
- `cargo test -p bsl-cli` — PASS, 0 failed (конформанс, round-trip
  байт-кода, бандлы, `the_jit_agrees_with_the_interpreter_on_every_script`).
- `cargo fmt --all -- --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — PASS.
- `cargo test --workspace` — PASS, 0 failed во всех бинарях, включая
  `open_questions_registry_matches_source_markers`.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — PASS.

**Реестр `НЕ ИЗМЕРЕНО` не тронут, новых маркеров нет** — новых решений
«рассуждением» в этой итерации не появилось, а единственный спорный факт
(регистр имён) закрыт замером.

**Неизвестное:**

- Регистрозависимость измерена на ОДНОЙ паре «нижний ключ против верхнего
  имени». Обратный порядок (ключ документа верхним регистром, имя в списке
  нижним) и небуквенные различия (`Ё`/`Е`, юникодный регистр вне латиницы и
  кириллицы) не проверялись — предполагается посимвольное равенство, но
  замера на это нет.
- Почему платформа обращается с этими именами регистрозависимо, тогда как
  идентификаторы языка регистр не различают, — не выяснено; трактуется как
  «это ключи JSON, а не идентификаторы BSL», и это трактовка, а не замер.
2026-08-07T14:12:49Z  PASS  json-callbacks  panel=1

## 2026-08-07 — bin-binary-data (implement, итерация 1)

**Задача:** тип `ДвоичныеДанные` — иммутабельный байтовый буфер,
конструктор из файла, `Размер()`, `РазделитьДвоичныеДанные` /
`СоединитьДвоичныеДанные`.

**Изменённые файлы:**

- `crates/bsl-rt/src/object.rs` — вариант `BslObject::BinaryData(Rc<[u8]>)`
  (без `RefCell`: значение иммутабельно, всякая операция отдаёт новое).
- `crates/bsl-rt/src/types.rs` — `TypeId::BinaryData` + строка
  `("Двоичные данные", "BinaryData")` в `NAMES`.
- `crates/bsl-rt/src/lib.rs` — пять исчерпывающих матчей (`type_name`,
  `is_filled`, `type_of`, `collection_len`, `Display`), равенство и хэш ПО
  СОДЕРЖИМОМУ, `binary_data_of`/`new_binary_data`/`binary_data_size`/
  `binary_data_split`/`binary_data_combine`, `binary_data_display`,
  `binary_split_max_part`, десять юнит-тестов.
- `crates/bsl-rt/src/builtin.rs` — `BuiltinFn::SplitBinaryData`/
  `ConcatBinaryData` (имена, арности, вызов), `BuiltinMethod::Size`
  (`Размер`/`Size`).
- `crates/bsl-sema/src/{resolver.rs,resolved.rs}` — `NEW_TYPES`, ветка
  `resolve_new` (ровно 1 аргумент), `RExpr::NewBinaryData`,
  `expr_uses_dynamic`, арность метода `Размер` = 0.
- `crates/bsl-bytecode/src/{instr.rs,compiler.rs,text.rs,bundle.rs}` —
  опкод `NewBinaryData`, печать/разбор, строка в `OPCODES`, `FORMAT_VERSION`
  2 -> 3, строка корпуса round-trip, классификация эффектов (читает `path`,
  пишет `dst`, `io = true`).
- `crates/bsl-vm/src/lib.rs` — исполнение опкода. JIT не тронут:
  непокрытые опкоды уходят в интерпретаторный фолбэк через `_ => None`.
- `tests/conformance/fixtures/binary-data.{bsl,expected,platform.txt}` —
  новая фикстура, эталон снят прогоном на 8.3.27.

**Что снято с платформы (8.3.27).** Кроме самой фикстуры (44 строки,
`.expected` побайтово равен `binary-data.platform.txt`) прогонялись два
разведочных скрипта вне репозитория; их выводы приведены ниже, потому что
дальше от них зависят таблицы имён:

- Имя ТИПА — `Двоичные данные` (с пробелом), английское — `BinaryData`.
- Строковое представление ЗНАЧЕНИЯ — не имя, а дамп байтов:
  шестнадцатеричные пары в ВЕРХНЕМ регистре через пробел. Обрезается на
  256 байтах, после чего БЕЗ пробела приписывается `...` (граница
  проверена в фикстуре на 255/256/257 байтах: 764/767/770 символов, то
  есть многоточие появляется строго при длине больше 256).
- Английские написания НЕ угаданы, а пробиты `Вычислить` в `Попытка`:
  приняты `SplitBinaryData`, `ConcatBinaryData`, `Size`, `BinaryData`;
  отвергнуты `CombineBinaryData`, `MergeBinaryData`, `JoinBinaryData`.
- Равенство — ПО СОДЕРЖИМОМУ (два `Новый ДвоичныеДанные` от одного файла
  равны, от разных нет), поэтому хэш тоже по байтам: иначе одинаковые
  данные стали бы разными ключами `Соответствие`.
- `ЗначениеЗаполнено` — по длине: 13 байт «Да», ноль байт «Нет».
- Разбиение: хвост КОРОЧЕ (13 по 5 -> 5,5,3), точная кратность лишней
  пустой части не даёт, размер части больше целого — не ошибка.
  Отвергаются ноль, отрицательное, дробное и числовая СТРОКА `"5"`.
- Ошибки конструктора: нет файла, каталог вместо файла, ноль аргументов,
  два аргумента, число вместо имени.
- Склейка: порядок массива сохраняется, пустой массив даёт ПУСТЫЕ данные
  (не ошибку), элемент-строка и элемент-`Неопределено` — ошибка, не массив
  — ошибка.

**Два места, где платформа разошлась с «очевидной» реализацией**, и оба
пойманы фикстурой, а не рассуждением:

1. Разбиение ПУСТЫХ данных. `slice::chunks` не даёт ни одной части,
   платформа отдаёт массив из ОДНОЙ пустой части. Исправлено явной веткой;
   фикстура печатает и тип, и размер этой части.
2. Верхняя граница размера части. Первая версия принимала то, что влезает
   в `i64`. Замер показал границу РОВНО на `2^64-1`: `10^10`, `2^63`,
   `2^63-1` и `2^64-1` приняты, `2^64` и `10^25` — ошибка. То есть счётчик
   у платформы 64-битный БЕЗ знака. Проверка переписана на сравнение с
   `2^64-1`, значения шире `usize` насыщаются до `usize::MAX` (результат
   тот же — одна часть целиком).

**Команды и результаты:**

- `cargo build -p bsl-rt`, `cargo build --workspace` — PASS.
- `cargo test -p bsl-rt binary_data` — PASS, 10 тестов.
- `cargo test -p bsl-cli` — PASS, 0 failed (конформанс 19 проверено /
  19 пропущено, round-trip печатного байт-кода, инварианты бандлов,
  `the_jit_agrees_with_the_interpreter_on_every_script` на 35 скриптах).
- `diff tests/conformance/fixtures/binary-data.expected
  tests/conformance/fixtures/binary-data.platform.txt` — PASS (пусто).
- `cargo fmt --all -- --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — PASS.
- `cargo test --workspace` — PASS, 0 failed в 24 бинарях, включая
  `open_questions_registry_matches_source_markers`.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — PASS.

**Новых `НЕ ИЗМЕРЕНО` нет**, реестр `open_questions.rs` и
`measure-all.bsl` не тронуты: все решения этой итерации закрыты замером, а
не рассуждением. Единственная строка, которую платформа не наблюдает, —
`type_name()` = `ДвоичныеДанные`: она уходит только в текст `RtError` и
из BSL недостижима (`ОписаниеОшибки()` в этой реализации нет), поэтому
маркера не заводил; в комментарии это сказано прямо.

**Неизвестное (кандидаты в backlog, за рамками задачи):**

- `ЗначениеВСтрокуВнутр(ДД)`, `ДД.Количество()` и обход `Для Каждого` в
  этой реализации дают ЛОВИМУЮ ошибку (проверено), но что на них отвечает
  платформа — не мерилось. Первое из трёх осмысленно: внутренний формат
  для двоичных данных у платформы, скорее всего, есть.
- Сериализация `ДвоичныеДанные` в JSON уходит в «несериализуемые» через
  негативный `matches!` в `json.rs::needs_convert` — то же поведение, что
  у любого нового варианта `BslObject`, и оно не измерено.
- Прочие методы типа (`Записать(Путь)`, `Base64Строка`, `ОткрытьПотокДляЧтения`)
  не заводились: в задаче их нет.
- Точность границы обрезания дампа проверена на 255/256/257 байтах;
  поведение на очень больших значениях (мегабайты) не мерилось — счётчик
  тот же, но замера на это нет.
2026-08-07T19:12:20Z  PASS  bin-binary-data  panel=0

## 2026-08-07 — bin-buffer: тип `БуферДвоичныхДанных`

**Задача:** изменяемый байтовый буфер фиксированного размера с индексацией,
целыми 8/16/32/64, порядком байтов, `Разделить`/`Соединить` и побитовыми
методами.

**Файлы:** новый `crates/bsl-rt/src/binbuf.rs` (данные и вся логика, 24
юнит-теста); правки в `bsl-rt` — `lib.rs` (модуль, `type_name`, `type_of`,
`is_filled`, `collection_len`, `Display`, `get_index`/`set_index`, протокол
свойств в `get_field_by_name`/`set_field_by_name`, обёртка
`BslValue::new_binary_buffer`), `object.rs` (`BslObject::BinaryBuffer`),
`types.rs` (`TypeId::BinaryDataBuffer`, `TypeId::ByteOrder`), `enums.rs`
(`EnumKind::ByteOrder` + два члена), `builtin.rs` (13 вариантов
`BuiltinMethod`, строки имён, диспетч); `bsl-sema` — `resolved.rs`
(`RExpr::NewBinaryBuffer`), `resolver.rs` (`NEW_TYPES`, `resolve_new`,
арности методов); `bsl-bytecode` — `instr.rs`, `compiler.rs`, `text.rs`
(опкод, печать, разбор, корпус, `FORMAT_VERSION` 3 -> 4), `bundle.rs`;
`bsl-vm` — `lib.rs`. Фикстура `tests/conformance/fixtures/binary-buffer.bsl`
плюс `.expected`/`.platform.txt`.

**Команды и результат:**

- `cargo test -p bsl-rt binbuf` — PASS (24 теста).
- `./tests/conformance/measure/1c/run-on-1c.sh tests/conformance/fixtures/binary-buffer.bsl`
  — снято 109 строк с платформы 8.3.27.
- `diff binary-buffer.expected binary-buffer.platform.txt` — ПУСТО; эталон
  снят платформой и совпал с нашим выводом построчно с первого прогона.
  Проверено, что фикстура действительно исполняется: намеренная порча
  эталона роняет `conformance_fixtures_match_oracle_output`.
- `cargo test --workspace` — PASS, включая
  `open_questions_registry_matches_source_markers`,
  `the_jit_agrees_with_the_interpreter_on_every_script` и
  `measure_script_runs_under_this_interpreter`.
- `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`,
  `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — все PASS.

**Что план предполагал неверно (выяснено замером, восемь заходов на платформу):**

- **`ПрочитатьЦелое8`/`ЗаписатьЦелое8` НЕ СУЩЕСТВУЮТ** — ни в русском, ни в
  английском написании; `ПрочитатьБайт`, `ПолучитьБайт`, `ReadByte` тоже
  нет. Один байт берётся индексом либо `Получить`/`Установить`. Заведены
  только 16/32/64.
- **`Разделить` — это не нарезка на куски, а раскрой по РАЗДЕЛИТЕЛЮ.**
  Аргумент — `БуферДвоичныхДанных`, число платформа отвергает. Семантика
  строкового `СтрРазделить`: `[1,2,0,3,4,0,5]` по `[0]` даёт `[1 2]`,
  `[3 4]`, `[5]`, разделитель в начале даёт ведущую пустую часть.
- **Исключающее ИЛИ называется `ЗаписатьПобитовоеИсключительноеИли`**, а не
  `...ИсключающееИли`, как в плане. Английское — `WriteBitwiseXor`.
- **Второй аргумент побитовых методов — БУФЕР-маска, а не число.**
- **`Размер` — свойство, а не метод:** `Буфер.Размер()` со скобками
  платформа отвергает (у `ДвоичныеДанные` наоборот — метод).
- **Целые БЕЗЗНАКОВЫЕ:** восемь байтов `FF` читаются как
  `18446744073709551615`, а не как `-1`.
- **Равенство ПО ТОЖДЕСТВУ**, в отличие от `ДвоичныеДанные`: два свежих
  нулевых буфера одного размера не равны. Досталось даром от общей ветки
  `Rc::ptr_eq`.
- `Инвертировать` с `Количество = 0` означает «до конца», а не «ничего»;
  на ПУСТОМ буфере — ошибка даже без аргументов.
- `Соединить` наследует порядок байтов ЛЕВОГО буфера; части `Разделить`
  наследуют порядок источника.
- Дробная ПОЗИЦИЯ отбрасывается к нулю (`Б[1.9]` -> байт 1), а дробный
  РАЗМЕР в конструкторе — ошибка.

**Сюрприз платформы, из-за которого замер невозможен (ни одного OpenQuestion
не заведено, см. ниже):**

Запись в байт значения вне `0..255` или дробного (`Б[1] = 256`, `Б[1] = 2.9`)
и мусор вместо аргумента порядка байтов (`Б.ПрочитатьЦелое16(0, 5)`) на
8.3.27 **не бросают перехватываемое исключение, а молча уносят сеанс**:
`Попытка` вокруг не ловит ничего, прогон висит до таймаута и эталон не
снимается. Проверено трижды — через `Выполнить`, прямой `Попыткой` и на
дробном значении; три захода на платформу были потеряны именно на этом.

Маркеров `НЕ ИЗМЕРЕНО(BIN.*)` я на это НЕ заводил, и вот почему: реестр
требует ровно одну строку `М()` в `measure-all.bsl`, а такая строка убила бы
весь сеанс замеров для всех остальных ИД. При этом наблюдаемый вывод не
является догадкой — значение платформой НЕ принимается (а `255`, `65535` и
`4294967295` принимаются, это измерено), и реализация отвечает обычной
ловимой `RtError`. Воспроизводить гибель сеанса нельзя и не нужно. Всё это
записано в заголовке `binbuf.rs` и в шапке фикстуры. Если такие случаи
всё-таки надо внести в реестр, нужен способ помечать вопрос как
«неизмеримый данной оснасткой» — сейчас такого механизма нет.

**Неизвестное (кандидаты в backlog, за рамками задачи):**

- `Для Каждого` по буферу платформа ПРИНИМАЕТ (проба вернула «ок»), хотя
  `Буфер.Количество()` отвергает. Здесь обход не заведён: `collection_len`
  даёт ловимую ошибку. Что именно платформа отдаёт на каждой итерации — не
  мерилось, и своего эталона у этого нет.
- `Скопировать()` и `ПолучитьСрез(0, 2)` у платформы ЕСТЬ (обе вернули
  буфер) — вне рамок задачи, не заводились.
- Сдвигов (`ЗаписатьПобитовыйСдвигВлево/Вправо`) у платформы нет —
  проверено, заводить нечего.
- Верхняя граница размера буфера не мерилась: `Новый БуферДвоичныхДанных`
  с огромным числом означал бы попытку выделить гигабайты на платформе.
  Здесь такой размер даёт ловимую ошибку через `try_reserve_exact`, а не
  панику, но с чем именно платформа сравнивает размер — неизвестно.
- Сериализация буфера в JSON уходит в «несериализуемые» через негативный
  `matches!` в `json.rs::needs_convert` — то же, что у любого нового
  варианта `BslObject`, и это не измерено.
2026-08-07T20:17:07Z  FAIL  bin-buffer  fable=FAIL opus=n/a  panel=1

## 2026-08-07 — bin-buffer, исправление по вердиктам FAIL (реализация)

**Задача:** ровно один пункт из PLAN.md — убрать панику на пользовательском
вводе в `Инвертировать` и закрыть её регрессионным юнит-тестом. Ничего
больше: фикстуры не трогались, незаблокирующие замечания ревью не делались.

**Изменённые файлы:** только `crates/bsl-rt/src/binbuf.rs`.

- `invert`, ветка `[pos, count]`: граница считается через
  `p.checked_add(c).ok_or(RtError::BadIndex)?`, сравнение и поле `index`
  берут уже посчитанный `end`. Это тот же шаблон, что у соседей `read_int`,
  `write_int` и `bitwise`. На входах, не выходящих за границы, `end == p + c`,
  так что поведение прежних случаев не изменилось.
- Новый юнит-тест `invert_rejects_a_count_that_overflows_the_position`:
  буфер из четырёх байтов, позиция `1`, количество `18446744073709551615`;
  ожидание — `Err` и нетронутое содержимое буфера.

**Команды и результаты:**

- `cargo build -p bsl-rt` — ок.
- `cargo test -p bsl-rt invert_rejects` — 3 теста, все зелёные (отобралось
  ≥ 2, как требовала приёмка).
- `cargo test -p bsl-rt binbuf` — 25 зелёных (было 24).
- Проверка, что тест действительно ловит дефект: с временно откаченной
  правкой `invert_rejects_a_count_that_overflows_the_position` падает
  паникой `attempt to add with overflow` на `binbuf.rs:689`. Правка
  возвращена, тест снова зелёный.
- Воспроизведение из ревью:
  `Б = Новый БуферДвоичныхДанных(4); Попытка Б.Инвертировать(1, 18446744073709551615); Исключение Сообщить("поймано"); КонецПопытки;`
  через `cargo run -p bsl-cli -- <файл>` печатает `поймано`, код выхода `0`
  (был `101`).
- `cargo fmt --all -- --check` — ок.
- `cargo clippy --workspace --all-targets -- -D warnings` — ок.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.
- `cargo test --workspace` — зелёный (24 набора `test result: ok`).

**Честно про один сбой в прогоне.** Самый первый `cargo test --workspace` я
запустил одновременно с `cargo doc --workspace --no-deps` (две команды в
одном сообщении), и в нём упал
`the_jit_agrees_with_the_interpreter_on_every_script`
(`conformance.rs:302` — расхождение вывода интерпретатора и `--jit`). Имя
фикстуры я потерял: вывод был отфильтрован по строкам `test result:` и
`panicked`, а сам текст ассерта в фильтр не попал. Дальше сбой НЕ
воспроизвёлся ни разу: полный `cargo test --workspace` трижды, файл
`tests/conformance.rs` целиком дважды, сам тест пять раз подряд, и один
прогон, специально повторяющий ту же связку «тесты параллельно с `cargo
doc`» — везде зелено.

Правдоподобный механизм (НЕ проверен): тест запускает `bsl-cli` подпроцессом
по два раза на фикстуру, сравнивая и stdout, и код возврата; под нагрузкой от
параллельной сборки один из ~180 подпроцессов мог получить иной код возврата.
Утверждать этого я не могу. С правкой сбой связан вряд ли: `invert` в обоих
режимах идёт по одному и тому же коду `bsl-rt`, а JIT своей семантики для
буфера не пишет — расхождение он дать не может в принципе. Тем не менее это
незакрытый вопрос, а не «всё зелёное»: если он повторится, его надо ловить с
полным выводом (`-- --nocapture`, без фильтра по grep).

**Кандидатов в `НЕ ИЗМЕРЕНО` эта правка не породила.** Наблюдаемое поведение
метода — ловимая ошибка при выходе за границы — уже измерено
(`Инвертировать(0, 99)` на четырёх байтах платформа отвергает); менялось
только то, что вместо ловимой ошибки процесс падал. Новых маркеров и строк в
`measure-all.bsl` не добавлял; реестр открытых вопросов не трогал.
2026-08-07T20:28:44Z  PASS  bin-buffer  panel=1

## 2026-08-08 — bin-streams: проводка потоков (implement)

**Задача.** `PLAN.md`, `TASK_ID: bin-streams`. Измерительная фаза была
закрыта прошлой итерацией: `crates/bsl-rt/src/stream.rs` (1385 строк, вся
семантика в шапке помечена измеренной на 8.3.27) и фикстура
`binary-streams.{bsl,expected,platform.txt}` уже лежали в рабочем дереве, но
модуль не был подключён ни к одной таблице-единственному-источнику. Требовалось
провести `ПотокВПамяти`, `ФайловыйПоток`, менеджер `ФайловыеПотоки` и три
перечисления по всем таблицам и заставить фикстуру пройти. Эталон
(`.expected`) не трогал: он от платформы.

### Что сделано, по файлам

`crates/bsl-rt/src/types.rs`
- Шесть новых `TypeId`: `MemoryStream`, `FileStream`, `FileStreamsManager`,
  `FileOpenMode`, `FileAccess`, `StreamPosition`; строки в `NAMES`.
- Новая таблица `DISPLAY` и ветка в `TypeId::name()`. Понадобилась из-за
  измеренной странности платформы: `Строка(Тип("ПотокВПамяти"))` и
  `Строка(Тип("ФайловыйПоток"))` ОБА дают «Файловый поток», хотя сами типы
  разные (`Тип("ПотокВПамяти") = Тип("ФайловыйПоток")` — «Нет»; обе строки
  есть в платформенном эталоне, строки 2/5/6/8). В `NAMES` это не помещается:
  русская колонка там служит и поиском, и печатью.
- `every_type_round_trips_through_its_own_names` пропускает типы из
  `DISPLAY` (иначе он требовал бы `name() == ru`), плюс новый тест
  `both_streams_print_the_same_name_but_stay_different_types`.

`crates/bsl-rt/src/enums.rs` — три `EnumKind` (`FileOpenMode`, `FileAccess`,
`StreamPosition`), двенадцать `EnumValue`, строки в `MEMBERS` и `ENUM_NAMES`,
метатипы. Написания членов взяты из платформенного эталона, включая
раздельные «Создать новый», «Открыть или создать», «Чтение и запись» и
английские `ReadAndWrite`/`PositionInStream` (не `ReadWrite`/`StreamPosition`).

`crates/bsl-rt/src/object.rs` — варианты `MemoryStream`, `FileStream`,
`FileStreamsManager`.

`crates/bsl-rt/src/lib.rs` — `mod stream;`, реэкспорт (`is_stream`,
`stream_write`, `StreamData`), ветки в `type_name`, `type_of`, `is_filled`,
`collection_len`, `get_field_by_name` (три признака доступности как СВОЙСТВА),
`Display`, `close_object`, `enum_kind_type_id`; обёртки `BslValue::
new_memory_stream`/`new_file_stream`/`new_file_streams_manager`.

`crates/bsl-rt/src/builtin.rs` — семь новых `BuiltinMethod`
(`CurrentPosition`, `Seek` и пять методов менеджера), их имена в
`BUILTIN_METHOD_NAMES`, диспетч. `Размер`, `Записать`, `Прочитать` и
`Закрыть` переиспользуют существующие варианты с ветвлением по получателю.
Общая функция `manager()` проверяет, что получатель метода менеджера — сам
менеджер.

`crates/bsl-sema` — `NEW_TYPES` (+4 написания), два конструктора в
`resolve_new`, голое имя `ФайловыеПотоки`/`FileStreams` в `resolve_expr`,
арности новых методов; три варианта `RExpr` и ветки `expr_uses_dynamic`.

`crates/bsl-bytecode` — три опкода `NewMemoryStream` / `NewFileStream` /
`NewFileStreamsManager` в `instr.rs`, `compiler.rs`, `text.rs`
(`OPCODES` + `write_instr` + `parse_instr` + строка корпуса round-trip),
классификация эффектов в `bundle.rs`, `FORMAT_VERSION` 4 -> 5.

`crates/bsl-vm/src/lib.rs` — исполнение трёх опкодов плюс ветка потока в
быстром пути `WriteText`. JIT не трогал: у него `_ => None`, непокрытые
опкоды уходят в интерпретаторный фолбэк.

**Третий опкод сверх плана.** В `CRATE(S)` плана перечислены два опкода, но
менеджеру нужен свой третий: измерено, что `ФайловыеПотоки = ФайловыеПотоки`
— «Нет» (строка 14 эталона), то есть каждое обращение обязано давать новый
объект, а `LoadConst` отдавал бы один и тот же `Rc`. Это же написано в
шапке `stream::new_file_streams_manager`. Расширением задачи это не считаю:
без него фикстура не проходит.

**Правки в самом `stream.rs`** — только вынужденные, семантику не менял:
сигнатура тестового помощника `frac(units, scale: u8 -> i32)` (не
компилировалось против `BslNumber::from_parts`), переименование вариантов
`StreamFlag::CanWrite/CanRead/CanSeek` -> `Writable/Readable/Seekable`
(clippy `enum_variant_names`) и шесть `&[x.clone()]` -> `slice::from_ref`
в тестах (clippy `cloned_ref_to_slice_refs`).

### Прогоны

- `cargo build --workspace` — ок.
- `cargo test -p bsl-rt stream` — ок, 26 тестов (23 из `stream.rs` плюс
  новые в `enums.rs`/`types.rs`).
- `diff tests/conformance/fixtures/binary-streams.expected
  tests/conformance/fixtures/binary-streams.platform.txt` — пусто.
- Фикстура под интерпретатором совпала с платформенным эталоном побайтово с
  первого прогона; второй прогон подряд дал то же самое (файлы во временном
  каталоге к тому моменту уже существуют — проверял намеренно).
- `cargo test -p bsl-cli` — ок; сводка раннера «21 проверено, 19
  пропущено», `binary-streams` в пропущенных НЕТ. Зелены и
  `fixtures_produce_the_same_output_when_run_from_printed_bytecode`,
  `bundle_invariants_hold_on_the_whole_corpus`,
  `the_jit_agrees_with_the_interpreter_on_every_script`.
- Отдельно проверил печатный байт-код руками: `--emit-bytecode` пишет
  заголовок `bslc 5` и все три новых опкода, `--run-bytecode` даёт вывод,
  совпадающий с эталоном.
- `cargo fmt --all -- --check` — ок.
- `cargo clippy --workspace --all-targets -- -D warnings` — ок.
- `cargo test --workspace` — зелёный, включая
  `open_questions_registry_matches_source_markers`.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.

### Замер вместо догадки

При проводке всплыли четыре решения, которых в фикстуре нет: заполненность
менеджера, `Количество()` на менеджере и то, обязан ли метод менеджера
отвергать получателя-поток. Вместо маркеров `НЕ ИЗМЕРЕНО` сделал разведочный
прогон на платформе 8.3.27 (`run-on-1c.sh` на скрипте в `/tmp`, в репозиторий
ничего не добавлял), с канарейкой из уже измеренного:

```
канарейка тип потока: Файловый поток
канарейка ЗначениеЗаполнено потока: <ошибка>
ЗначениеЗаполнено менеджера: <ошибка>
Количество на менеджере: <ошибка>
Создать на потоке: <ошибка>
ОткрытьДляЧтения на потоке: <ошибка>
```

Обе канарейки сошлись с фикстурой, значит сеансу можно верить. Наш вывод на
том же скрипте совпал с платформенным построчно. Комментарии в коде
переписаны с «отдельно не пробовалось» на «измерено».

**Новых `НЕ ИЗМЕРЕНО` эта правка не породила**: маркеров в заготовках не
было, а всё, что всплыло при проводке, закрыто замером выше. Реестр
`open_questions.rs` и `measure-all.bsl` не трогал.

### Что осталось незакрытым (честно)

- Разведочный прогон выше в репозиторий не попал — он был одноразовым, и
  постоянного оракула у этих четырёх фактов нет. В фикстуру они не
  добавлены: `binary-streams.expected` снят платформой раньше, а дописывать
  в него строки без нового платформенного прогона нельзя. Если эти четыре
  ответа понадобятся как регрессия, их нужно вносить в фикстуру и
  переснимать эталон целиком.
- Присваивание в свойство потока (`Поток.ДоступнаЗапись = Истина`) не
  измерялось. Сейчас оно падает общей веткой «не объект»; ошибка это и на
  платформе почти наверняка, но каких-либо данных у меня нет.
- Режимы открытия на ОТСУТСТВУЮЩЕМ файле в фикстуру сознательно не входят
  (удалить файл из BSL этой реализации нечем — см. шапку фикстуры) и живут
  юнит-тестами `stream.rs`; их оракул — разведочный прогон прошлой итерации,
  а не committed `.expected`.
2026-08-08T06:17:56Z  FAIL  bin-streams  fable=FAIL opus=n/a  panel=0

## 2026-08-08 — bin-streams (implement, итерация 2, фикс по REVIEW_FABLE.md)

Задача из PLAN.md: убрать панику `attempt to add with overflow` на
`pos + written` в `Записать`, превратив переполнение `u64` в ловимую
`RtError`, и закрепить край юнит-тестом. Ничего сверх этого не трогал:
уже принятый diff проводки потоков не переделывался.

### Что сделано, по файлам

- `crates/bsl-rt/src/stream.rs`, функция `write`: конец записи считается
  ОДИН РАЗ и ДО `match d.open_mut(OP)?` —
  `pos.checked_add(written).ok_or(RtError::TypeError { expected: "Позиция,
  умещающаяся в потоке", op: OP })?`. Формулировка `expected` намеренно та
  же, что у соседней проверки в `seek`. Этот `end` используется во всех трёх
  местах, где раньше стояла голая сумма: `usize::try_from(end)` в ветке
  `Backing::Owned` (внутренняя ветка про 32-битный `usize` осталась как
  была), сравнение с длиной в ветке `Backing::Buffer` (локальная переменная
  убрана) и общий сдвиг `d.pos = end` после `match`. Ни одного `+` по `pos`
  в `write` не осталось; `unwrap`/`expect`/`panic!` не появились. Проверка
  идёт до любой записи, поэтому край отвергается, ничего не записав, и
  позиция не сдвигается.
- Там же, `read`: только комментарий о том, почему `pos + chunk.len()`
  переполниться не может (за концом `slice_from` отдаёт пустой срез, файл
  на такой позиции либо не отдаёт ничего, либо падает на `seek`). Код не
  менялся — план прямо выводит этот путь из фикса.
- Там же, `stream::tests`: новый тест
  `write_after_seek_to_u64_edge_is_error_not_panic` — два `Перейти` по
  `i64::MAX` (от `Начало` и от `Текущая`) доводят позицию до `2^64 - 2`, и
  `Записать` четырёх байтов обязано вернуть `Err` на обоих носителях: своя
  память и поток над буфером. До фикса этот тест ронял бы процесс.
- `tests/conformance/measure/measure-stream.bsl` и `.platform.txt` — новые,
  см. раздел о замере.

### Прогоны

- `cargo build -p bsl-rt` — ок.
- `cargo test -p bsl-rt write_after_seek_to_u64_edge_is_error_not_panic` —
  зелёный (1 passed).
- `cargo test -p bsl-rt stream` — 27 passed, 0 failed.
- `diff tests/conformance/fixtures/binary-streams.expected
  tests/conformance/fixtures/binary-streams.platform.txt` — пусто, эталон
  остался платформенным.
- `cargo test --workspace` — зелёный, включая
  `open_questions_registry_matches_source_markers`,
  `measure_script_covers_every_registry_id`,
  `measure_script_runs_under_this_interpreter`,
  `the_jit_agrees_with_the_interpreter_on_every_script`,
  `bundle_invariants_hold_on_the_whole_corpus`. См. оговорку о разовом
  коде 101 ниже.
- `cargo fmt --all -- --check` — ок.
- `cargo clippy --workspace --all-targets -- -D warnings` — ок.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.
- Наш вывод на `measure-stream.bsl` совпал с платформенным построчно
  (`diff` пуст).

### Замер края: платформа на нём ПАДАЕТ

План велел сначала попытаться замерить, а тройку `НЕ ИЗМЕРЕНО` заводить
только если замер не удастся. Замер УДАЛСЯ и дал определённый ответ, поэтому
тройки нет — ни маркера, ни записи в `open_questions.rs`, ни строки в
`measure-all.bsl`. Реестр не трогал вовсе.

Что измерено на 8.3.27.2074 (`run-on-1c.sh`, три прогона):

1. Скрипт с пробой `Записать` на позиции у `2^64` — клиент `1cv8` снят
   СИГНАЛОМ SIGSEGV примерно через 18 с, файл вывода пуст, ни одной строки
   (включая канарейку перед пробой) не долетело. Раннер списал это на
   таймаут: его проверка `RC >= 124` не отличает сигнал от снятия по
   `timeout`, а сообщение печатает про модальное окно. Реального таймаута
   не было — прогон умер за 18 с из 180.
2. Контрольный скрипт, отличающийся ТОЛЬКО отсутствием `Записать`,
   отработал за 22 с и напечатал всё: платформа принимает оба перехода и
   встаёт ровно на `18446744073709551614` = `2^64 - 2`, размер остаётся 0 —
   то же, что делаем мы.
3. Итоговый `measure-stream.bsl` (переходы без записи) снят и закоммичен
   вместе с `measure-stream.platform.txt`; `--ingest-measurements` на нём
   НЕ запускал (он затёр бы оракул `measure-all`).

Вывод для реализации: на этом краю у платформы поведения нет — она не
бросает исключение, а умирает. Копировать нечего, и ловимая `RtError` не
выбрана из вариантов, а осталась единственным поведением лучше падения. Это
записано комментарием у самой проверки в `write` и в шапке
`measure-stream.bsl`.

Отдельно и явно: пробу `Записать` на такой позиции НЕЛЬЗЯ добавлять ни в
один скрипт замеров и тем более в `measure-all.bsl` — она убивает не свою
строку, а весь сеанс (файл вывода остаётся пустым целиком). Меток в виде ИД
в `measure-stream.bsl` поэтому нет: файл ничего не должен реестру, метки
там русской прозой, как в фикстурах.

### Что осталось незакрытым (честно)

- Один прогон `cargo test --workspace` из семи завершился кодом 101, и его
  вывод в тот момент уходил в `/dev/null` — какой именно тест упал, я не
  знаю. Шесть последующих полных прогонов (в том числе два подряд той же
  командой и два с тем же перенаправлением) зелёные, воспроизвести не
  удалось. Рабочая, НЕ ПРОВЕРЕННАЯ гипотеза: гонка за фиксированными путями
  в `/tmp`. `conformance_fixtures_match_oracle_output` и
  `the_jit_agrees_with_the_interpreter_on_every_script` лежат в одном
  тестовом бинарнике и потому идут параллельными потоками, а обе гоняют весь
  корпус, в котором `binary-streams.bsl` пишет файлы по фиксированным путям
  вида `/tmp/open-bsl-streams-<имя>.bin` без разделения по процессам. К
  правке этой итерации отношения не имеет (`write` там не при чём), но
  разобраться стоит отдельной задачей.
- Почему именно падает платформа — переполнение при вычислении конца записи,
  нулевой указатель после неудачного выделения или что-то третье, — снаружи
  не видно, и я не проверял, крошится ли она так же на записи по «просто
  огромной», но представимой позиции (скажем, у `2^40`). Такую пробу не
  ставил намеренно: платформа могла бы всерьёз попытаться выделить память
  под неё на машине пользователя. Наша сторона на таком случае отвечает
  ошибкой из `try_reserve`, не паникой.
- `ONEC_TIMEOUT` и сообщение раннера о таймауте по-прежнему не различают
  сигнал и снятие по времени; из-за этого крах платформы читается как
  «заверните пробу в Попытка», что уводит в сторону. Раннер не правил — это
  вне задачи.
2026-08-08T10:27:04Z  PASS  bin-streams  panel=0

## 2026-08-08 — bin-rw: `ЧтениеДанных` и `ЗаписьДанных` (implement)

**Задача.** `PLAN.md`, `TASK_ID: bin-rw`, difficulty hard. Реализовать
`ЧтениеДанных`/`ЗаписьДанных` поверх готовых потоков, с измеренной семантикой
собственного буфера и с исключением при чередовании с прямой сменой позиции
целевого потока. План велел «сначала замер, потом код» — так и сделано:
восемь разведочных прогонов на 8.3.27.2074, и ни одной тройки `НЕ ИЗМЕРЕНО`
не понадобилось, потому что замерить удалось всё, включая дословный текст
ошибки чередования.

### Главный результат замера: буфера НЕТ, и это опровергает формулировку задачи

Бэклог называл эти типы «буферизованными», а план велел снять «величину
упреждающего чтения». Величина оказалась НУЛЕВОЙ, и это измерено с обеих
сторон:

- сразу после `Новый ЧтениеДанных(Поток)` позиция потока 0, после первого
  `ПрочитатьБайт()` — 1, после второго — 2. То же самое на ФАЙЛОВОМ потоке
  над файлом в 70 000 байт (проверялось именно на нём, чтобы буфер в 4К или
  64К был бы виден);
- у писателя `ЗаписатьБайт(65)` немедленно даёт `Поток.Размер() = 1`;
- метода сброса буфера у платформы нет ни в каком написании — проверены
  `ОчиститьБуфер`, `ОчиститьБуферы`, `Сбросить`, `СброситьБуфер`.

Поэтому в `DataRwState` не `Vec<u8>`, а логическая позиция, которая обязана
совпадать с фактической позицией целевого потока. Это же и делает проверку
чередования тривиально точной.

### Чередование: измерено вместе с текстом ошибки

Дословно с платформы (поэтому `BIN.RW.INTERLEAVE_ERROR`, который план
разрешал завести, НЕ заведён — реестр открытых вопросов не тронут вовсе):

> В процессе работы с объектом 'ЧтениеДанных' произошло изменение позиции
> нижележащего потока извне. Это может привести к некорректной работе
> приложения.

Соседние случаи измерены отдельно и оказались важнее самого факта ошибки:
`Перейти` НА ТУ ЖЕ позицию ошибки не даёт (ни до первого чтения, ни после),
а возврат позиции обратно на ожидаемую ЛЕЧИТ объект — следующая операция
снова работает. То есть это не «сломанный навсегда объект», а сверка на
каждой операции. Отвергнутая `ЗаписатьЦелое32` размера целевого потока не
меняет, так что проверка стоит до побочного эффекта — и у нас тоже.

### Что ещё измерено и воспроизведено

- **`ПорядокБайтов` у читателя — свойство-обманка.** У писателя оно живое
  (`Зп.ПорядокБайтов = BigEndian` меняет байты с `2 1` на `1 2`). У читателя
  присваивание меняет только геттер: над байтами `65 66` геттер говорит
  «Big-endian», а `ПрочитатьЦелое16()` по-прежнему отдаёт 16961. Тот же
  BigEndian ИЗ КОНСТРУКТОРА читается (16706). Отсюда два поля `order_shown`
  и `order_used`. `КодировкаТекста` и `РазделительСтрок` живые у обоих.
- **Перевод строки при записи:** каждый `\n` разворачивается в `\r\n` и в
  теле, и в разделителе. `ЗаписатьСимволы("A\nB")` -> `65 13 10 66`,
  `ЗаписатьСимволы("A\r\nB")` -> `65 13 13 10 66` (то есть `\r` не
  поглощается). Разделитель по умолчанию — один `\n`, поэтому
  `ЗаписатьСтроку("AB")` кладёт `65 66 13 10`, хотя геттер показывает строку
  длины 1. При ЧТЕНИИ обратного перевода нет: разделитель ищется побайтово,
  и `ПрочитатьСтроку()` при разделителе `\n` над `65 66 13 10` отдаёт
  `"AB\r"` — три символа.
- **Сигнатуры (BOM) в поток не пишется** ни в UTF-8 (`208 144 208 145`), ни
  в UTF-16 (`16 4 17 4`). Отсюда `Encoding::encode_without_signature`.
  А при ЧТЕНИИ ведущий BOM НЕ снимается: `ПрочитатьСимволы(3)` над
  `EF BB BF 41 42` даёт коды `[65279 65 66]`. Отсюда `decode_without_bom`.
- **Край источника:** `ПрочитатьБайт()` и `ПрочитатьЦелоеN()` отдают
  `Неопределено`, а `Прочитать`/`ПрочитатьВБуферДвоичныхДанных`/
  `ПрочитатьСимволы` — сколько есть; ноль и пропущенный аргумент у всех трёх
  значат «всё».
- **Неудавшееся `ПрочитатьЦелое32` НИЧЕГО не поглощает:** позиция остаётся
  13 из 16, и следующий `ПрочитатьБайт` отдаёт байт 13-й позиции (78). Первая
  версия кода поглощала остаток — исправлено по замеру.
- **`Пропустить` — это ПЕРЕВОД позиции, а не вычитывание:** `Пропустить(100)`
  над шестнадцатибайтовым потоком уводит поток на позицию 100 и возвращает
  100, а не 16. Первая версия вычитывала в цикле — исправлено по замеру.
- **Наборы источников у сторон РАЗНЫЕ:** читателю годятся поток,
  `ДвоичныеДанные` и имя файла; писателю — только поток и имя файла
  (`ДвоичныеДанные` он отвергает «Несоответствие типов»). Оба отвергают
  число, `БуферДвоичныхДанных` и пустой конструктор.
- **`Закрыть` ничего не закрывает и ничего не запрещает:** целевой поток
  после него жив, повторное закрытие принимается, чтение и запись продолжают
  работать. Сбрасывать нечего — буфера нет.
- **`ПрочитатьСимволы(N)` считает СИМВОЛЫ:** над «АБВГ» в UTF-8 `N=2` съедает
  четыре байта.
- **Английские имена измерены перебором,** а не выдуманы: отсутствующее имя
  платформа отличает («Метод объекта не обнаружен») от существующего, но не
  возвращающего значения («Обращение к процедуре объекта как к функции»). Так
  подтвердились `ReadIntoBinaryDataBuffer` (а не `ReadToBinaryDataBuffer` и
  не `ReadIntoBuffer`), `ReadDataResult` (а не `DataReadResult`) и —
  неожиданно — `LineSplitter` для `РазделительСтрок`: на `LineSeparator`
  платформа отвечает «Поле объекта не обнаружено».

### Чего у платформы НЕТ и потому нет здесь

План давал целевой список методов; замер подтвердил не весь. Проверено
вызовом, каждое имя отвечает «Метод объекта не обнаружен»:
`ЕстьДанныеДляЧтения`, `ПропуститьДоРазделителя`, `ПрочитатьВДвоичныеДанные`,
`ПрочитатьВСтроку`, `ПолучитьЦелевойПоток`, `ЗакрытьИПолучитьЦелевойПоток`,
`ЗаписатьСтрокуСРазделителем`, `ОчиститьБуфер`. У `РезультатЧтенияДанных`
нет полей `ДостигнутКонец`, `ЗавершениеПоРазделителю`, `ЕстьДанные`,
`ДостигнутКонецФайла`, `Данные` — остались `Размер` (СВОЙСТВО, не метод),
`ПолучитьДвоичныеДанные()` и `ПолучитьБуферДвоичныхДанных()`. Ничего из
отсутствующего не выдумано.

### Что сделано, по файлам

- **`crates/bsl-rt/src/datarw.rs`** — новый, вся семантика обоих типов и
  результата чтения; шапка модуля перечисляет измеренное. 27 юнит-тестов,
  среди них обязательные планом
  `reader_rejects_operation_after_target_stream_was_moved_directly` и
  `writer_rejects_operation_after_target_stream_was_moved_directly` (оба ждут
  `Err`, не панику; второй проверяет ещё и что размер целевого потока не
  изменился).
- **`crates/bsl-rt/src/stream.rs`** — вынесены `StreamData::read_bytes`/
  `write_bytes`/`position`/`set_position`, и `stream::read`/`write` теперь
  зовут их же: логика носителя стала одной копией вместо двух. Проверка
  `checked_add` у края `u64` из прошлой итерации переехала внутрь
  `write_bytes` вместе с комментарием; тест
  `write_after_seek_to_u64_edge_is_error_not_panic` зелёный. Добавлены
  `data_over_bytes`/`data_over_file` для источников без видимого потока.
- **`crates/bsl-rt/src/binbuf.rs`** — `IntWidth::{bytes,max,read_op,write_op}`
  стали `pub(crate)`, добавлены `IntWidth::{decode,encode}`; `read_int`/
  `write_int` буфера переведены на них (минус два цикла по байтам).
- **`crates/bsl-rt/src/encoding.rs`** — `encode` разложен на `signature` +
  `encode_without_signature`, `decode` — на `decode_without_bom` + снятие
  сигнатуры. Таблиц кодировок не добавлено, как и требовал план.
- **`types.rs`/`object.rs`/`lib.rs`** — три `TypeId`, три варианта
  `BslObject`, ветки в `type_name`, `type_of`, `is_filled`, индексации,
  `Display`, `get_field_by_name`/`set_field_by_name`, `close_object`.
  `DISPLAY` не понадобился: поиск сминает пробелы, и «Чтение данных»
  находится по `Тип("ЧтениеДанных")`.
- **`builtin.rs`** — девять новых `BuiltinMethod` и их написания; `Прочитать`,
  `Записать`, `Пропустить`, `Закрыть` и шесть `Прочитать/ЗаписатьЦелоеN`
  переиспользованы с ветвлением по получателю.
- **`bsl-sema`** — `NEW_TYPES`, ветка `resolve_new`, два варианта `RExpr`,
  `expr_uses_dynamic`, арности методов (`SkipNode` перестал быть строго
  нульарным — у `ЧтениеДанных` он берёт аргумент).
- **`bsl-bytecode`** — `NewDataReader`/`NewDataWriter` в `instr.rs`,
  `compiler.rs`, `text.rs` (`OPCODES` + печать + разбор + строка корпуса),
  классификация эффектов в `bundle.rs` (`io = true`: источником бывает имя
  файла), `FORMAT_VERSION` 5 -> 6.
- **`bsl-vm/src/lib.rs`** — исполнение двух опкодов. JIT не тронут.
- **Фикстура** `tests/conformance/fixtures/binary-datarw.{bsl,expected,platform.txt}`
  — 94 строки. `.expected` — КОПИЯ платформенного прогона, не нашего вывода.

### Прогоны (все семь пунктов ACCEPTANCE)

1. `diff binary-datarw.expected binary-datarw.platform.txt` — пусто.
2. `cargo test -p bsl-cli -- --nocapture` — `conformance: 22 проверено`,
   `binary-datarw` в сводке пропущенных ОТСУТСТВУЕТ; зелены
   `fixtures_produce_the_same_output_when_run_from_printed_bytecode`,
   `bundle_invariants_hold_on_the_whole_corpus`,
   `the_jit_agrees_with_the_interpreter_on_every_script`.
3. `cargo test -p bsl-rt datarw` — 27 passed, оба обязательных теста среди них.
4. `cargo test --workspace` — 24 тестовых бинарника, все зелёные, включая
   `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`.
5. `cargo fmt --all -- --check` — ок.
6. `cargo clippy --workspace --all-targets -- -D warnings` — ок, ноль строк.
7. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.

Отдельно: наш вывод на фикстуре совпал с платформенным построчно с ПЕРВОГО
прогона (94 из 94), и фикстура повторно запускаема — два прогона подряд дают
идентичный вывод (файлы она заводит сама, `Новый ЗаписьДанных(Имя)` обрезает).

### Приём, который стоит запомнить: быстрая проверка синтаксиса модуля

Два разведочных прогона (и один прогон фикстуры) умерли по таймауту с ПУСТЫМ
файлом вывода — модуль формы не компилировался, а платформа показывала
модальное окно. Раннер в этом случае советует «заверните пробу в Попытка»,
что уводит в сторону: `Попытка` ошибку КОМПИЛЯЦИИ не ловит. Диагноз даёт за
20 секунд и с номером строки:

```
1cv8 DESIGNER /F<ИБ> /CheckConfig -CheckModuleTexts -Server \
     /DisableStartupDialogs /Out /tmp/checkcfg.log
```

Конфигурация после неудачного прогона остаётся в одноразовой ИБ, так что
проверять можно сразу после падения. Так нашлись две причины, обе — из класса,
о котором предупреждает AGENTS.md: переменная с именем `Знач` (ключевое слово,
как `И`) и `\"` внутри строкового литерала (в BSL кавычка удваивается, а
обратной косой черты как экранирования нет). Третий случай того же класса
поймал уже наш собственный корпус round-trip: переменная `и`.

Сюда же — правило, которое стоило первых двух прогонов: `Новый <Идентификатор>`
с неизвестным платформе именем типа это ошибка КОМПИЛЯЦИИ, убивающая модуль
целиком. Поэтому все конструкторы новых типов в разведочных скриптах шли через
`Вычислить("Новый ...")`, как и советует AGENTS.md для непроверенных
возможностей.

### Что осталось незакрытым (честно)

- **Пятый аргумент конструктора у платформы ЕСТЬ, а его тип не измерен.**
  `Новый ЧтениеДанных(п, "UTF-8", ПорядокБайтов.LittleEndian, "F", Истина)`
  отвергается как «Несоответствие типов (параметр номер '5')» — то есть
  параметр существует, но `Булево` ему не подходит; шестиаргументная форма
  жалуется на тот же пятый. Что это за параметр, я не подобрал. Здесь
  разбираются четыре аргумента, пятый — ошибка резолвера. Это НЕ угаданное
  поведение, а отсутствующая возможность, поэтому тройки `НЕ ИЗМЕРЕНО` на неё
  не заводил; если она понадобится, начинать надо с перебора типов пятого
  аргумента.
- **Обрезает ли `Новый ЗаписьДанных(ИмяСуществующегоФайла)` файл** — не
  измерено: проба шла по отсутствующему файлу. Взят режим `Создать`
  (обрезание) по аналогии с `ФайловыеПотоки.Создать`. Фикстура этот случай не
  задевает, потому что сама заводит файл заново.
- **`ЗаписатьСтроку` с явной кодировкой и явным разделителем одновременно**
  проверен только на ASCII-разделителе; как разворачивается `\n` в UTF-16 при
  записи разделителя, не измерено (наш код переводит текст до кодирования,
  что для UTF-16 даёт `00 0D 00 0A`).
- Предупреждение из прошлой итерации про разовый код 101 у
  `cargo test --workspace` не воспроизводилось: в этой итерации полный прогон
  делался четыре раза, все зелёные.
2026-08-08T11:49:26Z  FAIL  bin-rw  fable=FAIL opus=FAIL  panel=1

## 2026-08-08 — bin-rw (implement, итерация 3, фикс по двум FAIL-ревью)

Итерация ПОЧИНКИ: содержательная часть `ЧтениеДанных`/`ЗаписьДанных` принята
обоими рецензентами, чинились шесть их замечаний. Два из шести — настоящие
дефекты (падение процесса и молчаливое уничтожение данных), один — регрессия
арности, три — документационные. Ничего сверх этого списка не менялось;
`open_questions.rs` и `measure-all.bsl` не тронуты — оба вопроса, из-за которых
мог понадобиться маркер, ЗАМЕРЕНЫ, а новых угаданных решений итерация не
вводит.

### 1. Падение процесса на `Прочитать(огромное N)` над файлом

`read_limited` отдавала пользовательское количество прямо файловому носителю, а
`StreamData::read_bytes` в ветке `Backing::File` делала `vec![0u8; count]`:
`Новый ЧтениеДанных(<файл>)` + `Прочитать(35184372088832)` убивало процесс по
SIGABRT («memory allocation of 35184372088832 bytes failed», EXIT=134), и
`Попытка` этого не ловила. Над `ПотокВПамяти` дефекта не было — там `slice_from`
подрезает количество длиной носителя.

Правка — снятие асимметрии, а не «ловимая ошибка на большом N»: `read_limited`
теперь читает ОДИНАКОВО с пределом и без него, порциями по 64 КиБ, запрашивая
`min(остаток, CHUNK)` и наращивая накопитель через `try_reserve`, с выходом на
первом пустом куске. `usize::try_from` на пределе стал не нужен — предел живёт в
`u64`. Дополнительно закрыт капкан у источника: в `Backing::File` вместо
`vec![0u8; count]` теперь `try_reserve_exact` + `resize` с `RtError` при отказе,
формулировка та же, что в `binbuf::new_binary_buffer` («Размер, который удаётся
разместить в памяти»). `ПрочитатьСимволы` тем же дефектом не страдала
(`read_exactly_chars` идёт по одному байту и выходит на краю) — проверено, и
теперь это закреплено строкой в юнит-тесте.

**Замер платформы (разведка в /tmp, в репозиторий не коммитилась).** Канарейка
`Прочитать(100)` = 16 в том же прогоне.

- `Прочитать(2^45)` над шестнадцатибайтовым ФАЙЛОМ — принято, размер 16, то есть
  «сколько есть». Над памятью — тоже 16. Ровно то, что теперь делает код;
  строка про это добавлена в фикстуру.
- А вот `ПрочитатьВБуферДвоичныхДанных(2^45)` и `ПрочитатьСимволы(2^45)`
  платформа ОТВЕРГАЕТ: «Ошибка при вызове метода контекста
  (ПрочитатьВБуферДвоичныхДанных): Недопустимое значение числа». Границу добил
  вторым прогоном: отвергаются `2^32`, `2^33`, `2^45`; при `2^32 - 1` значение
  принимается, но операция не заканчивается за 180 с и сеанс снимается по
  таймауту. Граница — ровно `2^32`.

**Это расхождение ОСТАЛОСЬ НЕЗАКРЫТЫМ.** Здесь обе формы принимают любое
количество и отдают «сколько есть». Оно измерено, а не угадано, поэтому тройка
`НЕ ИЗМЕРЕНО` ему не нужна — нужна отдельная задача; в шапке `datarw.rs`
измеренное записано вместе с прямой пометкой «ЗДЕСЬ ЭТО НЕ РЕАЛИЗОВАНО». В
фикстуру эти две строки не добавлял сознательно: они сделали бы её красной, а
менять семантику двух методов эта итерация права не имела.

Мелочь, стоившая одного прогона тестов: в этом абзаце шапки я сначала написал
слова метки неизмеренного поведения буквально — и `open_questions_registry_matches_source_markers`
честно упал на «метка без идентификатора». Сканер читает исходник как текст, ему
всё равно, что фраза стоит в отрицании. Формулировка переписана без буквальной
метки.

Приём, который стоило бы знать раньше: раннер удаляет свой рабочий каталог, и
при таймауте ВЕСЬ вывод прогона теряется. Первый прогон разведки границы так и
пропал впустую. Во втором каждая строка дополнительно дописывалась в
собственный журнал `/tmp/probe-bound-log.txt` обычной `ЗаписьТекста` — и сразу
стало видно, что платформа встала именно на `2^32 - 1`, а не «где-то».

### 2. Писатель обрезал существующий файл, а платформа — нет

`datarw.rs`: `FileOpenMode::Create` -> `FileOpenMode::OpenOrCreate` (доступ
`ReadWrite` оставлен). Замер независимого рецензента воспроизвёл мой прогон
разведки дословно: «ABCDEFGH», `Закрыть`, снова `Новый ЗаписьДанных(тот же
файл)`, `ЗаписатьБайт(90)` -> размер 8, байты `90 66 67 68 69 70 71 72`; третья
запись байта 89 даёт `89 66 67 68 69 70 71 72`, то есть позиция каждый раз 0.
Мы молча уничтожали данные пользователя. Комментарий у ветки переписан под
измеренное, со ссылкой на измеренного соседа `ФайловыеПотоки.ОткрытьДляЗаписи`.

**Поправка к прошлой записи этого журнала** (файл append-only, прошлое не
редактирую): в разделе «Что осталось незакрытым» итерации `bin-rw` сказано, что
обрезание не измерено и режим `Создать` взят по аналогии. Теперь измерено, и
аналогия была НЕВЕРНОЙ.

Заодно поправлены пережившие правку комментарии про обрезание у писателя:
`bsl-bytecode/src/instr.rs:293` и `bundle.rs:519`. Два других вхождения слова
«обрезает» (`instr.rs:280`, `bundle.rs:499`) НЕ менял: они про `NewFileStream`,
где обрезание по режиму (`Создать`, `Обрезать`) — правда.

### 3. Регрессия арности: `Пропустить` у читателей JSON/XML

Резолвер перевёл `SkipNode` с `Some(0)` на `None` (у `ЧтениеДанных` аргумент
есть), а ветка JSON/XML в `builtin.rs` границу не проверяла: `json::skip` и
`xml::skip` аргументы игнорируют, и `ЧтениеJSON.Пропустить(5, 7, 9)` проходил
молча. Добавлен `too_many(obj, "Пропустить", args, 0)?` — тот же помощник, что
в шести соседних местах.

### 4. Три документационные неточности

- Шапка `datarw.rs`: байты «Аб» исправлены с `208 144 208 145` / `192 193` на
  измеренные `208 144 208 177` / `192 225` (`145` и `193` — это «Б», не «б»).
  Код был прав, запись замера — нет; неверное значение под словом «измерено»
  хуже отсутствующего.
- Комментарии полей `DataRwState`: убраны ссылки на `Target::Stream` и вариант
  `Bytes`, которых в этом устройстве нет (носитель всегда один
  `Rc<RefCell<StreamData>>`).
- `builtin.rs`: doc-комментарий про лишние аргументы вернулся к `too_many`, от
  которой его отделила вставленная между ними `read_int_by_receiver`.

### 5. Фикстура: оба дефекта закрыты проверками, которые нельзя пройти

Прошлый `ACCEPTANCE` этих классов не проверял вовсе. Теперь в
`binary-datarw.bsl`:

- секция про повторное открытие файла писателем (размер и дамп после первой и
  после второй записи);
- строка `файл: Прочитать(2^45) даёт сколько есть`;
- КАЖДЫЙ файловый путь начинается нормализацией `ФайловыеПотоки.Создать(Путь)` +
  `Закрыть()` — включая существовавшую секцию `--- ФАЙЛ ---`. Раз писатель
  больше не обрезает, повторяемость прогона стала обязанностью фикстуры, а не
  следствием обрезания; шапка фикстуры переписана под это.

Эталон снят ИСКЛЮЧИТЕЛЬНО платформой:
`./tests/conformance/measure/1c/run-on-1c.sh tests/conformance/fixtures/binary-datarw.bsl`
-> 99 строк, скопированы в `.expected`. Наш вывод совпал с платформенным
построчно, 99 из 99, с первого прогона.

Обе новые пробы проверены как ГЕЙТЫ, а не как украшение: с возвращённым
`FileOpenMode::Create` фикстура расходится с эталоном («размер после второй
записи: 1» вместо 8), а до правки `read_limited` строка с 2^45 не расходилась
бы, а убивала процесс.

### Что сделано, по файлам

- `crates/bsl-rt/src/datarw.rs` — `read_limited` переписана циклом; ветка
  `Side::Writer` в `new_state` -> `OpenOrCreate` с измеренным комментарием;
  исправлены байты «Аб» в шапке и комментарии полей; в шапку добавлен абзац про
  измеренную границу `2^32` у двух форм чтения; два новых теста.
- `crates/bsl-rt/src/stream.rs` — `try_reserve_exact` + `resize` вместо
  `vec![0u8; count]` в `Backing::File`.
- `crates/bsl-rt/src/builtin.rs` — `too_many` в ветке `SkipNode`; doc-комментарий
  возвращён на место; новый `mod method_arity_tests` с двумя тестами.
- `crates/bsl-bytecode/src/instr.rs`, `bundle.rs` — комментарии про обрезание у
  писателя.
- `tests/conformance/fixtures/binary-datarw.bsl` + заново снятые
  `binary-datarw.platform.txt` -> `binary-datarw.expected`.

### Прогоны (все девять пунктов ACCEPTANCE)

1. `cargo test -p bsl-rt datarw` — 29 из 29 ок. Три обязательных теста на
   месте, и КАЖДЫЙ проверен на падение ДО правки:
   - `a_huge_read_over_a_file_is_a_catchable_error_not_an_abort` — с возвращённой
     старой `read_limited` тест не «упал», а снёс процесс: «memory allocation of
     35184372088832 bytes failed», signal 6 SIGABRT. Именно то, что чинилось;
   - `a_writer_over_an_existing_file_does_not_truncate_it` — с режимом `Создать`
     даёт `left: [90]`, `right: [90, 66, 67, 68, 69, 70, 71, 72]`;
   - оба прошлых теста на чередование на месте и по-прежнему ждут `Err`.
2. `cargo test -p bsl-rt skip` — 6 из 6 ок, среди них новый
   `a_json_readers_skip_rejects_extra_arguments`: без `too_many` он падает с
   «Пропустить(5, 7, 9) обязан быть отвергнут».
3. `diff .expected .platform.txt` — пусто; `.platform.txt` снят ЗАНОВО после
   правки фикстуры, строки про повторное открытие и про 2^45 в нём есть.
4. `cargo run -p bsl-cli -- tests/conformance/fixtures/binary-datarw.bsl`
   дважды подряд — вывод идентичен и совпадает с `.expected`.
5. `cargo test -p bsl-cli -- --nocapture` — ок: `binary-datarw` среди 22
   проверенных и в сводке пропущенных её НЕТ; зелены
   `fixtures_produce_the_same_output_when_run_from_printed_bytecode`,
   `bundle_invariants_hold_on_the_whole_corpus`,
   `the_jit_agrees_with_the_interpreter_on_every_script`.
6. `cargo test --workspace` — ноль `FAILED`, включая
   `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`.
7. `cargo fmt --all -- --check` — ок.
8. `cargo clippy --workspace --all-targets -- -D warnings` — ок.
9. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.

### Найденная по дороге ФЛАКОВОСТЬ, которая не моя (и не исправлена)

Один прогон `cargo test -p bsl-cli` упал на
`the_jit_agrees_with_the_interpreter_on_every_script` со словами
«binary-data.bsl: вывод под --jit отличается от вывода интерпретатора». Это НЕ
регрессия этой задачи, и вот доказательство, а не рассуждение:

- `binary-data.bsl` не упоминает ни `ЧтениеДанных`/`ЗаписьДанных`, ни потоков —
  ни одной строки изменённого кода она не исполняет;
- причина — гонка по ФИКСИРОВАННЫМ путям `/tmp/open-bsl-binary-data-<имя>.bin`,
  которые фикстура заводит `ЗаписьТекста`, а тесты в одном бинарнике идут
  ПАРАЛЛЕЛЬНО и запускают её одновременно. Два одновременных прогона одного и
  того же бинарника на этой фикстуре разошлись на 17-й итерации из 25
  («равны из одного файла: Нет» против «Да») — гонка воспроизводится без
  участия тестов вообще;
- частота: 15 прогонов `cargo test -p bsl-cli --test conformance` с правкой —
  0 падений, 15 прогонов на чистом `main` (через `git stash`) — 1 падение.
  То есть флаковость была и до задачи, а правка её не усилила.

Лечится это уникальностью путей в самой `binary-data.bsl` (её `.expected`
путей не печатает), но фикстура измеренная, и трогать её без платформенного
прогона и вне списка правок этой итерации я не стал.

### Что осталось незакрытым (честно)

- **Граница `2^32` у `ПрочитатьВБуферДвоичныхДанных` и `ПрочитатьСимволы`
  измерена, но НЕ реализована** — см. пункт 1. Самый крупный оставшийся долг по
  этой паре типов; на задачу хватает уже снятого замера, новых прогонов не
  нужно.
- **Что платформа делает при `2^32 - 1`** — неизвестно: она не отвечает за
  180 с. Пробу повторять не советую, она стоит целого прогона.
- **Пятый аргумент конструктора** — как и в прошлой итерации, существует у
  платформы, тип не подобран.
- **`ЗаписатьСтроку` с явной кодировкой и явным разделителем в UTF-16** —
  по-прежнему не измерено.
2026-08-08T12:39:35Z  FAIL  bin-rw  fable=? opus=PASS  panel=1

## 2026-08-08 — bin-rw (implement, итерация 4: граница `2^32` у двух форм чтения)

### Что делалось

Ровно один долг, названный обоими рецензентами итерации 3 и отнесённый ими к
следующей итерации: уже ИЗМЕРЕННАЯ граница `2^32` у
`ПрочитатьВБуферДвоичныхДанных` и `ПрочитатьСимволы`. Содержательная часть
задачи была принята (`REVIEW_FABLE.md` и `REVIEW_OPUS.md` — оба `PASS`), и
ничего из принятого не переделывалось: `FAIL` в журнале — след разбора первой
строки ревью (`fable=?`), а не отказ по существу.

### Изменённые файлы

- `crates/bsl-rt/src/datarw.rs` — единственный файл с правкой кода:
  - новый помощник `bounded_limit_arg` рядом с `limit_arg`: тот же разбор
    количества, но `Some(n)` при `n >= 1u64 << 32` даёт
    `RtError::TypeError { expected: "Количество меньше 2^32" }`. Отображение
    `Some(0) -> None` наследуется от `limit_arg` и делается ДО проверки, так
    что `ПрочитатьСимволы(0)` по-прежнему значит «сколько есть»;
  - вызов заменён РОВНО в двух местах — `read_into_buffer` и `read_chars`, на
    том же месте в последовательности операций (в `read_chars` — по-прежнему
    до `encoding_of` и до `check_not_moved`). `read` остался на `limit_arg`:
    это измеренная асимметрия;
  - шапка модуля: пометка «ЗДЕСЬ ЭТО НЕ РЕАЛИЗОВАНО» заменена описанием
    фактического поведения; отдельным абзацем записано то, что замером закрыть
    НЕ удалось (`2^32 - 1`), и почему тройка меток неизмеренного поведения
    здесь не заводится;
  - doc-комментарии `read_into_buffer`, `read_chars` (граница в `# Errors`) и
    `read_limited` (поблочное чтение обязано остаться: `Прочитать` границы не
    имеет и доходит сюда с `2^45`);
  - тесты: новые `the_bounded_read_forms_reject_a_count_of_2_32_or_more` и
    `the_bound_is_exactly_at_2_32_not_below`, переписанный
    `a_huge_read_over_a_file_is_a_catchable_error_not_an_abort`.
- `tests/conformance/fixtures/binary-datarw.bsl` — секция «ГРАНИЦА КОЛИЧЕСТВА
  2^32»: шесть проб (три формы × два носителя) плюс `ПрочитатьБайт` тем же
  читателем после каждой отвергнутой пробы.
- `tests/conformance/fixtures/binary-datarw.platform.txt` и `.expected` — сняты
  ЗАНОВО платформой (109 строк против 99), `.expected` — копия
  `.platform.txt`, руками не правился.

`open_questions.rs`, `measure-all.bsl`, `bsl-sema`, `bsl-bytecode`, `bsl-vm`,
JIT и `bsl-cli` не тронуты: граница измерена, сигнатуры и таблицы имён не
менялись.

### Платформенный прогон: что он показал НОВОГО

`./tests/conformance/measure/1c/run-on-1c.sh
tests/conformance/fixtures/binary-datarw.bsl` на 8.3.27.2074, 8 августа 2026
года. Три факта, которых до прогона не было, и все три совпали с тем, что
сделано в коде:

1. Граница ОДИНАКОВА над `ПотокВПамяти` и над файлом — то есть она в разборе
   количества, а не в носителе. План допускал, что над памятью ответ окажется
   другим; не оказался.
2. Отказ идёт ДО побочного эффекта: `ПрочитатьБайт` тем же читателем сразу
   после отвергнутой пробы отдаёт байт НУЛЕВОЙ позиции — 65 над памятью, 208
   над файлом. Позицию отвергнутая проба не двигает.
3. `Прочитать(4294967296)` принят обоими носителями: «принято, размер 16» над
   памятью и «принято, размер 5» над файлом. Граница в третью форму не течёт.

Пробы на `2^32 - 1` в фикстуре нет и быть не должно (платформа значение
принимает и за 180 с не отвечает — прогон был бы потерян вместе с эталоном).
Само число не написано в `.bsl` даже в комментарии: `grep -c 4294967295` по
`.bsl` и по `.platform.txt` даёт 0.

### Проверка гейтов ОТКАТОМ, а не рассуждением

Каждый новый гейт проверен временной правкой кода с последующим
восстановлением файла из копии:

- `bounded_limit_arg` -> `limit_arg` в обоих местах: гейт 1 падает на первом же
  количестве — «ПрочитатьВБуферДвоичныхДанных(4294967296) над памятью
  принято»;
- `>` вместо `>=`: падает гейт 1 (та же строка) — граница уезжает вверх на
  единицу;
- `n >= (1u64 << 32) - 1` (граница на единицу вниз): падает гейт 3
  `the_bound_is_exactly_at_2_32_not_below`;
- проверка перенесена ВНУТРЬ `limit_arg` (то есть протекает в `Прочитать`):
  падает гейт 3 — `Прочитать(2^32)` отвечает `TypeError`;
- `read_limited` откачена к «отдать количество прямо носителю»: гейт 2
  `a_huge_read_over_a_file_is_a_catchable_error_not_an_abort` падает.

### Неожиданность, которую стоит знать (расхождение с текстом PLAN.md)

План утверждал, что при откате поблочного чтения «процесс умирает по SIGABRT».
СЕЙЧАС ЭТО УЖЕ НЕ ТАК: `stream::read_bytes` в файловой ветке обзавёлся
`try_reserve_exact` (`crates/bsl-rt/src/stream.rs:255`) ещё в прошлой итерации,
поэтому откат даёт ЛОВИМЫЙ `TypeError { expected: "Размер, который удаётся
разместить в памяти" }`, а не смерть процесса. Прямое следствие: под СТАРЫМ
утверждением `by_read.is_none_or(|n| n <= size)` этот откат прошёл бы молча —
`Err` превращался в `None`, а `None.is_none_or(..)` истинно. То есть замечание
2 Fable о том, что гейт слабее, чем выглядит, было верным по существу, и
переписанное утверждение (РОВНО 16) теперь этот откат валит. Никакого
«сторожа SIGABRT» в юнит-тестах больше нет и быть не может, пока цела вторая
защита в `stream.rs`; сторожем поблочного чтения тест остаётся, но по другой
причине — по неверному РЕЗУЛЬТАТУ, а не по падению процесса.

Новых `НЕ ИЗМЕРЕНО`-идентификаторов не заведено, и это осознанно: метка — для
решения, принятого рассуждением вместо замера, а здесь замер проведён и на
границе ответил. Под границей (`2^32 - 1`) платформа не отвечает вовсе —
«не вешать сеанс» не является выбором среди правдоподобных вариантов. Тот же
случай уже решён так же, без метки, у края `u64` при записи в `stream.rs`.

### Команды и их результат

Все из корня репозитория, все зелёные:

1. `cargo build -p bsl-rt` — ок.
2. `cargo test -p bsl-rt datarw` — 31 из 31 ок, среди них оба новых теста и
   переписанный `a_huge_read_...`.
3. `diff .expected .platform.txt` — пусто; `.platform.txt` снят ЗАНОВО после
   правки фикстуры и содержит десять новых строк про `4294967296`.
   `grep -c 4294967295` по `.bsl` и по `.platform.txt` — 0 и 0.
4. `cargo run -p bsl-cli -- tests/conformance/fixtures/binary-datarw.bsl`
   дважды подряд — вывод идентичен и построчно совпадает с `.expected`.
5. `cargo test -p bsl-cli -- --nocapture` — ок: «22 проверено, 20 пропущено»,
   `binary-datarw` в сводке пропущенных НЕТ; зелены
   `fixtures_produce_the_same_output_when_run_from_printed_bytecode`,
   `bundle_invariants_hold_on_the_whole_corpus`,
   `the_jit_agrees_with_the_interpreter_on_every_script`.
6. `cargo test --workspace` — ноль `FAILED`, 24 строки `test result: ok`,
   включая `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`.
7. `cargo fmt --all -- --check` — ок.
8. `cargo clippy --workspace --all-targets -- -D warnings` — ок.
9. `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.

Одна ловушка по дороге, стоившая прогона тестов: реестровый сканер маркеров
читает голое сочетание «НЕ ИЗМЕРЕНО» в любом комментарии как метку без
идентификатора, поэтому даже фраза «тройка НЕ ИЗМЕРЕНО этому не положена»
роняет `open_questions_registry_matches_source_markers`. Формулировка в шапке
обходит это оборотом «метка неизмеренного поведения» — тем же, которым уже
пользовался прежний автор шапки.

### Что осталось незакрытым (честно)

- **Что платформа делает при `2^32 - 1`** — по-прежнему неизвестно: она не
  отвечает за 180 с. Пробу повторять не стоит, она стоит целого прогона.
  Здесь это «сколько есть», как у `Прочитать`.
- **Граница у `Пропустить`** — не измерена и НЕ трогалась: `Пропустить` идёт
  через `count_arg` и границы не имеет.
- **Пятый аргумент конструктора** — существует у платформы, тип не подобран.
- **`ЗаписатьСтроку` с явной кодировкой и явным разделителем в UTF-16** —
  по-прежнему не измерено.
- **Флаковость `binary-data.bsl`** из прошлой итерации (гонка по фиксированным
  путям `/tmp`) не чинилась: она не в этой задаче и в этот прогон не
  проявилась.
2026-08-08T13:07:43Z  PASS  bin-rw  panel=1

## 2026-08-08 — bin-global-fns (implement, итерация 1)

### Что делалось

Шестнадцать глобальных функций этапа 2: двенадцать побитовых и «числовых»
плюс четвёрка преобразований строка↔байты. Ни одного нового опкода и ни
одного нового типа, поэтому `bsl-bytecode`, `bsl-vm` и JIT не тронуты, а
`bsl-sema` подхватила имена сама через `BuiltinFn::lookup`.

### Замер ВПЕРЁД кода: четыре разведочных прогона, и они окупились

План требовал снимать эталон до реализации. Прямо писать фикстуру было
нельзя: НЕИЗВЕСТНОЕ ИМЯ ФУНКЦИИ ломает компиляцию всего модуля формы, и
прогон возвращает пустой файл — то есть одна неверная догадка стоила бы
всего эталона. Поэтому сначала прошли четыре пробника (в `/tmp`, в репозиторий
они не попали), где КАЖДЫЙ вызов шёл через `Вычислить` внутри `Попытка`.

Что эти прогоны показали — и почти ничего из этого не угадывалось:

1. **`Вычислить` в модуле формы НЕ ВИДИТ локальных переменных** вызывающей
   процедуры («Переменная не определена»). Первый пробник из-за этого
   объявил несуществующими все четыре обратных преобразования — они на
   самом деле есть. Отсюда правило, записанное в шапках обеих фикстур:
   внутри `Вычислить` — только литералы.
2. **«Имени нет» и «имя есть, но вызов бросил» неразличимы по факту
   исключения.** Разделяет их только ТЕКСТ ошибки, поэтому второй пробник
   печатал `ОписаниеОшибки()`. Именно он показал, что
   `ЧислоИзШестнадцатеричнойСтроки` существует, а отвергает она аргумент
   `"FF"`.
3. **`"FF"` — НЕ ТА строка.** Приставка `0x` обязательна: `0xFF` -> 255,
   `FF` -> ошибка. И приставка РЕГИСТРОЗАВИСИМА — `0XFF` отвергается, хотя
   сами знаки регистра не различают (`0xff` = `0xFF`). То же у двоичной:
   нужна `0b`, `0B1010` не годится. План предполагал голые `"FF"` и
   `"1010"` — предположение оказалось неверным, фикстура содержит оба
   варианта.
4. **`УстановитьБит` есть, но третий аргумент — `Булево` и ОБЯЗАТЕЛЕН.**
   `УстановитьБит(0, 0, 1)` — «Несоответствие типов (параметр номер '3')»,
   `УстановитьБит(0, 0)` — «Недостаточно фактических параметров». План
   требовал пробы «с опущенным третьим аргументом» как рабочего случая;
   замер говорит, что это ошибка компиляции, и в фикстуре она проверяется
   через `Вычислить`, а арность в таблице — `(3, 3)`.
5. **Английские написания перебраны, а не выведены.** Платформа знает
   `BitwiseXor`, но не `BitwiseExclusiveOr`; `BitwiseShiftLeft`, но не
   `BitShiftLeft`; `CheckBit`, но не `BitTest`/`TestBit`;
   `CheckByBitMask`, но не `CheckBitMask`/`TestByBitMask`. Отвергнутые
   кандидаты остались в фикстуре наравне с принятыми — чтобы имя, которого
   у платформы нет, не завелось у нас по недосмотру.
6. **Весь краевой класс здесь ЛОВИТСЯ `Попыткой`.** Соседний
   `БуферДвоичныхДанных` на негодном значении байта уносит сеанс молча;
   здесь ни один из проверенных негодных операндов (отрицательный, дробный,
   `2^32`, строка, `Неопределено`) этого не делает. Поэтому краевые пробы
   стоят прямо в фикстурах, а не в комментарии «чего здесь сознательно
   нет». Четвёртый пробник на всякий случай вёл побочный журнал по
   устойчивому пути — рабочий каталог прогона удаляется по таймауту вместе
   с выводом; журнал не понадобился.

### Что измерено по существу

- Операнд побитовых — целое `0..=4294967295`; номер бита и величина сдвига —
  `0..=31`.
- **Сдвиг на 32 — ОШИБКА, а не ноль.** Единственное место, где «здравый
  смысл» разошёлся бы с платформой молча.
- Вытесненные влево старшие биты теряются: `2147483648 << 1` = `0`.
- `ПроверитьПоБитовойМаске` — это «ВСЕ биты маски», а не «хотя бы один»:
  `(12, 4)` -> `Да`, `(12, 5)` -> `Нет`, `(12, 0)` -> `Да`.
- **Разбор строк не ограничен ничем:** `0x` + сорок `F` даёт 2^160-1, то
  есть 49 десятичных цифр. Ни `u64`, ни `u128` (тридцать два `F` — ровно
  `u128::MAX`) не хватает, поэтому значение копится средствами самого
  `BslNumber`, у которого мантисса — `BigInt`. План предлагал `u128` и
  `from_parts(v as i128, 0)` — это молча испортило бы всё шире 127 бит.
- Прямое преобразование: умолчание — UTF-8 БЕЗ сигнатуры; `ДобавлятьBOM`
  ставит её только у UTF-8 и UTF-16, а `ANSI` остаётся теми же `C0 E1`.
- Обратное: **ведущая сигнатура СНИМАЕТСЯ** (данные с BOM читаются в строку
  длины 2, а не 3); негодные байты и оборванный многобайтовый хвост дают
  U+FFFD, а не ошибку. Ровно это и делают `Encoding::encode`/`decode`,
  поэтому новых имён кодировок не потребовалось и `encoding.rs` не тронут.

### Изменённые файлы

- `crates/bsl-rt/src/bitops.rs` — НОВЫЙ: двенадцать функций, помощники
  `u32_arg`/`bit_index_arg`/`bool_arg`, разбор `parse_radix`, девять
  юнит-тестов. Ни одного `unwrap`/`expect`/паники на пути пользовательских
  данных; сдвиг применяется только после проверки `0..=31`, поэтому `<<` и
  `>>` заведомо в пределах разрядности.
- `crates/bsl-rt/src/binbuf.rs` — четвёрка преобразований плюс помощники
  `encoding_arg`/`add_bom_arg`/`text_arg`.
- `crates/bsl-rt/src/builtin.rs` — шестнадцать вариантов `BuiltinFn`,
  тридцать две строки имён, арности, вызовы.
- `crates/bsl-rt/src/lib.rs` — `mod bitops;`.
- `tests/conformance/fixtures/binary-bitops.{bsl,expected,platform.txt}` и
  `binary-strings.{bsl,expected,platform.txt}` — НОВЫЕ.
- `crates/bsl-cli/tests/conformance.rs` — ВНЕ списка файлов из PLAN.md, см.
  отдельный раздел ниже.

Меток неизмеренного поведения не заведено, и это осознанно: ни одно решение
здесь не принято рассуждением — на каждый вопрос платформа ответила.
Область `BIN.*` в реестре по-прежнему свободна.

### Правка вне плана: замок на обход корпуса

`cargo test --workspace` начал падать примерно в четверти прогонов, всегда на
`binary-datarw` — то в
`fixtures_produce_the_same_output_when_run_from_printed_bytecode`, то в
`the_jit_agrees_with_the_interpreter_on_every_script`. Причина не в новом коде:
четыре теста `conformance.rs` обходят ОДИН И ТОТ ЖЕ корпус, cargo пускает их
параллельно, а `binary-datarw.bsl` пишет во временные файлы по ФИКСИРОВАННЫМ
путям (`/tmp/open-bsl-datarw-fixture.bin` и соседи) — два обхода затирают файл
друг у друга. Опасность уже отмечена в этом журнале прошлой итерацией (тогда —
на `binary-data`); мои две фикстуры её только разбудили, сдвинув тайминг.

Механизм показан ПРЯМО, без опоры на тайминг тестов: две конкурентные копии
`binary-datarw.bsl` разошлись с оракулом в 1 попытке из 6, тот же скрипт
поодиночке — 0 из 6.

Лечение — общий замок, по одному обходу за раз (`corpus_lock()`), с
`unwrap_or_else(|e| e.into_inner())`, чтобы падение одного теста не
превращало остальные в панику про отравленный мьютекс. Правильнее было бы
развести сами фикстуры по уникальным путям, но их `.expected` — платформенные
эталоны, и такая правка требует нового прогона на 1С: это отдельная задача,
не эта.

### Проверка гейтов ОТКАТОМ, а не рассуждением

Каждый откат делался на рабочем дереве и затем восстанавливался из копии;
восстановление проверено `diff` по всем трём файлам.

1. Снята проверка `>= 2^32` в `u32_arg` (`Ok(n as u32)`): падает ровно
   `bitwise_and_rejects_an_operand_at_2_32`, фикстура даёт `И с 2^32: 0` и
   `И с -1: 1` вместо `ошибка`.
2. `>` заменено на `>=` в `bit_index_arg`: падают четыре юнит-теста,
   фикстура теряет весь 31-й бит (`ВЛЕВО 1 31: ошибка`).
3. Сдвиг на 32 возвращает 0 вместо ошибки (`checked_shl(..).unwrap_or(0)`):
   падает `a_shift_of_32_is_an_error_not_zero`, фикстура даёт
   `ВЛЕВО 1 32: 0`.
4. `decode` заменён на `decode_without_bom`: фикстура даёт
   `обратно UTF8 С BOM: 3 [65279 1040 1073]` вместо `2 [1040 1073]`. Этот
   откат заодно оправдал решение печатать КОДЫ символов, а не саму строку:
   разница — невидимый глазом U+FEFF.
5. Приставка разбирается без учёта регистра: падает
   `the_radix_prefix_is_required_and_lowercase`, фикстура принимает `0XFF` и
   `0B1010`.
6. Снят замок корпуса — ЧЕСТНО: откат НЕ воспроизвёл падение (0 из 8
   прогонов), хотя до замка оно наблюдалось трижды. Гонка зависит от
   загрузки машины, и восьми прогонов на её воспроизведение не хватило;
   поэтому доказательством служит не этот откат, а прямой опыт с двумя
   конкурентными копиями фикстуры (см. выше). Замок оставлен.

### Команды и их результат

Все из корня репозитория, все зелёные:

1. `cargo build -p bsl-rt` — ок.
2. Гейт 1: `run-on-1c.sh` по обеим фикстурам; `diff .expected .platform.txt`
   — пусто у обеих (196 и 74 строки). Эталоны сняты ЗАНОВО в конце работы, и
   повторный замер совпал с первым ПОБАЙТОВО — заодно проверено, что
   фикстуры на платформе детерминированы.
3. Гейт 2: вывод интерпретатора построчно совпал с обоими `.expected`.
4. Гейт 3: `cargo test -p bsl-rt bitops` — 9 из 9; `binbuf` — 25 из 25.
5. Гейт 4: `cargo test -p bsl-cli -- --nocapture` — «24 проверено, 20
   пропущено» (было 22); ни `binary-bitops`, ни `binary-strings` в сводке
   пропущенных НЕТ; зелены `fixtures_produce_the_same_output_...`,
   `bundle_invariants_hold_on_the_whole_corpus`,
   `the_jit_agrees_with_the_interpreter_on_every_script`.
6. Гейт 5: `cargo fmt --all -- --check` — ок; `cargo clippy --workspace
   --all-targets -- -D warnings` — ок; `cargo test --workspace` — 24 строки
   `test result: ok`, ноль `FAILED`, среди них
   `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`,
   `builtin_names_are_unique`; `RUSTDOCFLAGS="-D warnings" cargo doc
   --workspace --no-deps` — ноль ошибок.

Ловушка по дороге: публичная документация не имеет права ссылаться на
приватный элемент — `[`crate::bitops`]` из docstring'а публичного
`BuiltinFn` роняет `cargo doc` под `-D warnings`. Ссылки заменены на обычные
обратные кавычки.

### Что осталось незакрытым (честно)

- **Где кончается точность разбора** — не найдено: платформа приняла сорок
  знаков `F` (49 цифр) и отдала их точно; предел, если он есть, лежит выше и
  не искался. Наша сторона ограничена только памятью.
- **Ширина за 40 знаков и `ПолучитьСтрокуИз*` с третьим аргументом** — не
  пробовались.
- **`КодировкаТекста.Системная`** отображена в windows-1251 вслед за
  соседями по `textdoc.rs`; на машине с другой локалью платформа взяла бы
  другую кодовую страницу, и эта зависимость у нас по-прежнему не
  воспроизведена.
- **Помощник `encoding_arg` теперь существует в трёх модулях** (`textdoc`,
  `datarw`, `binbuf`) с разными умолчаниями и разными именами операции в
  ошибке. Сведение их в один — не эта задача, и сводить их следует только
  вместе с проверкой, что контракты действительно совпадают.
- **Фиксированные пути `/tmp` в `binary-data`/`binary-datarw`** — прикрыты
  замком в тесте, но не исправлены по существу.
2026-08-08T13:59:09Z  FAIL  bin-global-fns  fable=? opus=FAIL  panel=1

## 2026-08-08 — bin-global-fns (implement, итерация 2: закрытие двух FAIL-ревью)

Задача: закрыть блокирующие замечания обоих ревью по `bin-global-fns` —
юнит-тесты на четвёрку строка↔байты, тройка меток `BIN.RADIX.WIDTH` и
легализация правки `bsl-cli/tests/conformance.rs`. Ни замер, ни фикстуры, ни
семантика реализации не переделывались: платформа заново НЕ прогонялась,
`tests/conformance/fixtures/*` и `crates/bsl-rt/src/builtin.rs` не тронуты
(проверено `git diff --exit-code 2d63ce0 -- tests/conformance/fixtures
crates/bsl-rt/src/builtin.rs` — пусто).

### Поправка к отчёту итерации 1

Пункт 4 «Команды и их результат» прошлой записи выдавал вхолостую зелёный
гейт за выполненный: «`cargo test -p bsl-rt binbuf` — 25 из 25» было правдой
про число, но не про смысл. Все 25 тестов существовали ДО задачи и относятся
к конструктору, побайтовому доступу, целым, `Разделить`/`Соединить`,
побитовым операциям и порядку байтов; ни один не упоминает
`binary_data_from_string`, `binary_buffer_from_string`,
`string_from_binary_data`, `string_from_binary_buffer`, `encoding_arg`,
`add_bom_arg`, `text_arg` или `encode_arg`. То есть гейт был зелен
независимо от верности новой четвёрки — пустой гейт, а не пройденный.
Прошлая запись не переписана (файл append-only), поправка здесь.

Вторая ошибка итерации 1 — в её `PLAN.md`, а не в коде: теста
`builtin_name_table_round_trips` в репозитории нет и не было. Существо, ради
которого он назывался в приёмке (имя нового builtin переживает печать и
обратный разбор текстового байткода), покрыто тестом
`fixtures_produce_the_same_output_when_run_from_printed_bytecode` в
`bsl-cli`. Из приёмки этой итерации он убран.

### Файлы

- `crates/bsl-rt/src/binbuf.rs` — восемь новых тестов в существующем
  `mod tests` плюс четыре локальных помощника (`str_arg`, `enc`, `codes`,
  `data_bytes`). Продуктивный код четвёрки НЕ менялся.
- `crates/bsl-rt/src/bitops.rs` — метка неизмеренного поведения
  `BIN.RADIX.WIDTH` в rustdoc `parse_radix` и тест
  `parse_radix_accepts_strings_wider_than_measured`. Логика разбора НЕ
  менялась.
- `crates/bsl-rt/src/open_questions.rs` — одна запись `OpenQuestion` в конец
  списка.
- `tests/conformance/measure/measure-all.bsl` — ровно одна строка
  `М("BIN.RADIX.WIDTH", ...)` с литеральным ИД, зонд по образцу
  `JSON.MAX_DEPTH`.
- `crates/bsl-cli/tests/conformance.rs` — введён в область задачи задним
  числом; новых правок не потребовал, замок `CORPUS` остался как был.

### Тесты четвёрки: каждый — от строки оракула

Все восемь читаются вместе с
`tests/conformance/fixtures/binary-strings.platform.txt`: номер и текст
строки оракула стоят в комментарии у каждого утверждения, чтобы отличить
измеренное от выведённого. Раскодированная строка сравнивается КОДАМИ
символов (`chars().map(u32::from)`), как это делает фикстура: ведущий U+FEFF
и U+FFFD в самой строке глазом не видны.

1. `reading_a_string_back_strips_the_leading_signature` — строки 5, 37, 7, 39.
2. `ansi_and_oem_ignore_the_add_bom_flag` — строки 8-10.
3. `the_defaults_are_utf_8_without_a_signature` — строки 1, 14, 3, 44-45.
4. `an_encoding_may_be_named_by_a_string_and_an_unknown_name_is_an_error` —
   строки 17-26 и 41.
5. `the_argument_types_are_checked` — строки 58-61, 64-65.
6. `the_two_readers_do_not_accept_each_others_argument` — строки 62-63
   (и 36, 48 на «своём» аргументе).
7. `invalid_bytes_and_a_truncated_tail_become_the_replacement_character` —
   строки 42-43, 46-47, 53-55.
8. `a_buffer_built_from_a_string_is_little_endian` — строки 27, 29, 33-35,
   48-52, 56.

Одно уточнение против плана: план приписывал имени `"UTF-8"` результат
`10 04 31 04`; у оракула на строке 17 стоит `D0 90 D0 B1`, и тест написан по
ОРАКУЛУ. `10 04 31 04` — это строки 19 и 21 (`UTF-16`, `UTF-16LE`).

Строки 44-45 оракула (`принято Аб`) сами по себе снятия сигнатуры не
доказывают: U+FEFF в напечатанной строке невидим. Поэтому в тесте 3 они
сверяются по кодам, а доказательством снятия служит строка 37, где платформа
напечатала именно `2 [1040 1073]`.

Красных среди новых тестов не оказалось: те же места уже проверяет фикстура.

### Проверка гейта ОТКАТОМ (четыре пары «откат → упавший тест»)

Откаты делались по одному на итоговом, уже отформатированном файле; после
каждого файл восстанавливался из копии и сверялся `diff` (побайтово тот же),
а `git diff --exit-code` по `crates/bsl-rt/src/binbuf.rs` при
проиндексированной версии — пуст.

1. `decode` → `decode_without_bom` в обеих читающих функциях ⇒ падают
   `reading_a_string_back_strips_the_leading_signature` (предсказанный),
   `the_defaults_are_utf_8_without_a_signature`,
   `a_buffer_built_from_a_string_is_little_endian` — 3 из 33.
2. В `add_bom_arg` не-`Булево` даёт `Ok(false)` вместо `Err` ⇒ падает ровно
   `the_argument_types_are_checked` — 1 из 33.
3. `string_from_binary_buffer` пропускает `ДвоичныеДанные` ⇒ падает ровно
   `the_two_readers_do_not_accept_each_others_argument` — 1 из 33.
4. `encode_arg` всегда зовёт `encode` ⇒ падают
   `the_defaults_are_utf_8_without_a_signature` (предсказанный),
   `an_encoding_may_be_named_by_a_string_and_an_unknown_name_is_an_error`,
   `invalid_bytes_and_a_truncated_tail_become_the_replacement_character`,
   `a_buffer_built_from_a_string_is_little_endian` — 4 из 33.

Каждый откат уронил предсказанный тест; там, где упало больше одного, лишние
падения — не шум, а перекрытие: сигнатура наблюдаема сразу в нескольких
измеренных местах.

### Тройка меток `BIN.RADIX.WIDTH`

Вопрос: есть ли у `ЧислоИзШестнадцатеричнойСтроки`/`ЧислоИзДвоичнойСтроки`
потолок длины разбираемой строки и что за ним — ошибка или молчаливая потеря
старших разрядов. Измерены сорок знаков `F` (2^160-1, 49 цифр, отданы
точно); шире не пробовалось. Выбрано: потолка нет, значение копится
`BslNumber`, ограничение — только память, разбор O(n²) по длине.

Замер платформы в этой итерации НАМЕРЕННО не проводился: он потребовал бы
правки подтверждённой фикстуры и нового прогона, что обнулило бы
подтверждение обоих ревьюеров. Вопрос оформлен по правилам репозитория и
уходит в следующий сеанс замеров.

Зонд в `measure-all.bsl` печатает длины десятичных записей, а не «Ок»:
молчаливое усечение разрядов иначе неотличимо от успеха. Наш интерпретатор
даёт `BIN.RADIX.WIDTH  40=49 200=241 5000=6021`; чего ждать от платформы —
неизвестно, в этом и вопрос. Три ширины пробуются в трёх отдельных
`Попытка`, а `М()` зовётся один раз, склеенной строкой — правило «один `М()`
на ИД».

### Замок `CORPUS` в `bsl-cli/tests/conformance.rs`

Остался как есть, легализован расширением области задачи. Гонка структурная:
фикстуры `binary-datarw`, `binary-data`, `binary-streams`, `spreadsheet`,
`textdoc` пишут по фиксированным путям в `/tmp`, а четыре теста обходят
корпус в одном бинарнике, который cargo пускает параллельно. Настоящее
лечение — уникальные пути у фикстур — требует нового прогона на платформе и
остаётся долгом (см. ниже).

### Команды и их результат

Все из корня репозитория.

1. `cargo build -p bsl-rt` — ок.
2. Приёмка №1: `cargo test -p bsl-rt binbuf` — 33 из 33 (было 25);
   `cargo test -p bsl-rt binbuf -- --list | wc -l` — 37 (33 имени плюс
   служебные строки трёх бинарников); `cargo test -p bsl-rt bitops` — 10 из
   10 (было 9), `-- --list | wc -l` — 14.
3. Приёмка №2: четыре отката, четыре падения на предсказанных тестах (см.
   выше); файл восстановлен побайтово.
4. Приёмка №3: `cargo test -p bsl-rt --test open_questions_registry` — 2 из
   2; `cargo test -p bsl-cli measure_script_runs_under_this_interpreter` —
   зелёный; `cargo run -p bsl-cli -- tests/conformance/measure/measure-all.bsl
   | grep -c "^BIN.RADIX.WIDTH"` — печатает ровно `1`.
5. Приёмка №4: `git diff --exit-code 2d63ce0 -- tests/conformance/fixtures
   crates/bsl-rt/src/builtin.rs` — пусто.
6. Приёмка №5: оба `diff` вывода интерпретатора с `.expected`
   (`binary-bitops`, `binary-strings`) — пусты; `cargo test -p bsl-cli --
   --nocapture` — «24 проверено, 20 пропущено», ни `binary-bitops`, ни
   `binary-strings` в сводке пропущенных нет.
7. Приёмка №6: `cargo fmt --all -- --check` — ок; `cargo clippy --workspace
   --all-targets -- -D warnings` — ок; `cargo test --workspace` — ноль
   `FAILED`, зелены `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`,
   `builtin_names_are_unique` (полное имя —
   `builtin::name_table_tests::builtin_names_are_unique`);
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ноль
   ошибок.

Две ловушки по дороге, обе поймала общая проверка, а не рассуждение:
`rustfmt` перестроил четыре места в новых тестах, а clippy
(`cloned_ref_to_slice_refs`) потребовал `std::slice::from_ref(&v)` вместо
`&[v.clone()]` в тесте про два читателя.

### Что осталось незакрытым (честно)

- **`BIN.RADIX.WIDTH` не измерен** — это и есть смысл записи в реестре.
  Зонд написан, но на платформе не прогонялся; какой ответ даст платформа на
  200 и 5000 знаков `F` — неизвестно. Есть риск, что зонд на 5000 знаках
  окажется для платформы слишком тяжёлым: он обёрнут в `Попытка`, но
  `Попытка` ловит ошибку, а не долгую работу, и при неудаче сеанса зонд
  придётся ужать до 200 знаков.
- **Уникальные пути у фикстур вместо замка `CORPUS`** — долг с прошлой
  итерации, не погашен: `binary-data`, `binary-datarw`, `binary-streams`,
  `spreadsheet`, `textdoc` по-прежнему пишут в `/tmp` по фиксированным
  путям, а их `.expected` — платформенные эталоны, так что правка требует
  нового прогона на 1С. Строку в `TASKS.md` заводит не эта фаза.
- **Ошибки типов у буферной прямой функции** (`ПолучитьБуферДвоичныхДанных\
  ИзСтроки` с числом вместо текста, с числовым `ДобавлятьBOM`) в тестах не
  зафиксированы: фикстура их не измеряла, а выводить ожидание из общего
  `encode_arg` значило бы записать рассуждение как измерение.
- **`КодировкаТекста.Системная`** и **сведение трёх `encoding_arg`** —
  долги прошлой итерации, здесь не трогались.

## 2026-08-08 — bin-global-fns (implement, итерация 6: приёмка лежащей правки)

**Задача.** `PLAN.md`, `TASK_ID: bin-global-fns`. Правка по двум FAIL-ревью
(`REVIEW_FABLE.md`, `REVIEW_OPUS.md`) уже лежала НЕ ЗАКОММИЧЕННОЙ в рабочем
дереве против `2d63ce0`; фаза ревью по этому дифу не отрабатывала. Требовалось
прогнать её гейтами заново на итоговом файле и дозакрыть одну измеренную, но не
проверенную строку оракула. Ничего из подтверждённого не переделывалось:
продуктивный код четвёрки, `bitops.rs`, `open_questions.rs`, `measure-all.bsl`,
замок `CORPUS` и уже лежащие тесты не тронуты ни на строку. Платформа заново НЕ
прогонялась.

### Что добавлено (два утверждения, оба — от строк оракула)

Только `crates/bsl-rt/src/binbuf.rs`, только внутри `mod tests`. Проверено
структурно: `git diff 2d63ce0 -- crates/bsl-rt/src/binbuf.rs` даёт ровно два
ханка, `@@ -928,6 +928,39 @@ mod tests` и `@@ -1292,4 +1325,377 @@ mod tests`,
а `mod tests` начинается на строке 892 — то есть за пределы тестового модуля
диф не выходит. Число тестов не изменилось (33), изменилось покрытие внутри
двух из них.

1. **Строка 57 оракула** — `буфер обратно без кодировки: принято Аб` — в
   `the_defaults_are_utf_8_without_a_signature`. Была асимметрия: умолчание
   кодировки у ЧИТАЮЩЕЙ функции проверялось для `ДвоичныеДанные` (строки 44-45),
   а для буфера — нет, хотя платформа его измерила. Добавлен вызов
   `string_from_binary_buffer` с ОДНИМ аргументом на буфере, построенном
   `UTF8`/`Ложь`, со сверкой по кодам `[1040, 1073]` (оракул печатает саму
   строку, а невидимый U+FEFF в ней глазом не различить — та же дисциплина, что
   у соседних утверждений). Строка фикстуры-источника —
   `binary-strings.bsl:203`.
2. **Строка 31 оракула** — `буфер UTF16 без BOM: [16 4 49 4]` — в
   `a_buffer_built_from_a_string_is_little_endian`. У буфера была проверена
   только UTF-8 (строки 27, 29) и однобайтовая (33); двухбайтовая кодировка без
   сигнатуры оставалась непроверенной, хотя измерена. Источник —
   `binary-strings.bsl:145`.

Больше ничего не добавлено. В частности, ошибки типов у ПРЯМОЙ буферной функции
намеренно оставлены незакрытыми — оракул их не мерил (см. хвост записи).
Заголовочные комментарии обоих тестов приведены в соответствие: списки строк
теперь `1, 14, 3, 44-45 и 57` и `27, 29, 31, 33-35, 48-52 и 56`.

### Приёмка №2: гейт ловит регрессию (четыре пары «откат → упавший тест»)

Прогнано ЗАНОВО, на итоговом отформатированном файле. Откаты по одному; перед
каждым файл копировался в `/tmp/binbuf.orig.rs`, после — восстанавливался и
сверялся `diff` (во всех четырёх случаях побайтово тот же файл).

1. `decode` → `decode_without_bom` в обеих читающих функциях (2 вхождения) ⇒
   **3 из 33**: `reading_a_string_back_strips_the_leading_signature`
   (предсказанный, `binbuf.rs:1353`),
   `the_defaults_are_utf_8_without_a_signature` (`:1442`),
   `a_buffer_built_from_a_string_is_little_endian` (`:1685`).
2. В `add_bom_arg` не-`Булево` даёт `Ok(false)` вместо `Err` ⇒ **1 из 33**:
   ровно `the_argument_types_are_checked`.
3. `string_from_binary_buffer` принимает и `ДвоичныеДанные` ⇒ **1 из 33**:
   ровно `the_two_readers_do_not_accept_each_others_argument`.
4. `encode_arg` всегда зовёт `encode` ⇒ **4 из 33**:
   `the_defaults_are_utf_8_without_a_signature` (предсказанный),
   `an_encoding_may_be_named_by_a_string_and_an_unknown_name_is_an_error`,
   `invalid_bytes_and_a_truncated_tail_become_the_replacement_character`,
   `a_buffer_built_from_a_string_is_little_endian`.

Каждый откат уронил предсказанный тест; лишние падения — перекрытие, а не шум:
сигнатура наблюдаема сразу в нескольких измеренных местах. Результаты сошлись с
прошлым прогоном полностью.

### Невакуумность двух НОВЫХ утверждений — отдельно, мутацией

Четыре отката раздела 2 новые утверждения НЕ трогают (откат 4 роняет
`a_buffer_built_from_a_string_is_little_endian` на строке 27, до нового
утверждения), поэтому нагруженность каждого проверена своей мутацией, а не
рассуждением. Обе мутации откачены, файлы сверены `diff` побайтово.

- **Утверждение по строке 57.** Мутация: умолчание кодировки у буферного
  ЧИТАТЕЛЯ переведено с UTF-8 на windows-1251 ⇒ падают 2 из 33, в том числе
  `the_defaults_are_utf_8_without_a_signature` — до правки этот тест такой
  мутации не замечал вовсе. Честная оговорка: второй упавший —
  `the_two_readers_do_not_accept_each_others_argument`, который уже читал буфер
  одним аргументом (строка 48 оракула). То есть новое утверждение НЕ открывает
  дыру, а привязывает к тесту про умолчания собственную измеренную строку 57;
  покрытие тут перекрывающееся, и это надо знать.
- **Утверждение по строке 31.** Мутация в `encoding.rs`:
  `encode_without_signature` для UTF-16 всё-таки приписывает сигнатуру ⇒ падают
  3 из 33, и `a_buffer_built_from_a_string_is_little_endian` падает ИМЕННО на
  новом утверждении — `binbuf.rs:1663`, `left: [255, 254, 16, 4, 49, 4]`,
  `right: [16, 4, 49, 4]`. Здесь новое утверждение единственное на своём
  тесте: до него UTF-16-путь буферной функции не был закрыт ничем.

### Команды и их результат

Все из корня репозитория, все зелёные.

1. `cargo build -p bsl-rt` — ок. `cargo fmt --all -- --check` — ок.
2. Приёмка №1: `cargo test -p bsl-rt binbuf` — **33 из 33**;
   `-- --list | grep -c ": test"` — 33; все восемь имён четвёрки на месте
   (проверено поимённо, каждое встречается ровно один раз).
   `cargo test -p bsl-rt bitops` — **10 из 10**, `--list` — 10.
3. Приёмка №2 — см. выше: четыре отката, четыре падения на предсказанных
   тестах, файл после каждого восстановлен побайтово.
4. Приёмка №3: `cargo test -p bsl-rt --test open_questions_registry` — 2 из 2;
   `cargo test -p bsl-cli measure_script_runs_under_this_interpreter` — зелёный
   (1 passed в `tests/conformance.rs`; в первом прогоне я урезал вывод
   `head -3` и увидел одни «filtered out» — это была ошибка чтения, а не
   красный тест, перепроверено полным выводом);
   `cargo run -p bsl-cli -- tests/conformance/measure/measure-all.bsl
   | grep -c "^BIN.RADIX.WIDTH"` — печатает ровно `1`.
5. Приёмка №4: `git diff --exit-code 2d63ce0 -- tests/conformance/fixtures
   crates/bsl-rt/src/builtin.rs` — пусто. Ни один `.bsl`, `.expected` или
   `.platform.txt` не изменён.
6. Приёмка №5: оба `diff` вывода интерпретатора с `.expected`
   (`binary-bitops`, `binary-strings`) — пусты; `cargo test -p bsl-cli --
   --nocapture` — «24 проверено, 20 пропущено», в списке пропущенных ни
   `binary-bitops`, ни `binary-strings` нет (проверено `grep -c` по всему
   выводу — 0 вхождений).
7. Приёмка №6: `cargo clippy --workspace --all-targets -- -D warnings` — ок;
   `cargo test --workspace` — 24 набора `test result: ok`, ноль `FAILED`,
   поимённо зелены `open_questions_registry_matches_source_markers`,
   `measure_script_covers_every_registry_id`,
   `measure_script_runs_under_this_interpreter`,
   `fixtures_produce_the_same_output_when_run_from_printed_bytecode`;
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ноль
   ошибок и предупреждений.

Новых меток `НЕ ИЗМЕРЕНО` не заводил, `open_questions.rs` и `measure-all.bsl`
не трогал: обе добавленные строки — измеренные значения оракула, а не решения
рассуждением.

### Что осталось незакрытым (перенесено из итерации 5, без изменений)

- **`BIN.RADIX.WIDTH` не измерен** — это и есть смысл записи в реестре. Зонд
  написан, но на платформе не прогонялся; какой ответ даст платформа на 200 и
  5000 знаков `F` — неизвестно. Есть риск, что зонд на 5000 знаках окажется для
  платформы слишком тяжёлым: он обёрнут в `Попытка`, но `Попытка` ловит ошибку,
  а не долгую работу, и при неудаче сеанса зонд придётся ужать до 200 знаков.
- **Уникальные пути у фикстур вместо замка `CORPUS`** — долг не погашен:
  `binary-data`, `binary-datarw`, `binary-streams`, `spreadsheet`, `textdoc`
  по-прежнему пишут в `/tmp` по фиксированным путям, а их `.expected` —
  платформенные эталоны, так что правка требует нового прогона на 1С. Строку в
  `TASKS.md` заводит не эта фаза.
- **Ошибки типов у буферной прямой функции** (`ПолучитьБуферДвоичныхДанных\
  ИзСтроки` с числом вместо текста, с числовым `ДобавлятьBOM`) в тестах не
  зафиксированы: фикстура их не измеряла, а выводить ожидание из общего
  `encode_arg` значило бы записать рассуждение как измерение.
- **`КодировкаТекста.Системная`** и **сведение трёх `encoding_arg`** — долги
  прошлых итераций, здесь не трогались.

### Хенд-офф следующей задаче

Когда дойдёт очередь до `bin-measure`: зонд `BIN.RADIX.WIDTH` в
`measure-binary.bsl` НЕ дублировать. Реестр требует ровно одну строку `М()` на
ИД по ВСЕМ скриптам, а место по AGENTS.md — `measure-all.bsl`, где строка уже
стоит. Дубль уронит `measure_script_covers_every_registry_id`.
2026-08-08T17:26:58Z  PASS  bin-global-fns  panel=1

## 2026-08-08 — bin-measure: контракт `measure-binary.bsl` и закрытие вопроса о ширине разбора (implement)

Задача сделана целиком: оба оракула сняты РЕАЛЬНЫМ прогоном на платформе
8.3.27.2074, ни одна строка эталона не сочинена и не взята из вывода этого
интерпретатора.

### Половина 1 — вопрос о ширине разбора закрыт замером

`./tests/conformance/measure/1c/run-on-1c.sh` без аргументов, 17 секунд,
69 строк. Сеанс чистый по двум независимым признакам:

- ингест не напечатал блока «ЯКОРЯ РАЗОШЛИСЬ» и вернул 0;
- `diff` нового `platform.tsv` с прежним состоит РОВНО из одной добавленной
  строки — новой. Все 68 прежних значений повторились байт в байт, то есть
  сеанс снят с той же конфигурации, что и предыдущие.

Ответ платформы: `BIN.RADIX.WIDTH  40=49 200=241 5000=309`.

Читается он так. До двухсот знаков `F` (2^800-1, 241 десятичный знак)
платформа разбирает ТОЧНО и сходится с нами знак в знак — то есть выбранное
здесь поведение на этой ширине оказалось верным. Потолок лежит между 200 и
5000 знаками. За потолком платформа НЕ бросает ошибку и НЕ теряет старшие
разряды: она отдаёт 309 знаков вместо точных 6021.

Ни одна из двух гипотез, записанных в реестре («ошибка» либо «молчаливая
потеря разрядов»), не подтвердилась — ответ оказался третьим. Поэтому
вопрос перенесён в `MEASURED_ANCHORS` (`expect` — строка ровно как её
напечатала платформа), обе метки `НЕ ИЗМЕРЕНО` в `bitops.rs` сняты, тест
переименован в `parse_radix_matches_the_platform_at_two_hundred_digits` и
теперь утверждает ИЗМЕРЕННОЕ значение на 200 знаках, а не выбранное.
Расхождение на 5000 знаках оставлено и записано в `note` якоря — правку
`parse_radix` план прямо запретил втягивать сюда, и правильно: копировать
нечего, пока не известно, ЧТО это за число.

### Что это за число — честно не выяснено

Спутник-проба в контракте печатает его концы: `839555017536731758113448…
033861619420`, 309 знаков. Это не начало точного значения
(`398027684033796659235430…`), не его хвост (`…663406309375`) и не
округление — магнитуда другая (≈8.4e308 против ≈4e6020). Проверенные на
бумаге догадки не сошлись: 2^1024-1 (первые 256 знаков `F`) даёт 309 знаков,
но начинается с `1797…`, а не с `8395…`; усечение по основанию 10 и взятие
по модулю 2^1024 дают другие хвосты. Механизм неизвестен, и выдумывать его
я не стал — в `note` якоря так и написано: «что это за число, замер не
выясняет». Отдельным вопросом реестра это не заводил: реестр — про решения,
принятые здесь рассуждением, а тут решение как раз измерено, неизвестен
чужой механизм.

### Половина 2 — контракт `measure-binary.bsl`

11 проб, все шесть тем из плана. Снят оракул `measure-binary.platform.txt`
(11 строк, 22 секунды). `.expected` намеренно не заводил, ингест этого файла
не делал (он затёр бы реестровый `platform.tsv` — эта грабля записана в
AGENTS.md).

Из 11 строк разошлись 5, и каждая выписана в шапке скрипта поимённо:

1. «обход буфера» — платформа `итераций 4, первый Число 208`, здесь
   `<ошибка>`. Обход буфера платформа принимает и отдаёт БАЙТЫ ЧИСЛАМИ; это
   ровно то, что `lib.rs` в ветке `BinaryBuffer` называл незаведённым.
2. «буфер Скопировать» — платформа `принято Буфер двоичных данных`.
3. «буфер ПолучитьСрез» — то же. Обоих методов здесь нет.
4. «байты Системная» — платформа `[208 144 208 177]`, здесь `[192 225]`.
   САМАЯ СОДЕРЖАТЕЛЬНАЯ строка замера: «Системная» у платформы идёт за
   локалью системы и на этой машине (`ru_RU.UTF-8`) означает UTF-8, а вовсе
   не кодовую страницу. Здесь же «Системная» и ANSI сведены в одну
   windows-1251 (`binbuf.rs`, `encoding_arg`) — и, как выяснилось, зря.
   Соседняя «байты ANSI» при этом сошлась: windows-1251 платформа даёт и на
   UTF-8-системе. Строка машинозависима, оговорка — в шапке скрипта, по
   образцу `json-dates.expected`.
5. «разбор 5000 знаков» — см. выше.

Сошлись 6: «обход двоичных данных» (`<ошибка>` с обеих сторон — не
коллекция и у платформы, догадка не подтверждалась ничем и подтвердилась),
«байты ANSI», оба отказа буферной функции на негодных типах (долг прошлой
итерации, теперь измерен) и обе строки `ЗаписатьJSON` от двоичного.

### Грабля, стоившая двух прогонов (и попавшая в AGENTS.md)

Первый прогон контракта умер по таймауту: пустой вывод, 180 с, снят. Чтобы
не гадать, сделал в `/tmp` копию раннера БЕЗ `trap rm -rf "$WORK"` — приёмник
вывода дописывается по строке, поэтому уцелевший файл показывает, до какой
именно пробы дошло. Дошло до последней; изолированная проба того же блока не
напечатала вообще ничего.

Причина: скрипт исполняется в модуле УПРАВЛЯЕМОЙ ФОРМЫ, а я назвал
переменную `Ширина`. Это СВОЙСТВО ФОРМЫ, и `Ширина = "";` присваивает пустую
строку ширине формы — ошибка типа, модальное окно, таймаут. `Попытка` не
спасает: присваивание — это подготовка пробы, а не её тело. Подтверждено
разностной пробой: тот же скрипт, отличающийся ТОЛЬКО именем переменной
(`ШиринаЗнаков`), отработал и напечатал все пять строк. В `measure-all.bsl`
имена `Ширина40`/`Ширина5000` — поэтому там это никогда и не всплывало.

Правило дописано пятым пунктом в список AGENTS.md «четыре правила, каждое
из которых стоит впустую потраченного прогона» — теперь их пять. Это
единственная правка вне списка файлов плана; сделал её сознательно: список
существует ровно для таких находок, а иначе следующий агент потеряет те же
два прогона.

### Файлы

- `tests/conformance/measure/measure-binary.bsl` — новый (контракт).
- `tests/conformance/measure/measure-binary.platform.txt` — новый (оракул).
- `tests/conformance/measure/platform.tsv` — перезаписан ингестом, +1 строка.
- `crates/bsl-rt/src/open_questions.rs` — запись перенесена из
  `OPEN_QUESTIONS` в `MEASURED_ANCHORS`.
- `crates/bsl-rt/src/bitops.rs` — сняты обе метки, переписан и переименован
  тест.
- `tests/conformance/measure/measure-all.bsl` — правка ТОЛЬКО комментария к
  зонду (говорил «неизвестно» о том, что теперь измерено); строка `М()` не
  тронута, состав вопросов не тронут.
- `AGENTS.md` — пятое правило написания measure-скриптов.

### Команды и их результат

Все из корня репозитория.

1. Приёмка №1: `run-on-1c.sh` — 69 строк;
   `grep -c "^BIN.RADIX.WIDTH" …/platform.tsv` — ровно `1`;
   `--ingest-measurements …/platform.tsv` — код возврата **0**, блока
   «ЯКОРЯ РАЗОШЛИСЬ» нет, «СОВПАЛО: 21 из 25». Единственное расхождение —
   `SQRT.SMALL_ARG`, известное и оставленное сознательно (было и до этой
   задачи).
2. Приёмка №2: `run-on-1c.sh …/measure-binary.bsl` — 11 строк;
   `test -s` — непуст; `diff` вывода интерпретатора с оракулом — 5 строк,
   все пять выписаны в шапке скрипта (сверено построчно).
3. Приёмка №3: `cargo test -p bsl-rt --test open_questions_registry` —
   **2 из 2**, зелены и `open_questions_registry_matches_source_markers`, и
   `measure_script_covers_every_registry_id`.
4. Приёмка №4: `cargo test -p bsl-cli --test conformance` — **4 из 4**;
   по `--list` это ровно `conformance_fixtures_match_oracle_output`,
   `fixtures_produce_the_same_output_when_run_from_printed_bytecode`,
   `measure_script_runs_under_this_interpreter` и
   `the_jit_agrees_with_the_interpreter_on_every_script`.
5. Приёмка №5: `cargo test -p bsl-rt bitops` — **10 из 10**; по `--list`
   имя теста теперь `parse_radix_matches_the_platform_at_two_hundred_digits`,
   старого `…accepts_strings_wider_than_measured` в дереве нет (`grep` по
   `crates/` и `tests/` — 0 вхождений).
6. Приёмка №6: `cargo fmt --all -- --check` — ок; `cargo clippy --workspace
   --all-targets -- -D warnings` — ок; `cargo test --workspace` — ноль
   `FAILED`, все наборы `test result: ok`;
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.
7. Приёмка №7: `git diff --exit-code -- tests/conformance/fixtures` — пусто.

### Что осталось незакрытым (честно)

- **Механизм потолка разбора у платформы** — неизвестен, см. выше. Измерен
  ФАКТ расхождения, а не его причина.
- **`КодировкаТекста.Системная` сведена в windows-1251 и это ТЕПЕРЬ ИЗМЕРЕНО
  как неверное** — платформа идёт за локалью системы. Долг перестал быть
  догадкой и стал измеренным расхождением; чинить его в этой задаче план
  запретил, строку в `TASKS.md` заводит не эта фаза.
- **Обход `Для Каждого` по буферу** — платформа отдаёт байты числами,
  здесь обхода нет вовсе. Тоже теперь измерено, тоже отдельная задача.
- **`Скопировать` и `ПолучитьСрез` у буфера** — у платформы есть, здесь
  нет.
- **Уникальные пути у фикстур вместо замка `CORPUS`** — долг прошлых
  итераций, не трогался.
2026-08-08T18:13:08Z  FAIL  bin-measure  fable=PASS opus=FAIL  panel=1

## 2026-08-08 — bin-measure (итерация после FAIL, починка по двум находкам REVIEW_OPUS.md)

Задача: привести два комментария о `КодировкаТекста.Системная` и счёт правил
в `AGENTS.md` в соответствие с уже снятым оракулом
`tests/conformance/measure/measure-binary.platform.txt`. Правка чисто
документирующая: платформа НЕ запускалась, оракулы не перезаписывались,
поведение не менялось.

### Изменённые файлы

- `crates/bsl-rt/src/binbuf.rs` — комментарий в `encoding_arg` (строки
  741–742 -> 741–748). Опровергнутое «ANSI и «Системная» — кодовая страница
  системы» снято. Новый текст разводит две ветки по измеренному: `ANSI` —
  windows-1251 (`«Аб»` -> `C0 E1`), «Системная» в том же сеансе на той же
  машине — UTF-8 (`«Аб»` -> `D0 90 D0 B1`), то есть идёт за локалью, а не за
  кодовой страницей; сведение обеих в windows-1251 названо сознательным
  расхождением, со ссылкой на строки `байты Системная` / `байты ANSI` оракула.
  `match` не тронут.
- `crates/bsl-rt/src/textdoc.rs` — твин того же комментария в одноимённом
  `encoding_arg` (518–521 -> 518–523). Категоричность снята: сказано, что
  кодировка сведена по образцу соседнего семейства
  `ПолучитьБуферДвоичныхДанныхИзСтроки` (со ссылкой на `binbuf.rs`), что там
  «Системная» измерена как UTF-8, и что за `ЗаписьТекста` это не
  зондировалось ни для одного из двух членов. Про сам `ЗаписьТекста` не
  утверждается ни «кодовая страница», ни «UTF-8» — не измерено ни то ни
  другое.
- `AGENTS.md` — строка 84: `has four rules` -> `has five rules`. Пункты и их
  порядок не трогались.

Ничего больше: замерная часть прошлой итерации (`tests/conformance/measure`,
`open_questions.rs`, `bitops.rs`, `tests/conformance/fixtures`) не тронута.
Метка `НЕ ИЗМЕРЕНО(...)` намеренно НЕ заводилась — поведение здесь измерено,
метка была бы второй неправдой и потребовала бы записи в реестре и строки
`М()`, которой взяться неоткуда.

### Команды и результаты

1. Приёмка №1: `grep -n "кодовая страница" crates/bsl-rt/src/binbuf.rs` —
   пусто (rc=1); `grep -n "208 144 208 177\|D0 90 D0 B1"` — есть, строка 744,
   привязана к «Системной». Глазами: `C0 E1` на строке 741 привязан к `ANSI`.
2. Приёмка №2: `grep -n "кодовая страница" crates/bsl-rt/src/textdoc.rs` —
   пусто (rc=1).
3. Приёмка №3: `grep -n "has four rules" AGENTS.md` — пусто (rc=1); строка 84
   говорит `has five rules`; `grep -c "^- \*\*" AGENTS.md` — **5**, совпадает.
4. Приёмка №4: `git diff -U0 -- crates/bsl-rt/src/binbuf.rs
   crates/bsl-rt/src/textdoc.rs` — каждая изменённая строка после отступа
   начинается с `//` (проверено фильтром: строк-исключений ноль). Ни одной
   правки в `match`, ни одного изменённого `Encoding::`.
5. Приёмка №5: `git diff --exit-code -- tests/conformance/measure
   crates/bsl-rt/src/open_questions.rs crates/bsl-rt/src/bitops.rs
   tests/conformance/fixtures` — пусто (rc=0).
6. Приёмка №6: `cargo test -p bsl-rt --test open_questions_registry` — **2 из
   2 ok** (`measure_script_covers_every_registry_id`,
   `open_questions_registry_matches_source_markers`).
7. Приёмка №7 (общий гейт): `cargo build -p bsl-rt` — ок;
   `cargo fmt --all -- --check` — ок; `cargo clippy --workspace --all-targets
   -- -D warnings` — ок; `cargo test --workspace` — ноль `FAILED`, все наборы
   `test result: ok` (1 тест `ignored`, как и раньше);
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — ок.

### Что осталось незакрытым (честно)

- **Расхождение по `КодировкаТекста.Системная` не починено, а только честно
  описано.** Мы по-прежнему отдаём windows-1251 там, где платформа на машине
  оракула дала UTF-8. Это осознанное решение плана; заводить строку в
  `TASKS.md` реализатору запрещено, так что долг живёт только в комментарии и
  здесь.
- **Локальная зависимость «Системной» измерена ровно одной точкой.** Оракул
  снят на `ru_RU.UTF-8`; что платформа отдаст на машине с другой локалью —
  не измерено. Отсюда же неизвестно, будет ли `ANSI` windows-1251 и на не-ru
  локали: наш замер показывает лишь, что на `ru_RU.UTF-8` он ей НЕ следует.
- **`ЗаписьТекста`/`ЧтениеТекста` не зондировались вовсе.** Перенос вывода с
  соседнего семейства на них — предположение, и комментарий теперь это прямо
  говорит. OpenQuestion под это не заводился (см. выше про метку); если
  панель решит, что долг обязан быть видимым реестру, это отдельная строка
  бэклога с своей `М()` в `measure-*.bsl`.
- **Наблюдения рецензентов «на будущее»** (таксономия якоря
  `BIN.RADIX.WIDTH`, тест на нашу сторону расхождения на 5000 знаках) в эту
  итерацию не входили — оба рецензента явно сказали, что по ним не блокируют.
2026-08-08T18:28:52Z  FAIL  bin-measure  fable=FAIL opus=FAIL  panel=1

## 2026-08-08 — bin-measure (итерация 4, реализация)

**Задача:** переписать комментарий в `encoding_arg`
(`crates/bsl-rt/src/textdoc.rs:518-523`) так, чтобы он говорил про
`ТекстовыйДокумент` — объект, который эта функция реально обслуживает, — и
разводил ИЗМЕРЕННОЕ по `ANSI` от НЕЗОНДИРОВАННОГО по «Системной». Больше
ничего.

**Изменённые файлы:** `crates/bsl-rt/src/textdoc.rs` — и только он.
Шесть строк комментария заменены на двенадцать; `match`, ни один
`Encoding::` и ни одна строка кода не тронуты. `binbuf.rs`, `AGENTS.md`,
`tests/conformance/measure/**`, `open_questions.rs`, `bitops.rs`,
`tests/conformance/fixtures/**` не редактировались (принято прошлой
итерацией обоими рецензентами).

**Что проверено по дереву ПЕРЕД правкой** (не переписано с плана на веру):

- `encoding_arg` (`textdoc.rs:512`) зовут ровно два места — `read_file`
  (`textdoc.rs:549`, `Прочитать`) и `write_file` (`textdoc.rs:563`,
  `Записать`), оба у `ТекстовыйДокумент`. `ЗаписьТекста` в этой ветке
  `match` не появляется ни при каком вводе;
- `crates/bsl-rt/src/encoding.rs:12` — «`КодировкаТекста.ANSI` и строка
  `"windows-1251"` — `C0 E1`», в шапке раздела «Что здесь ИЗМЕРЕНО»,
  байты сняты дампом файлов, записанных платформой;
- `crates/bsl-rt/src/open_questions.rs:1528-1532` — якорь
  `ENC.WRITE.ANSI`, `expect: "записан"`, `note: "ANSI на машине замеров —
  windows-1251 (C0 E1)"`;
- `tests/conformance/measure/measure-textdoc.platform.txt:76` —
  `ENC.WRITE.ANSI<TAB>записан`, байтов в оракуле нет (проверено
  `sed -n '74,78p' | cat -A`: 74 — UTF8, 75 — UTF16, 76 — ANSI);
- `tests/conformance/measure/measure-binary.platform.txt:5` —
  `байты Системная<TAB>[208 144 208 177]`, то есть `D0 90 D0 B1` = UTF-8;
  строка 6 — `байты ANSI<TAB>[192 225]` = `C0 E1`.

**Команды и результаты:**

1. `cargo build -p bsl-rt` — **PASS**.
2. Приёмка №1: `grep -n "ЗаписьТекста\|ЧтениеТекста"
   crates/bsl-rt/src/textdoc.rs` — **пусто** (rc=1); до правки было одно
   попадание на строке 522.
3. Приёмка №2: `grep -n "ТекстовыйДокумент\.\(Записать\|Прочитать\)"
   crates/bsl-rt/src/textdoc.rs` — **1 попадание** (строка 519); до правки
   было 0.
4. Приёмка №3: `grep -n "C0 E1" ...` — **1** (строка 521);
   `grep -n "ENC.WRITE.ANSI\|encoding\.rs" ...` — **1** (та же строка 521).
   `C0 E1` стоит в предложении про `ANSI` у `ТекстовыйДокумент.Записать`,
   а не про оба члена разом.
5. Приёмка №4: `grep -n "D0 90 D0 B1\|measure-binary" ...` — **1**
   (строка 528); `grep -n "по образцу" ...` — **пусто**;
   `grep -n "кодовая страница" ...` — **пусто**.
6. Приёмка №6: `git diff --stat` — в `crates/` ровно один файл
   (`crates/bsl-rt/src/textdoc.rs`, 18 строк), плюс штатный `PLAN.md`
   цикла. `git diff -U0 -- crates/bsl-rt/src/textdoc.rs`: каждая
   добавленная строка после отступа начинается с `//`.
7. Приёмка №7: `git diff --exit-code -- crates/bsl-rt/src/binbuf.rs
   AGENTS.md tests/conformance/measure crates/bsl-rt/src/open_questions.rs
   crates/bsl-rt/src/bitops.rs tests/conformance/fixtures` — **пусто,
   rc=0**.
8. Приёмка №8: `cargo test -p bsl-rt --test open_questions_registry` —
   **2 из 2 ok** (`measure_script_covers_every_registry_id`,
   `open_questions_registry_matches_source_markers`). Метка
   `НЕ ИЗМЕРЕНО` не заводилась и не снималась.
9. Приёмка №9 (общий гейт): `cargo fmt --all -- --check` — **PASS**;
   `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**;
   `cargo test --workspace` — **rc=0**, 24 набора `test result: ok`, ноль
   `FAILED`, один тест `ignored` (как и раньше);
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — **rc=0**.

**Что осталось незакрытым (честно):**

- **«Системная» в семействе `ТекстовыйДокумент` так и не зондирована.** Мы
  отдаём для неё windows-1251, а у соседнего
  `ПолучитьБуферДвоичныхДанныхИзСтроки` платформа на той же машине дала
  UTF-8. Комментарий теперь прямо называет это переносом вывода с соседа, а
  не замером; метка `НЕ ИЗМЕРЕНО` по плану не заводилась (это потребовало бы
  записи в `open_questions.rs` и строки `М()` в `measure-all.bsl`), так что
  долг виден только прозой — здесь и в комментарии. Кандидат в бэклог, но
  заводить строку реализатору запрещено.
- **`ANSI` для `ТекстовыйДокумент` измерен байтами, но не автоматическим
  оракулом.** Проба `measure-textdoc.bsl:717-725` фиксирует лишь приём
  члена («записан»); привязка `C0 E1` живёт в прозе шапки `encoding.rs` и в
  `note` якоря, то есть регрессия отображения `ANSI` -> windows-1251
  автоматикой НЕ ловится. Тест на байты записанного файла был бы уместен —
  это отдельная строка бэклога, не эта фаза.
- **Локаль измерена одной точкой.** Оракул снят на `ru_RU.UTF-8`; поведение
  `ANSI` и «Системной» на другой локали не измерено. Новых OpenQuestion ID
  по итогам этой итерации не появилось.
2026-08-08T18:43:04Z  FAIL  bin-measure  fable=PASS opus=FAIL  panel=1

## 2026-08-08 — итерация 5, задача `bin-measure` (реализация)

**Что делалось:** ровно то, что описано в `PLAN.md` — правка комментария в
`encoding_arg` (`crates/bsl-rt/src/textdoc.rs`), снимающая два ложных
утверждения, за которые `REVIEW_OPUS.md` отклонил итерацию 4:

- **Дефект 1.** Концовка «так что windows-1251 здесь — перенос вывода с
  соседа, а не замер» приписывала здешнему выбору происхождение от
  `ПолучитьБуферДвоичныхДанныхИзСтроки`. Хронология это опровергает
  (сведение `ANSI | Системная` в windows-1251 — `8537c98`, 2026-08-02;
  сосед появился 2026-08-07 и был реализован 2026-08-08), да и «вывод
  соседа» — сам не замер, а сознательное расхождение с замером
  (`binbuf.rs:741-748`: измерен UTF-8, возвращается windows-1251).
  Направленность заимствования убрана целиком; сравнение с соседом
  осталось как факт (у него «Системная» измерена как UTF-8).
- **Дефект 2.** Зачин «но измерены они по-разному» буквально объявлял
  измеренными оба члена, противореча собственной же строке пятью ниже.
  Стало «но измерен из них только `ANSI`».

Новая концовка: «Опоры под windows-1251 для «Системной» здесь нет ни в
одном замере: это сознательное расхождение с единственным имеющимся, ровно
как у соседа.» Строки 519–528 (принятые обоими рецензентами) перенесены без
изменения содержания; поменялась только пунктуация на стыке зачина («: у
…», «платформа, «Аб»») из-за переформулированного первого предложения.

**Файлы изменены:** `crates/bsl-rt/src/textdoc.rs` (только комментарий
внутри `encoding_arg`, строки 518–531) — и больше ни одного файла, кроме
штатных `PLAN.md`/`PROGRESS.md` цикла. `match` и все `Encoding::` не
тронуты: поведение бит в бит прежнее.

**Команды и результаты:**

1. `cargo build -p bsl-rt` — **PASS**.
2. Приёмка №1: `grep -n "ЗаписьТекста\|ЧтениеТекста" …` — **пусто (rc=1)**;
   `grep -n "ТекстовыйДокумент\.\(Записать\|Прочитать\)" …` — **1 (строка
   519)**.
3. Приёмка №2 (четыре факта): `C0 E1` — строка 521; `ENC.WRITE.ANSI` +
   `encoding.rs` — строка 521; `measure-textdoc.platform.txt:76` — строка
   523; `D0 90 D0 B1` и `measure-binary.platform.txt:5` — строка 528. Все
   **≥ 1**.
4. Приёмка №3: `grep -n "перенос\|по образцу\|вслед за\|по аналогии\|образц"
   …` — **пусто (rc=1)**; `grep -n "сознательное расхождение" …` — **1
   (строка 530)**.
5. Приёмка №4: `grep -n "измерены они\|измерены оба" …` — **пусто (rc=1)**.
6. Приёмка №5: `git diff --stat` — в `crates/` ровно один файл
   (`crates/bsl-rt/src/textdoc.rs`, +14/−12), плюс штатный `PLAN.md`.
   `git diff -U0 -- crates/bsl-rt/src/textdoc.rs`: все шесть добавленных
   строк после отступа начинаются с `//`, ни одной правки в `match`, ни
   одного изменённого `Encoding::`.
7. Приёмка №7: `git diff --exit-code -- crates/bsl-rt/src/binbuf.rs
   AGENTS.md tests/conformance/measure crates/bsl-rt/src/open_questions.rs
   crates/bsl-rt/src/bitops.rs tests/conformance/fixtures` — **пусто,
   rc=0**.
8. Приёмка №8: `cargo test -p bsl-rt --test open_questions_registry` —
   **2 из 2 ok**. Метка `НЕ ИЗМЕРЕНО` не заводилась и не снималась.
9. Приёмка №9 (общий гейт): `cargo fmt --all -- --check` — **PASS**;
   `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**;
   `cargo test --workspace` — **rc=0**, ноль `failed`, один `ignored` (как
   и раньше); `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps`
   — **rc=0**.

Приёмка №6 — ручная и остаётся за рецензентом: списка синонимов
«заимствования» конечного не существует, зелёного `grep` для неё нет.
Платформа не запускалась, `--ingest-measurements` не вызывался — для правки
комментария сеанс не нужен, а ингест затёр бы оракул `platform.tsv`.

**Что осталось незакрытым (честно):**

- **«Системная» в семействе `ТекстовыйДокумент` не зондирована ни разу.**
  Мы отдаём для неё windows-1251; единственный имеющийся замер (у соседа,
  `measure-binary.platform.txt:5`) даёт UTF-8. Теперь комментарий называет
  это сознательным расхождением, а не замером и не заимствованием. Метка
  `НЕ ИЗМЕРЕНО` по плану не заводилась (потребовала бы записи в
  `open_questions.rs` и строки `М()` в `measure-all.bsl`), так что долг
  виден только прозой. Кандидат в бэклог; заводить строку реализатору
  запрещено.
- **`ANSI` для `ТекстовыйДокумент` измерен байтами, но не автоматическим
  оракулом.** Проба `measure-textdoc.bsl:717-725` фиксирует лишь приём
  члена («записан»); привязка `C0 E1` живёт в прозе шапки `encoding.rs`,
  то есть регрессия отображения `ANSI` -> windows-1251 автоматикой НЕ
  ловится. Тест на байты записанного файла — отдельная строка бэклога.
- **Локаль измерена одной точкой** (`ru_RU.UTF-8`). Новых OpenQuestion ID
  по итогам этой итерации не появилось.
2026-08-08T18:56:08Z  PASS  bin-measure  panel=1

## 2026-08-12 — dom-model: DOM на чтение поверх `ЧтениеXML` (implement, итерация 1)

**Что делалось:** задача `dom-model` из `TASKS.md` целиком — `ПостроительDOM`,
`ДокументDOM`, модель узлов с навигацией, атрибутами и пространствами имён.
Порядок работы по плану: СНАЧАЛА платформа, потом код. Пять разведочных
прогонов (`run-on-1c.sh`, ~6 с каждый) на временных скриптах в scratchpad,
затем реализация, затем контракт и фикстура с эталоном, снятым на платформе.

**Измерено на 8.3.27.2130 (главное, всё — пробами, не из справки):**

1. **Имена.** `ПостроительDOM`/`DOMBuilder` строится через `Новый` (без
   аргументов; `Новый ПостроительDOM("x")` отвергается). Корень документа —
   `ЭлементДокумента`, а НЕ `ДокументныйЭлемент` (справка врёт). Владелец
   документа у узла — `ДокументВладелец`, владелец атрибута —
   `ЭлементВладелец`; `ВладелецДокумента`, `ВладеющийДокумент`,
   `ВладеющийЭлемент`, `Родитель` платформа не знает. У коллекции атрибутов
   нет ни `Получить`, ни `ПолучитьПоИмени` — только индекс, `Количество()` и
   `Для Каждого`. Английское имя поиска — `GetElementByTagName` (в
   единственном числе, `GetElementsByTagName` — ошибка).
2. **Имена типов.** `Строка(ТипЗнч(...))`: «Построитель DOM», «Документ  DOM»
   и «Комментарий  DOM» — с ДВУМЯ пробелами, «Элемент DOM», «Атрибут DOM»,
   «Текст DOM», «Инструкция обработки DOM», «Список узлов DOM», «Коллекция
   атрибутов DOM», «Список элементов DOM». Английские сняты через `Тип(...)`:
   `DOMDocument`, `DOMElement`, `DOMAttribute`, `DOMText`, `DOMComment`,
   `DOMProcessingInstruction`, `DOMNodeList`, `DOMElementList`, `DOMNodeType`,
   а у коллекции атрибутов — `DOMAttributeMap` (`DOMNamedNodeMap` и
   `DOMAttributeCollection` платформа не разрешает).
3. **`ТипУзлаDOM`** — двенадцать членов; `ТипДокумента`, `Пробелы` и
   `ЗначащиеПробелы` платформа не знает. Английское написание члена
   `ОпределениеТипаДокумента` — `DocumentType`, а НЕ `DocumentTypeDefinition`
   (проверены оба).
4. **Построение идёт ОТ ТЕКУЩЕЙ позиции читателя.** Разностный опыт на
   `<а х="1">раз<б/>два</а>`: после 0 и 1 `Прочитать` дерево полное; после 2
   (читатель на тексте «раз») — корень `а` С АТРИБУТОМ, дети `б` и «два», а
   «раз» потерян; после 4 (конец `б`) — только «два»; после 6 — пустой
   документ. То есть предки восстанавливаются из стека открытых элементов
   вместе с атрибутами, а содержимое берётся из последующих событий.
   Исчерпанный читатель даёт ПУСТОЙ документ, а не ошибку; после построения
   читатель исчерпан (`Прочитать()` — «Нет», `ТипУзла` — «Ничего»).
5. **Комментарий ДО корневого элемента в дерево не попадает**, а после корня
   попадает; инструкция обработки попадает и до, и после; `<?xml ...?>` узлом
   не становится. Проверено четырьмя документами.
6. **Поиск элементов.** `ПолучитьЭлементыПоИмени` сопоставляет имя И как
   полное, И как локальное: на `<к:а xmlns:к="urn:к"><а><к:а/></а></к:а>`
   запрос `"а"` находит ТРИ элемента, `"к:а"` — два, `"*"` — три. Аргумент
   URI в двухаргументной форме платформа ИГНОРИРУЕТ: `("urn:нет","а")` тоже
   даёт три. У элемента ищутся только потомки, себя он не включает.
7. **Атрибуты.** Объявления `xmlns` — обычные атрибуты в порядке записи, с
   URI `http://www.w3.org/2000/xmlns/`. Поиск ОДНИМ аргументом — по
   ЛОКАЛЬНОМУ имени среди атрибутов без пространства имён: у атрибута `к:б`
   и `ПолучитьАтрибут("к:б")`, и `ПолучитьАтрибут("б")` дают `Неопределено`,
   а `ПолучитьАтрибут("urn:к","б")` — значение; `ЕстьАтрибут("xmlns")` —
   «Нет». Отсутствующий атрибут — `Неопределено`, не пустая строка.
8. **Прочее:** `ВерсияXML` берётся ИЗ ОБЪЯВЛЕНИЯ (`version="1.1"` даёт
   «1.1»), без объявления — «1.0»; `ТекстовоеСодержимое` у документа и у
   инструкции обработки — `Неопределено`, у элемента — склейка текстовых
   потомков без комментариев; узлы сравниваются по тождеству, а два
   обращения к `ДочерниеУзлы` дают РАЗНЫЕ списки; `ЗначениеЗаполнено` на
   узле и построителе — ОШИБКА, а на коллекции — по длине («Да» у непустых
   `ДочерниеУзлы`, «Нет» у пустых `Атрибуты`); индекс за границей списка —
   ошибка.

**Что осталось незакрытым (честно):**

- **Узел `ОпределениеТипаДокумента` не строится.** Платформа его создаёт
  (`<!DOCTYPE а SYSTEM "вн.dtd">` — `ИмяУзла` «а», `СистемныйИдентификатор`
  «вн.dtd», плюс `ПубличныйИдентификатор`, `ВнутреннееПодмножество`,
  `Сущности`, `Нотации`), но разбор внутреннего подмножества DTD — отдельная
  работа: наш разборщик глотает `<!...>` до первого `>`, и на подмножестве с
  `>` внутри он уже неправ. Сознательное расхождение, описано в шапке
  `dom.rs`; кандидат в бэклог (строку в `TASKS.md` реализатору заводить
  запрещено).
- **Дети у объявления `xmlns` у платформы нестабильны.** На одном и том же
  документе в двух прогонах `Э.Атрибуты[0].ДочерниеУзлы.Количество()` дало 2
  и 3, а обход этих детей падает с ошибкой. У обычного атрибута (`а="1"`)
  измерен ровно один текстовый ребёнок. Дефект не воспроизводим: у нас у
  любого атрибута один текстовый ребёнок.
- **Повторный `Прочитать` с ТЕМ ЖЕ читателем УБИВАЕТ ПЛАТФОРМУ** — прогон
  снимается по таймауту, вывода нет вообще; разностный прогон, отличающийся
  только этой строкой, проходит штатно. Ответ платформы здесь неизвестен в
  принципе, поэтому такой строки нет в контракте (она унесла бы весь сеанс),
  а наше поведение — пустой документ, как у всякого исчерпанного читателя, —
  описано в шапке `dom.rs` как сознательное расхождение.
- **Английское имя `Прочитать` у построителя** (`П.Read(Ч)`) отдельной
  пробой не подтверждено: строка «англ Read» контракта отвечает `<ошибка>` с
  обеих сторон, потому что имя `Read` уже живёт в общей строке таблицы
  `BUILTIN_METHOD_NAMES` (`ReadNext`) и проба через `Выполнить` упирается в
  ту же ошибку разбора у платформы. Наш построитель принимает оба написания.
- Новых `НЕ ИЗМЕРЕНО` не осталось: единственный заведённый по ходу работы ID
  `DOM.DOC.ELEMENT_BY_ID` ЗАКРЫТ замером и перенесён в `MEASURED_ANCHORS`
  (платформа отдаёт `Неопределено` и на атрибут, объявленный типом `ID` во
  внутреннем подмножестве DTD, и на атрибут с именем `id`).

**Файлы изменены:** новый `crates/bsl-rt/src/dom.rs` (модель узлов, разбор,
свойства и методы, 15 юнит-тестов); `xml.rs` (событие `Comment` под флагом
`set_report_comments`, атрибуты открытых элементов в `Rc`, версия из
объявления); `object.rs`, `lib.rs`, `types.rs`, `enums.rs`, `builtin.rs`
(три новых варианта `BslObject`, одиннадцать `TypeId`, перечисление
`ТипУзлаDOM`, семь методов); `spreadsheet_template.rs` (ветка нового
события); `bsl-sema` (`NEW_TYPES`, `resolve_new`, `RExpr::NewDomBuilder`,
арности методов); `bsl-bytecode` (`NewDomBuilder` в `instr.rs`,
`compiler.rs`, `text.rs` с бампом `FORMAT_VERSION` 6 -> 7 и корпусом
round-trip, классификация в `bundle.rs`; плюс починка печати обрезанной
строковой константы — перевод строки внутри неё разрывал строку листинга и
ломал `--run-bytecode`); `bsl-vm` (ветка опкода в `step_cold`);
`tests/conformance/fixtures/dom-model.{bsl,expected,platform.txt}`;
`tests/conformance/measure/measure-dom.{bsl,platform.txt}`; `measure-all.bsl`
и `platform.tsv` (строка `DOM.DOC.ELEMENT_BY_ID`; файл заодно перевыложен
целиком свежим прогоном — значения всех прежних строк совпали, изменился
только порядок, он идёт по скрипту).

**Команды и результаты:**

1. `cargo build --workspace` — **PASS**.
2. `cargo test -p bsl-rt dom` — **15 из 15 ok**.
3. `cargo test -p bsl-cli --test conformance -- --nocapture` — **4 из 4 ok**;
   в отчёте «26 проверено, 23 пропущено», `dom-model` в пропущенных НЕТ (у
   него есть `.expected`), `measure-dom` — есть, как и задумано.
4. `cmp tests/conformance/fixtures/dom-model.expected
   tests/conformance/fixtures/dom-model.platform.txt` — **совпадают** (90
   строк, эталон — копия платформенного вывода).
5. `cargo test -p bsl-rt --test open_questions_registry` — **2 из 2 ok**.
6. `cargo test -p bsl-cli --test bundles` — **PASS**;
   `the_jit_agrees_with_the_interpreter_on_every_script` — **PASS**; сверх
   того `--jit` на самой фикстуре даёт вывод, совпадающий с эталоном.
7. `cargo test -p bsl-bytecode` — **PASS** (в том числе
   `the_corpus_covers_every_opcode` и обе round-trip-проверки).
8. Общий гейт: `cargo fmt --all -- --check` — **PASS**;
   `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**;
   `cargo test --workspace` — **24 набора ok, ноль failed**, один ignored
   (как и раньше); `RUSTDOCFLAGS="-D warnings" cargo doc --workspace
   --no-deps` — **PASS**.
9. Сличение с платформой: контракт `measure-dom.bsl` (281 строка) и фикстура
   `dom-model.bsl` (90 строк) совпадают с платформенным выводом ПОСТРОЧНО,
   без единого расхождения.

**Замер производительности (A/B, чередование, один сеанс).** Хранение
атрибутов в стеке открытых элементов сперва было сделано копией
(`attrs.clone()` на каждый начальный тег) и стоило `xml_parse` систематических
двух процентов: пары «старый/новый» 1473/1485, 1458/1478, 1456/1505,
1466/1499, 1444/1483 мс — новый медленнее во всех пяти. Копия заменена на
общий `Rc<Vec<XmlAttr>>` (событие и стек держат один и тот же вектор), после
чего разница ушла в шум: 1450/1475, 1449/1475, 1491/1470, 1497/1487,
1488/1478, 1452/1459, 1448/1465, 1495/1550 — знак меняется, медианы 1488 и
1475. Базовый бинарник собран из отдельного git worktree на `HEAD`; таблица
`benchmarks/README.md` не трогалась.
2026-08-12T14:06:30Z  FAIL  dom-model  fable=FAIL opus=FAIL  panel=1

## 2026-08-12 — dom-model (итерация 2: починка двух FAIL-ревью)

**Задача:** ровно два блокирующих дефекта из `REVIEW_FABLE.md` и
`REVIEW_OPUS.md` плюс две дешёвые оговорки. Сама модель DOM, замеры,
фикстура `dom-model` и якорь `DOM.DOC.ELEMENT_BY_ID` не трогались.

**Изменённые файлы:** `crates/bsl-rt/src/dom.rs` (предел глубины,
протаскивание документа, четвёртое «сознательное расхождение» в шапке,
четыре новых юнит-теста); `crates/bsl-rt/src/object.rs` (`DomNode` и
`DomList` получили поле документа); `crates/bsl-rt/src/lib.rs` (десять
площадок исчерпывающего `match` по этим вариантам);
`crates/bsl-rt/src/open_questions.rs` (`DOM.MAX_DEPTH`);
`tests/conformance/measure/measure-all.bsl` (одна строка `М("DOM.MAX_DEPTH", Р)`);
`tests/conformance/measure/platform.tsv` (свежий прогон).

**Дефект 1 — `abort` на глубоко вложенном XML.** В `dom.rs` заведён
`MAX_DOM_DEPTH = 500` по образцу `MAX_JSON_DEPTH` (`json.rs:2355`). Проверка
стоит в `build_into` в обеих точках, где растёт стек элементов: и на
предках, восстановленных из `parser.open_elements()`, и на событии
`ElementStart`. Превышение — `RtError::StackOverflow`, а не построенное
дерево. Одной точки достаточно: мутационного API у дерева нет, чтение —
единственный способ его получить, поэтому ограниченная глубина делает
безопасными рекурсивные `Drop`, `collect_text` и `descendants`;
итеративная переделка обхода не понадобилась.

Проверено A/B на одном и том же скрипте (`Попытка` вокруг всего построения,
документ `<a>`×30000 + `x` + `</a>`×30000, debug-сборка):

- HEAD: «построено / детей: 1 / конец», затем `thread 'main' has overflowed
  its stack, fatal runtime error: stack overflow, aborting`, **exit 134** —
  падение приходится на разрушение дерева, уже после всего вывода, и
  `Попытка` его не ловит;
- с правкой: «ошибка поймана / конец», **exit 0**.

**Дефект 2 — узел не переживал документ.** `BslObject::DomNode` стал
`(Rc<DomNode>, Rc<DomNode>)` — узел и его документ (у самого документа один
и тот же `Rc` дважды), `DomList` получил третьим полем тот же документ.
Цикла `Rc` это не создаёт: сильная ссылка лежит в ЗНАЧЕНИИ, снаружи дерева,
а `parent`/`owner` внутри дерева остались `Weak`. Тождество и хэш
по-прежнему по указателю УЗЛА (`lib.rs`, ветки `PartialEq` и `Hash`), так
что измеренные «тождество узлов = Да» и «тождество списков = Нет» не
поехали.

A/B на идиоме `Функция ВзятьКорень(...) ... Возврат Д.ЭлементДокумента`:

- HEAD: `родитель: []`, `владелец: []`, `элемент-владелец: []`;
- с правкой: `родитель: [ДокументDOM]`, `владелец: [ДокументDOM]`,
  `элемент-владелец: [ЭлементDOM]`, `дети: 1`.

**Замер `DOM.MAX_DEPTH`.** Зонд — нижняя граница по образцу
`JSON.MAX_DEPTH`: цикл собирает `<а>`×500 + `х` + `</а>`×500, читает
`ПостроительDOM` внутри `Попытка` и печатает `Ок`/`Пусто`/`Ошибка` (третья
ветвь — потому что пустой документ платформа отдаёт молча, ошибкой это не
считая). Растущий зонд намеренно не ставится: он рискует унести весь сеанс.
Платформа 8.3.27 ответила **`DOM.MAX_DEPTH  Ок`** — 500 уровней читаются, и
наш интерпретатор на том же скрипте даёт `Ок`. Маркер `НЕ ИЗМЕРЕНО` при
этом ОСТАЁТСЯ: замер фиксирует нижнюю границу, а не платформенный предел, —
ровно как у JSON.

`platform.tsv` перевыложен целиком свежим прогоном (`run-on-1c.sh`, 71
строка, затем `--ingest-measurements`). Диф против состояния ДО задачи
(`HEAD~1`) — ровно две новые строки, `DOM.DOC.ELEMENT_BY_ID
Неопределено|Неопределено` и `DOM.MAX_DEPTH Ок`; ничего прежнего не
изменилось. Порядок строк вернулся к сортировке по ИД: её делает
`--ingest-measurements`, а checkpoint прошлой итерации сохранил сырой вывод
платформы в порядке скрипта. Ингест напечатал одно расхождение —
`SQRT.SMALL_ARG`, известное и оставленное сознательно.

**Оговорки из «не блокирующего».** В шапку `dom.rs` добавлено четвёртое
сознательное расхождение: у предков, восстановленных из `open_elements()`,
префикс атрибута резолвится `parser.namespace_of` по ТЕКУЩЕЙ области
видимости, так что переобъявление того же префикса глубже по документу
победит; на платформе случай не мерился. Вакуумная строка приёмки
JIT-теста исправлена ещё в `PLAN.md` этой итерации: тест живёт в
`crates/bsl-cli/tests/conformance.rs`, с `-p bsl-vm` он давал «0 passed».

**Что осталось неизвестным.** Настоящий предел вложенности у платформы —
`DOM.MAX_DEPTH` закрывает только «500 обязаны читаться». Что 8.3.27 делает
на 100 000 уровнях (ошибка, свой предел или падение клиента), не мерилось и
мериться этим зондом не будет. Отдельно: наш вывод на строке
`DOM.DOC.ELEMENT_BY_ID` — `Ошибка|Неопределено` против платформенного
`Неопределено|Неопределено`; расходится первая половина, где документ
содержит внутреннее подмножество DTD (`<!ATTLIST ...>`), которое наш
`ЧтениеXML` отвергает. Это существующее ограничение разбора DTD, к обоим
чинимым дефектам отношения не имеющее и в эту задачу не входящее.

**Команды и результаты:**

1. `cargo build --workspace` — **PASS**.
2. `cargo test -p bsl-rt dom` — **19 из 19 ok** (было 15; четыре новых:
   `dom_document_deeper_than_the_limit_is_an_error_not_a_crash`,
   `dom_document_at_the_depth_limit_still_works`,
   `dom_node_value_outlives_the_document_variable`,
   `dom_node_values_of_one_node_are_equal_and_hash_alike`).
3. `cargo test -p bsl-rt --test open_questions_registry` — **2 из 2 ok**
   (тройка `DOM.MAX_DEPTH` сходится в обе стороны).
4. `cargo test -p bsl-cli --test conformance -- --nocapture` — **4 из 4 ok**,
   «26 проверено, 23 пропущено», `dom-model` в пропущенных НЕТ.
5. `cmp tests/conformance/fixtures/dom-model.expected
   tests/conformance/fixtures/dom-model.platform.txt` — **совпадают**
   побайтно.
6. `cargo test -p bsl-cli --test conformance
   the_jit_agrees_with_the_interpreter_on_every_script` — **PASS**, «режимы
   сверены на 46 скриптах».
7. `cargo fmt --all -- --check` — **PASS**;
   `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**;
   `cargo test --workspace` — **ноль failed**, один ignored (как и раньше);
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — **PASS**.
8. `./tests/conformance/measure/1c/run-on-1c.sh` — снято 71 строка;
   `cargo run -p bsl-cli -- --ingest-measurements
   tests/conformance/measure/platform.tsv` — «СОВПАЛО: 22 из 26», одно
   известное расхождение `SQRT.SMALL_ARG`.
2026-08-12T14:50:51Z  PASS  dom-model  panel=1

## 2026-08-12 — dom-write: создание, мутация и запись DOM (implement, итерация 1)

**Задача.** `PLAN.md` (dom-write): фабрики узлов у `ДокументDOM`, вставка и
удаление детей и атрибутов, сериализация дерева обратно через `ЗаписьXML`,
круговая фикстура и эталон с платформы.

**Замер — семь прогонов, и справка врёт трижды.** Новый контракт-скрипт
`tests/conformance/measure/measure-dom-write.bsl` (271 строка проб) снимался
`run-on-1c.sh` семь раз: первый прогон разведал ИМЕНА, дальше уточнялись
углы. Что справка обещала и чего на 8.3.27 НЕТ: `СоздатьЭлементNS`,
`СоздатьАтрибутNS`, `CreateElementNS`, `CreateAttributeNS`. Пространство
имён задаёт ВТОРАЯ форма того же метода — `СоздатьЭлемент("urn:х", "п:а")`.
Тип-писатель всё-таки `ЗаписьDOM` (`Тип("ЗаписьDOM")` печатается «Запись
DOM», `DOMWriter` — тот же тип), а `Записать(Узел, ЗаписьXML)` берёт узел
ПЕРВЫМ: обратный порядок отвергается.

Заодно выяснилось, что `Новый ДокументDOM` платформа СТРОИТ (пустой
документ: `#document`, детей ноль, `ЭлементДокумента` — `Неопределено`,
`ВерсияXML` — «1.0», владелец — сам он), а `Новый ЭлементDOM`,
`Новый ТекстDOM` и `Новый РеализацияDOM` — нет.

**Три находки, которые изменили модель.**

1. **`ЛокальноеИмя` и `Префикс` у созданных узлов НЕ выводятся из имени.**
   `СоздатьЭлемент("п:а")` даёт `ЛокальноеИмя` и `Префикс` ПУСТЫМИ, а
   `СоздатьАтрибут("п:а")` — `ЛокальноеИмя`, равное ПОЛНОМУ имени `п:а`.
   Двухаргументная форма расщепляет: префикс `п`, локальное `а`. Поэтому в
   `DomNode` появились ХРАНИМЫЕ поля `local`/`prefix` вместо вывода через
   `local_of`/`prefix_of`. Следствие видно и в поиске: созданный атрибут
   `п:а` находится по ПОЛНОМУ имени (`ПолучитьАтрибут("п:а")` — «1»), а
   разобранный — нет.
2. **Элемент, созданный формой `(URI, Имя)`, сразу несёт объявление
   пространства имён АТРИБУТОМ** (`xmlns:п` либо `xmlns`, URI
   `http://www.w3.org/2000/xmlns/`). Это вылезло на первом же прогоне
   фикстуры: платформа печатала у `к:цена` лишний атрибут. При этом
   `УстановитьАтрибут(URI, Имя, Значение)` объявления НЕ добавляет — там
   его выдумывает писатель.
3. **`ДочерниеУзлы` и `Атрибуты` — ЖИВЫЕ окна, а не снимки.** Список,
   взятый ДО `ДобавитьДочерний`, показывает уже двоих детей («в списке=2»),
   коллекция атрибутов до `УстановитьАтрибут` — уже два. А вот результат
   `ПолучитьЭлементыПоИмени("*")` остаётся прежним (2), тогда как свежий
   запрос находит 3 — это снимок. `DomListKind` из плоского перечисления
   стал перечислением с полезной нагрузкой: `Nodes(узел)`,
   `Attributes(элемент)`, `Elements(снимок)`.

**Пространства имён при записи.** Правило снято по кускам:
`<к:а xmlns:к=".." xmlns=".." к:б="2">` выходит как
`xmlns="urn:по" xmlns:к="urn:к" к:б="2"` — объявления идут ПЕРЕД обычными
атрибутами и отсортированы по ИМЕНИ (`xmlns` < `xmlns:а` < `xmlns:я`), а
обычные атрибуты сохраняют порядок документа (`я="1" а="2" ю="3"` так и
выходит). Писатель ДОБАВЛЯЕТ объявление, которого в атрибутах нет
(`<корень xmlns:п="urn:х" п:а="1"/>`, `<б xmlns="urn:по"/>` у отдельно
записанного потомка) и ОПУСКАЕТ уже действующее в области видимости — даже
если у элемента есть свой атрибут-объявление (`<к:цена/>` внутри родителя с
тем же `xmlns:к`). Переобъявление того же префикса другим URI сохраняется.

**Побочная находка в `XmlWriter`: перевод строки принадлежит НАЧАЛУ узла.**
Прямая запись двух моделей не различала (`ЗаписатьОбъявлениеXML` + элемент
даёт одно и то же), а запись дерева различила: документ без корня платформа
отдаёт как `<?xml version="1.0"?>` БЕЗ хвостового перевода строки, а
одиночный комментарий — как `<!--к-->` без ведущего. Поэтому
`write_declaration` больше не дописывает `\n`, а `write_start_element`,
`write_comment`, `write_processing_instruction` и `write_cdata` зовут новый
`newline_before_node`, который молчит на пустом выводе. Все измеренные
случаи прямой `ЗаписьXML` (`XML.WRITE.*`, 20 строк) от этого не
шевельнулись.

**Предел вложенности теперь охраняется в ДВУХ точках.** Комментарий у
`MAX_DOM_DEPTH` утверждал «мутационного API у дерева нет» — с этой задачей
он перестал быть правдой, поэтому `check_insert` складывает уровень
приёмника с высотой вставляемого поддерева и сверяет с тем же числом.
Инвариант держится по индукции, так что рекурсия в `element_height`
ограничена сама собой. Новый тест собирает мутацией дерево ровно по
предел: текст на последний уровень ещё влезает, элемент уже нет
(`RtError::StackOverflow`, ловится `Попытка`).

**Что сознательно НЕ реализовано** (замеры сняты и лежат в контракте, чтобы
не мерить заново): `КлонироватьУзел` (измерено: глубокий клон копирует детей
И атрибуты, поверхностный — НИ детей, НИ атрибутов, вопреки спецификации
DOM), `ИмпортироватьУзел`, `СоздатьФрагментДокумента` (дети фрагмента при
вставке переезжают, сам он остаётся пустым) и `СоздатьСсылкуНаСущность`.
Ничего из этого в задачу («создание узлов у `ДокументDOM`, вставка и
удаление детей и атрибутов, сериализация») не входит.

**Расхождение, которое остаётся.** `УстановитьАтрибут` и `УдалитьАтрибут` на
платформе ПРОЦЕДУРЫ: `Р = Э.УстановитьАтрибут("а", "1")` она отвергает, а
здесь вернётся `Неопределено`. Различия «процедура/функция» у встроенных
методов нет во всей реализации (то же у `ЗаписатьТекст` и десятков других),
и заводить его ради одного семейства нельзя. Записано в шапке `dom.rs`.

Новых `НЕ ИЗМЕРЕНО` не появилось: всё, что решалось бы рассуждением, ушло в
пробы — включая пустой URI без префикса (принимается), элемент в атрибут
(ошибка), `ТекстовоеСодержимое` тексту и атрибуту, `Данные` инструкции
обработки, запись двух корней (ошибка) и годность имени по краям (`а б` и
`-а` отвергаются, `а.б` и `а:б:в` принимаются). `DOM.MAX_DEPTH` остался
как был.

**Диф контракта против платформы** — 273 строки, расходятся ровно 18: 16
про нереализованные клон/импорт/фрагмент/ссылку на сущность и 2 про
процедуры (`УстановитьАтрибут как функция`, `УдалитьАтрибут`).

**Команды и результаты:**

1. `cargo build --workspace` — **PASS**.
2. `cargo test -p bsl-rt dom` — **30 из 30 ok** (было 19; одиннадцать новых:
   имена созданных узлов, негодные имена и арности, вставка/удаление/замена,
   переезд узла с родителем, таблица допустимых детей, атрибуты,
   записываемые свойства, живость коллекций, форма сериализации,
   расстановка объявлений, предел вложенности при вставке).
3. `cargo test -p bsl-rt --test open_questions_registry` — **2 из 2 ok**.
4. `cargo test -p bsl-cli --test conformance -- --nocapture` — **4 из 4 ok**,
   «27 проверено, 24 пропущено» (было 26/23), `dom-write` в пропущенных НЕТ.
5. `cmp tests/conformance/fixtures/dom-write.expected
   tests/conformance/fixtures/dom-write.platform.txt` — **совпадают**
   побайтно; вывод интерпретатора на фикстуре совпадает с эталоном
   побайтно же.
6. `cargo test -p bsl-cli --test conformance
   the_jit_agrees_with_the_interpreter_on_every_script` — **PASS**, «режимы
   сверены на 48 скриптах» (было 46).
7. `cargo fmt --all -- --check` — **PASS**;
   `cargo clippy --workspace --all-targets -- -D warnings` — **PASS**;
   `cargo test --workspace` — **ноль failed**, один ignored (как и раньше);
   `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — **PASS**.
8. `./tests/conformance/measure/1c/run-on-1c.sh` (реестр) — 71 строка,
   `--ingest-measurements` печатает «СОВПАЛО: 22 из 26» и одно известное
   расхождение `SQRT.SMALL_ARG`; `platform.tsv` после прогона побайтно тот
   же, что в коммите, — якори сеанса целы.
9. `./tests/conformance/measure/1c/run-on-1c.sh
   tests/conformance/measure/measure-dom-write.bsl` — 273 строки;
   `run-on-1c.sh tests/conformance/fixtures/dom-write.bsl` — 111 строк
   эталона.

**Единственные два опкода задачи** — `NewDomDocument` и `NewDomWriter`
(`Новый` для двух новых типов), проведены через `instr.rs`, `compiler.rs`,
`write_instr`/`parse_instr`/`OPCODES`, классификацию эффектов в `bundle.rs`,
корпус round-trip и `step`; `FORMAT_VERSION` 7 -> 8. Плановая фраза «новых
опкодов не предвидится» оказалась неверной: план не знал, что писатель и
пустой документ — конструируемые типы.
2026-08-12T15:55:03Z  PASS  dom-write  panel=0
