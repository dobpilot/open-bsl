# План реализации и приёмки

Все пункты ниже — будущая работа. Создание этого пакета не означает, что
код уже разделён. Карта владельцев и общие ворота определены в
[`design.md`](design.md); действующий подробный план VM и его измерения —
в [`docs/plans/bsl-vm-refactor.md`](../../../docs/plans/bsl-vm-refactor.md).

## 1. Зафиксировать проверяемую базу

- [x] 1.1 Перед реализацией повторно проверить актуальные OpenSpec changes и HEAD; записать baseline и перекрывающиеся работы, не менять чужой diff.
- [x] 1.2 На baseline выполнить fmt, clippy, build, workspace tests и rustdoc из design; записать результаты, пропуски и список тестов затронутых крейтов через `cargo test -p <crate> -- --list`.
- [x] 1.3 Добавить и запустить интеграционные compile-smoke тесты прежних Rust-путей и сигнатур переносимых элементов; отдельно сохранить проверку размера/выравнивания `BslValue` и `RtError` относительно baseline, не объявляя стабильный Rust ABI.
- [x] 1.4 Проверить на baseline API-reference, open-questions registry, bytecode round-trip, JIT-корпус и DAP handshake; сохранить список реально исполненных тестов.

## 2. Разделить `bsl-rt`

Пошаговые экраны и A/B отменены решением владельца от 5 сентября
2026 года; цена серии снимается скобками в задаче 4.7.

- [x] 2.1 Перенести `RtError`, `ComponentError`, `RtResult` и их реализации в приватный `error.rs`, восстановить прежние реэкспорты; `cargo test -p bsl-rt` и smoke-тесты проходят, тексты ошибок не изменились.
- [x] 2.2 Перенести связное ядро `BslValue` в `value/mod.rs` с прежним реэкспортом; проверить layout, арифметику, равенство/Hash и smoke-тесты.
- [x] 2.3 Выделить строковые `impl` в `value/strings.rs` без повторения алгоритмов `BslString`; проверить строковые тесты и конформанс.
- [x] 2.4 Выделить операции дат в `value/dates.rs`; проверить даты, host-clock тесты и конформанс.
- [x] 2.5 Выделить полиморфные коллекции/свойства в `value/collections.rs`; проверить индексацию, смену формы и кэшированные свойства, JIT-корпус.
- [x] 2.6 Выделить табличные методы в `value/tables.rs`, оставив алгоритмы у `table.rs`; проверить таблицы, типизированные колонки, копирование.
- [x] 2.7 Выделить существующие методы ввода-вывода в `value/io.rs`; проверить двоичные данные/буфер, запись текста и host filesystem; новых слоёв диспетчеризации не появляется.
- [x] 2.8 Разместить относящиеся к областям unit-тесты у владельцев; сравнить список/число тестов с baseline, запустить `cargo test -p bsl-rt --test open_questions_registry` и убедиться, что реестр, измерения и маркеры не изменили смысла.
- [x] 2.9 Провести ревью видимости и реэкспортов runtime; API reference и внешние smoke-тесты проходят, нет новых зависимостей, `dyn`-обёрток и дублирования алгоритмов; пройти полные ворота design.

## 3. Разделить `open-bsl::jobs`

- [x] 3.1 Выделить `jobs/registry.rs` вместе с unit-тестами; проверить admission, ключи, бюджеты, историю и terminal-переходы без Engine/VM; реестр не импортирует driver/service.
- [x] 3.2 Выделить `jobs/runtime.rs` и сохранить `open_bsl::jobs` фасадом; проверить ленивый старт, shutdown во время старта, panic/replacement и прежние публичные пути.
- [x] 3.3 Выделить `jobs/prepare.rs`; проверить разрешение целей, обязательные аргументы, каталог/entry, импорт библиотек и host-профили без изменения таблиц имён и DTO.
- [x] 3.4 Выделить `jobs/worker.rs`, сохранив вместе резидентов и гарды; проверить вложенное helping-ожидание, закрепление за потоком и окно commit/shutdown. Перенос — move-only коммит: `git diff --color-moved=dimmed-zebra` показывает чистые переносы (новые строки — только `use` и видимость), инвентарь `lock()`/guard-точек по функциям совпадает до и после.
- [x] 3.5 Выделить `jobs/service.rs`; `cargo test -p open-bsl --test job_review_fixes` проходит, включая профили, сообщения, бюджеты и временное хранилище.
- [x] 3.6 После всех переносов отдельно запустить `a_shutdown_during_the_commit_window_rolls_back`, `a_terminal_event_in_the_helping_window_is_not_lost`, `wait_first_change_parks_without_periodic_wakeups` и наборы `scheduler`, `http_async`, `configuration`; записать исполнение тестов, а не только код возврата команды с фильтром.
- [x] 3.7 Сверить тестовый состав jobs, внешние пути, границу DTO и минимальность видимости; `cargo test -p open-bsl` и полные ворота design проходят.
- [ ] 3.8 После серии jobs выполнить канарейку: чередующийся замер `background_jobs` против бинарной базы `59e7056`; записать пары и условия — отчёт, не вердикт.

## 4. Завершить стадии 2–7 принятого плана VM

Каждый следующий пункт закрывается отдельно: целевые тесты и полный
JIT-корпус. Пошаговые экраны и A/B отменены решением владельца от
5 сентября 2026 года — цена серии снимается скобками в задаче 4.7.
Выполненную стадию кэшей не повторять.

- [x] 4.1 Стадия 2: выделить `snippet.rs`; проверить Execute/Eval, REPL, глубину вложения и перенумерацию библиотек; отметить стадию в таблице принятого плана VM.
- [x] 4.2 Стадия 3: выделить `linking.rs`; проверить ошибки связывания, компонентные свойства/методы, совместное использование `HostIo` с dynamic/debug.
- [x] 4.3 Стадия 4: выделить `scheduler.rs`; проверить async, квантование, HTTP resume и отмену; горячие safe-point помощники не уехали.
- [x] 4.4 Стадия 5: выделить `modules.rs`; проверить изоляцию сеансов, ленивую инициализацию, импортные вызовы и соответствие каталог/кэши. Долг параллельных таблиц закрыть единым аксессором отдельным diff без изменения владения и политики прогрева; записать решение.
- [ ] 4.5 Стадия 6: выделить `debug.rs`; пройти `cargo test -p bsl-cli --test dap_handshake`, `cargo test -p bsl-compiler --test debug_info`, VM debug-тесты и сценарии evaluate/кадров.
- [ ] 4.6 Стадия 7: выделить `entry.rs` и сохранить все публичные пути реэкспортами; пройти smoke-тесты, embedding, configuration и REPL.
- [ ] 4.7 Закрыть скобки серии: чередующийся A/B против бинарной базы `59e7056` по протоколу design (7 пар, полный набор сценариев) и один экран `hot-code-diff.sh` против той же базы с прочитанным и объяснённым диффом; отчёт с сырыми парами, размахами, условиями и commits предъявить владельцу — вердикт его, без числового порога; результаты записать в принятый план VM.

## 5. Общая приёмка

- [ ] 5.1 Проверить каждый requirement нового delta по его сценариям; сохранить карту requirement → тест либо результат архитектурного ревью, включая недоступность новых модулей извне.
- [ ] 5.2 Проверить деревья зависимостей и неизменность `Cargo.lock`, профилей и `.cargo/config.toml`; `bsl-vm` без normal-зависимости от фронтенда, `bsl-bytecode` без frontend normal/dev-зависимостей.
- [ ] 5.3 Выполнить `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build --workspace`, `cargo test --workspace`, `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps`.
- [ ] 5.4 Явно выполнить JIT-корпус, API-reference и bytecode round-trip; не изменять `.expected`. Отдельно указать, запускался ли конфиденциальный `table_compare2` с `OPEN_BSL_TABLE_COMPARE2_CASES`, и все остальные пропуски окружения.
- [ ] 5.5 Обновить карту текущей архитектуры по фактическому результату; проверить ссылки и отсутствие конкурирующих checklist. Отложенные кандидаты из аудита не объявлять выполненными.
- [ ] 5.6 Провести итоговое ревью, слить delta в текущие specs и архивировать change датой завершения; `openspec validate --all --strict --no-interactive` проходит. До реализации этот пункт не закрывать.
