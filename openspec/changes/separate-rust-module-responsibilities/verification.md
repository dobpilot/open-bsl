# Журнал выполнения

## 1.1. Исходная версия

- Ветка: `bsl-rt-v4.3`; исходный HEAD: `408f4d5`.
- Перед началом рабочее дерево чистое. Единственный активный change —
  `separate-rust-module-responsibilities`; перекрывающихся изменений нет.
- `408f4d5` отличается от `59e7056` только документацией и OpenSpec.
  Для функциональных проверок исходная версия — `408f4d5`.
- Прочитаны `AGENTS.md`, proposal, design, tasks, delta и план VM.
- Среда: `rustc 1.97.1 (8bab26f4f 2026-07-14)`,
  `cargo 1.97.1 (c980f4866 2026-06-30)`.
- `OPEN_BSL_TABLE_COMPARE2_CASES` не задан: конфиденциальный набор не
  будет считаться исполненным по зелёному общему тесту.
- По границе поручения пункты 3.8, 4.7 и 5.6 остаются владельцу.
  Бенчмарки и машинные экраны не запускаются; суждений о
  производительности в этом журнале нет.
- Корневой `TASKS.md`, эталоны `.expected`, сгенерированный API reference,
  реестр открытых вопросов и измерительные скрипты не редактируются.

Полные логи проверок текущей сессии находятся в
`/tmp/open-bsl-module-refactor.zFAuBT/`; подтверждённые результаты и
сопоставление тестов фиксируются ниже по шагам.

## 1.2. Проверки базы

На исходной версии fmt, Clippy, build и полный workspace test прошли:
1730 passed, 0 failed, 1 ignored. Первый запуск тестов в песочнице
остановился на запрете локального HTTP-сокета; повторный с разрешёнными
сокетами завершился с кодом 0. Единственный явный ignore —
`dump_for_the_platform_crosscheck`, который готовит файлы для платформы.
Конфиденциальный `table_compare2` без переменной окружения не исполнялся.

Строгий rustdoc на `408f4d5` обнаружил публичную ссылку на приватный
`RunCaches` (`bsl-vm/src/lib.rs:2019`). Владелец разрешил отдельное
исправление: коммит `eaf715a` заменяет ссылку обычным кодовым выделением.
Поведение и видимость не менялись.

После исправления все обязательные команды прошли повторно: fmt,
Clippy, build, workspace test (1730 passed, 0 failed, 1 ignored), строгий
rustdoc и отдельный JIT-корпус. Логи: `rustdoc-preparation-*.log`.
Эти проверки относятся и к документальным коммитам 1.1/1.2: после
прогона исходники и состав тестов не менялись.

В `baseline-tests.txt` сохранён вывод инвентаризации `cargo test -p bsl-rt
-p bsl-vm -p open-bsl -- --list`: 672 зарегистрированных теста с
группировкой по исполняемым файлам. Инвентаризация не подменяет исполнение.
Layout исходной сборки x86-64 Linux, rustc 1.97.1:
`BslValue` — size 24, align 8; `RtError` — size 48, align 8.
Это локальная проверка представления, не обещание стабильного Rust ABI.

## 1.3. Публичные пути и layout

Добавлены интеграционные тесты `bsl-rt/tests/public_paths.rs` (3),
`bsl-vm/tests/public_paths.rs` (2) и `open-bsl/tests/job_public_paths.rs` (2).
Они используют только прежние публичные пути, проверяют сигнатуры
предметных методов, доступность интерфейсов VM/jobs и существующие
trait-контракты. Проверка layout ограничена исходной платформой x86-64
Linux; на другие цели числовое утверждение не распространяется.

Целевые запуски всех семи тестов прошли. Исправлены две ошибки в новых
тестах до приёмки: `NameId` создаётся публичным `from_index`, а не через
закрытое поле; убран лишний `unwrap()` над литеральным `Ok`.
Полные ворота `step-1.3-*.log` прошли: fmt, Clippy, build,
workspace test (1737 passed, 0 failed, 1 ignored), строгий rustdoc,
отдельный JIT-корпус. Production-код на этом шаге не изменялся.

## 1.4. Явные проверки контрактов

Повторно исполнены API-reference (1 тест), open-questions registry (3),
bytecode text round-trip (6), DAP handshake (16), JIT-корпус (1) и
конформанс с `.expected` (1). Все прошли; список фактически исполненных
тестов и сводки сохранены в `baseline-contracts.txt`, полные логи —
`step-1.4-*.log`. Production-код по-прежнему соответствует исходной базе
с единственным исправлением rustdoc; функциональные ворота шага 1.3
покрывают тот же состав исходников и тестов.

Конформанс проверил 48 сценариев, пропустил 76 без `.expected`; их
полный список сохранён в свидетельстве, эталоны не создавались.
Сверка JIT использует существующее исключение `SLOW_TO_COMPARE`:
`n-body-perf.bsl`, `n-body-precision.bsl`, `n-body-pow-variant.bsl`.
Это ограничения исходного раннера, не новые пропуски рефакторинга.

## 2.1. Модуль ошибок runtime

`RtError`, `ComponentError`, `RtResult` и их реализации перенесены в
приватный `bsl-rt/src/error.rs`; прежние имена реэкспортированы из корня.
Сравнение перенесённого блока с исходником подтверждает единственное
отличие внутри блока: rustdoc-ссылка на `HostErrorCode` получила явный
путь `crate::HostErrorCode`, чтобы не держать неиспользуемый импорт.
Тексты ошибок, варианты и классификация исключений не изменены.

Целевые smoke-тесты прошли. Полные ворота `step-2.1-*.log` прошли:
fmt, Clippy, build, workspace test (1737 passed, 0 failed, 1 ignored,
включая тесты `bsl-rt`), строгий rustdoc, отдельный JIT-корпус.
`Cargo.lock`, профили сборки и `.cargo/config.toml` не менялись.

## 2.2. Ядро значения

`BslValue` и связный блок его операций перенесены в приватный
`value/mod.rs`; корневой путь сохранён реэкспортом. `reserve_hint`
остаётся в корне. Предметные группы пока не разделены, тесты остаются
на прежних местах до шага 2.8.

Текстовое сравнение подтвердило неизменность вариантов типа и всего
блока реализаций, кроме `as_str`: `pub(super)` сохраняет доступ из
корня крейта и его дочерних модулей, который до переноса обеспечивала
приватность корневого метода. Других изменений видимости нет.

Полные ворота `step-2.2-*.log` прошли: fmt, Clippy, build, workspace
test (1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный
JIT-корпус. В выводе подтверждено исполнение smoke/layout, арифметики,
равенства и законов Hash. Layout остаётся 24/8 для `BslValue` и 48/8
для `RtError` на исходной платформе. Замеры не проводились.

## 2.3. Строковые операции

Строковой блок `impl BslValue` и его локальный помощник
`optional_positive_usize` перенесены в приватный `value/strings.rs`.
Общие приведения `as_str` и `as_usize` остаются у `value`; расширять
видимость не потребовалось. Сравнение исходного и перенесённого блоков
подтвердило полное совпадение, включая комментарии. `string.rs`
не изменён: алгоритмы `BslString` не копировались.

Полные ворота `step-2.3-*.log` прошли: fmt, Clippy, build, workspace
test (1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный
JIT-корпус. Строковые unit-тесты, преобразование регистра UUID и
конформанс с `.expected` исполнены успешно. Эталоны не менялись.

## 2.4. Календарные операции и часы

Календарные методы, их локальные приведения и обращения к `HostEnv`
перенесены в приватный `value/dates.rs`. Текст блока, включая комментарии,
совпадает с исходным; изменений видимости нет. Арифметика дат и её
помощники остаются у ядра `value`. Алгоритмы `date.rs` не менялись.

Полные ворота `step-2.4-*.log` прошли: fmt, Clippy, build, workspace
test (1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный
JIT-корпус. В логе подтверждено исполнение 11 тестов `date::tests`,
`a_test_clock_advances_on_its_own_terms`,
`a_fixed_clock_makes_the_time_reproducible`,
`jit::a_fixed_clock_survives_a_jit_run`, `two_states_do_not_share_a_clock`
и конформанса с `.expected`.

## 2.5. Коллекции и свойства

В приватный `value/collections.rs` перенесены конструкторы массива,
структуры и соответствия, полиморфная индексация, свойства, кэшированный
доступ и операции добавления/удаления/очистки коллекций. Все пять
перенесённых блоков текстово совпадают с исходными; изменений видимости
нет. Общий `index_as_usize` остаётся у `value`, поскольку его использует
также табличный `table_move`. Алгоритмы `MapData` и `StructureStorage`
не копировались.

Полные ворота `step-2.5-*.log` прошли: fmt, Clippy, build, workspace
test (1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный
JIT-корпус. Исполнены `array_index_get_set_roundtrip`,
`dictionary_structure_does_not_poison_inline_cache_for_shaped_objects`,
`shape_table_stops_growing_after_degradation`,
`get_prop_inline_cache_stays_correct_across_different_shapes`,
`set_prop_inline_cache_stays_correct_across_different_shapes` и тесты
переходов формы при вставке полей. Маркеры совместимости сохранены.

## 2.6. Табличные операции

Конструктор таблицы, методы `table_*` и локальные помощники проверки
аргументов перенесены в приватный `value/tables.rs`. Оба перенесённых
блока текстово совпадают с исходными; изменений видимости нет.
Алгоритмы `table.rs` остались на месте, повторных реализаций нет.

Полные ворота `step-2.6-*.log` прошли: fmt, Clippy, build, workspace
test (1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный
JIT-корпус. В выводе подтверждены тесты таблиц, включая
`table_copy_makes_an_independent_table`,
`table_copy_takes_only_the_listed_rows_and_columns`,
`table_copy_columns_keeps_the_structure_and_drops_the_rows`,
`table_column_exposes_name_and_type_description`,
`typed_column_matches_the_platform_byte_for_byte` и
`composite_column_types_serialize_in_the_canonical_platform_order`.

## 2.7. Ввод-вывод значения

Текстовая запись, двоичные данные/буфер, доступ к байтовому потоку и
локальный предел разбиения перенесены в приватный `value/io.rs`.
Четыре блока совпадают с исходными, кроме уточнения двух rustdoc-ссылок
через `crate::BslString`. Видимость и порядок операций не менялись;
`bindata`, файловые контракты и кодирование не дублировались. UUID и
диагностический `Display` остаются у ядра.

Первый строгий rustdoc выявил потерявшие контекст ссылки; они исправлены.
Параллельная пересборка после этой правки вызвала конфликт артефактов
`E0460` в doctest `open-bsl`: тот workspace-прогон не засчитан.
Все ворота повторены последовательно, без параллельных Cargo-команд.

Полные ворота `step-2.7-final-*.log` прошли: fmt, Clippy, build,
workspace test (1737 passed, 0 failed, 1 ignored), строгий rustdoc,
отдельный JIT-корпус. Исполнены тесты разбиения/склейки двоичных данных,
`text_writer_close_keeps_the_buffer_when_flush_fails`,
`binary_data_reads_through_the_sessions_file_system`,
`a_failing_close_from_the_session_file_system_is_catchable`,
`two_states_write_into_their_own_file_systems` и файловые JIT-проверки.

## 2.8. Тесты у владельцев

34 исходных корневых теста распределены по проверяемой обязанности:
9 — ядро значения, 11 — коллекции, 9 — ввод-вывод, 1 — строки,
3 — таблица имён и диспетчеризация builtin. Проверка `reserve_hint`
остаётся в корне. Тесты, уже находившиеся у `date.rs`, `table.rs` и
других владельцев, не переставлялись.

Все 40 исходных блоков (34 теста и 6 помощников) совпадают текстово,
кроме `pub(super)` у двух общих тестовых помощников. `num` остаётся
в корневом тестовом модуле для builtin и value; `bin` принадлежит
тестам ядра и используется также I/O-тестами. Помощники не копировались.
Все assertions, комментарии и атрибуты тестов сохранены.

Повторная инвентаризация трёх крейтов сохранена в `runtime-tests.txt`.
Сравнение по прежним именам с явной заменой модульных префиксов:
672 исходных теста сохранены, пропавших нет; текущие 679 отличаются
только семью smoke-тестами шага 1.3. Проверены не только количества,
но и каждый элемент списка. В полном логе все 34 распределённых теста
найдены с результатом `ok`.

Явный `cargo test -p bsl-rt --test open_questions_registry` прошёл:
`the_job_plan_remaining_table_is_registered`,
`measure_script_covers_every_registry_id`,
`open_questions_registry_matches_source_markers` действительно исполнены.
Реестр и измерительные скрипты не менялись.
Последовательные полные ворота `step-2.8-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий
rustdoc, отдельный JIT-корпус.

## 2.9. Ревью границы runtime

`error`, `value` и все пять предметных дочерних модулей объявлены через
приватный `mod`. Корень реэкспортирует `ComponentError`, `RtError`,
`RtResult` и `BslValue`; прежние публичные модули сохранены. Единственная
новая явная видимость production-помощника — `as_str: pub(super)`:
она восстанавливает прежний доступ из корня и его потомков. Видимость
`new_uuid` и `new_binary_data` была `pub(crate)` уже в базе; поля
не раскрывались. Два общих тестовых помощника доступны через
`pub(super)` исключительно при `cfg(test)`.

Сопоставлены перенесённые блоки и владельцы алгоритмов: `string.rs`,
`date.rs`, `table.rs`, `bindata.rs`, `encoding.rs`, `bsl-number` и
production-часть `builtin.rs` не менялись. В агрегате перенесённого
production-кода осталось столько же вхождений `.clone(`, `dyn` и
`Box`, сколько в исходном `lib.rs` (23, 3 и 5 соответственно);
поэтапные текстовые сравнения подтверждают отсутствие новых обёрток
и повторных алгоритмов, а не только совпадение счётчиков.

Манифесты, `Cargo.lock`, профили и `.cargo/config.toml`, generated API,
`.expected`, реестр, измерительные скрипты и корневой `TASKS.md`
не менялись. Дерево runtime по-прежнему идёт только через `bsl-number`;
в normal-дереве VM нет `bsl-syntax`/`bsl-sema`.

Явно прошли три runtime smoke/layout-теста и
`checked_in_api_reference_matches_the_generator`. Полные последовательные
ворота `step-2.9-*.log` прошли: fmt, Clippy, build, workspace test
(1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный JIT-корпус.

Новых открытых вопросов по границе runtime не выявлено.

## 3.1. Реестр jobs

В приватный `jobs/registry.rs` перенесены `RuntimeState`, `JobRecord`,
`JobRegistry`, `AdmissionError`, резервирование ключей и девять unit-тестов.
Закрытые поля не раскрывались. Реестр импортирует только конфигурацию,
runtime DTO и стандартные коллекции/`Arc`; импортов Engine, VM,
driver/service нет.

`fail_all_resident` и `fail_resident_jobs` перенесены вместе с реестром:
это операции конечного перехода над его записями, без управления
потоками/локами. Они получили `pub(super)` для прежних мест вызова;
их тела и вызовы под уже взятым локом сохранены. Это также сохраняет
claim/rollback-тест без зависимости реестра от driver/service.

После удаления первого тестового модуля Clippy начал диагностировать
`items_after_test_module` у оставшегося `pool_tests`. Весь блок
`pool_tests` перемещён в конец того же `jobs.rs` без изменения тела;
рабочие элементы и тестовые шлюзы не менялись. Подавление lint не добавлено.

Прочитан `git diff --color-moved=dimmed-zebra`; цветной diff сохранён
в `step-3.1-final-moved.diff`. Дополнительно текстово сопоставлены
реестр с тестами, terminal-помощники, весь `pool_tests` и весь остаток
`jobs.rs` без импортов/пустых строк: тела совпадают, кроме двух
разрешений видимости. Все 60 точек `lock()` сохранены; равенство тел
подтверждает их порядок по функциям и неизменность guard/Drop-точек.
Новые рабочие строки — подключение модуля, импорты и видимость.

Целевой запуск действительно исполнил все девять тестов реестра;
admission, ключи, бюджеты, история и claim/terminal прошли. После
перемещения `pool_tests` полные последовательные ворота
`step-3.1-final-*.log` прошли: fmt, Clippy, build, workspace test
(1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный JIT-корпус.

## 3.2. Runtime jobs

В приватный `jobs/runtime.rs` перенесены конфигурация, host-профили,
источники времени/идентификаторов, `JobRuntimeShared`, `JobRuntime`,
создание/закрытие пула, bootstrap/supervisor и целиком `pool_tests`.
Семь прежних публичных имён реэкспортированы из `open_bsl::jobs`;
внутренний путь `jobs::random_uuid` также сохранён. Worker-резиденты,
`RunningJob` и его гарды остаются в прежнем модуле до шага 3.4.

Для существующих потребителей внутри jobs пять проверок ожидания,
`SystemJobIds` и одно поле `JobRuntime::shared` получили `pub(super)`.
Поле `workers` осталось закрытым; остальные поля не раскрывались.
Условный импорт `Mutex`/`Condvar` в фасаде обслуживает только прежние
тестовые шлюзы. Новых обёрток и production-операций нет.

Прочитан `step-3.2-moved.diff` с `--color-moved=dimmed-zebra`.
Три перенесённых блока и весь остаток фасада текстово совпадают после
учёта импортов/видимости; все 60 точек `lock()` сохранены, порядок
операций, владение гардами и `Drop` внутри функций неизменны.

Целевой запуск `step-3.2-pool.log` исполнил все 14 pool-тестов, включая
ленивый старт и shutdown во время `Starting`. Полный workspace-прогон
также подтвердил `a_worker_panic_fails_the_job_and_the_worker_is_replaced`,
`three_consecutive_startup_panics_break_the_runtime` и оба jobs smoke-теста.
Последовательные полные ворота `step-3.2-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий rustdoc,
отдельный JIT-корпус. Повтор race-наборов после всей серии ещё предстоит
в задаче 3.6; текущий запуск её не закрывает.

## 3.3. Подготовка заданий

В приватный `jobs/prepare.rs` перенесены `TargetTable`/`TargetFunction`,
`WorkerRecipe`, `build_worker_engine`, `prepare_job`, тестовый
`resolve_target` и построение рецепта `runtime_for_engine`. Прежние
внутренние пути последних двух сохранены реэкспортами фасада.
Поля таблицы целей остаются закрытыми; только `resolve`,
`build_worker_engine` и `prepare_job` получили `pub(super)` для
существующих вызывающих модулей.

Прочитан `step-3.3-moved.diff` с `--color-moved=dimmed-zebra`.
Три перенесённых блока совпадают с исходными после учёта видимости;
остаток фасада также совпадает. Все точки `lock()` (21) этого исходного
файла остались в нём; подготовка не приобрела локов. Таблицы имён,
разрешение целей, DTO, материализация аргументов и создание entry не
менялись. Маркеры `JOB.ASYNC.TARGET` и `JOB.MODULE.INIT` перенесены
вместе с кодом без изменения реестра.

Целевые прогоны исполнили 14 pool-тестов и 27 тестов `job_review_fixes`,
включая разрешение целей, обязательные аргументы,
`a_job_target_calls_a_user_registered_library`, ошибки фабрики,
наследование deny-network и изолированную файловую систему.
Полные последовательные ворота `step-3.3-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий rustdoc,
отдельный JIT-корпус.

## 3.4. Worker и его гарды

В приватный `jobs/worker.rs` перенесены цикл worker, `RESIDENTS`,
`DriveMode`, `drive_local`, `RunningJob`, `StartGuard`, poll,
claim/commit/finish и шлюзы окон commit/helping. `RunningJob` и все
его гарды находятся вместе; поля резидента и тестовых шлюзов закрыты.
Три функции входа и два типа получили `pub(super)` для прежних
потребителей внутри jobs; новых интерфейсов и обёрток нет.

Четыре теста worker/commit/helping и их локальная сцена перенесены
из runtime к worker. Общие `engine`, `params`, `number` перемещены
в один приватный `cfg(test)`-модуль `jobs::test_support`: их тела
сохранены, копий нет. Это позволяет не раскрывать поля шлюзов тестам
соседнего модуля. Остальные десять pool-тестов остаются у runtime.

Прочитан `step-3.4-moved.diff` с `--color-moved=dimmed-zebra`.
Оба production-блока совпадают после учёта видимости и переноса строк
двух длинных сигнатур rustfmt; тела не менялись. Восемь блоков
тестов/помощников, оставшийся runtime и сервисная часть фасада также
совпадают. В совокупности исходных/полученных файлов сохранены все
60 точек `lock()`; равенство тел подтверждает порядок по функциям,
границы guard, места `Drop` и отсутствие переносов действий через локи.

Явно прошли четыре worker-теста, 27 `job_review_fixes` и 9 `http_async`.
Workspace-лог подтверждает исполнение
`a_nested_job_completes_on_a_single_worker_pool`,
`a_job_waiting_for_a_co_resident_of_its_own_worker_does_not_deadlock`,
`a_helper_driven_child_parked_on_http_frees_the_worker`,
`shutdown_cancels_residents_and_rejects_new_submissions` и всех четырёх
перенесённых worker-тестов. Полные последовательные ворота
`step-3.4-*.log` прошли: fmt, Clippy, build, workspace test
(1737 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный JIT-корпус.

## 3.5. Сервисные адаптеры

В приватный `jobs/service.rs` перенесены `JobMessageRoute`,
`WorkerJobService`, `EngineJobService` и `graph_limits_for`.
Прежние пути сервисов внутри крейта сохранены реэкспортами.
Маршрут сообщений и два его поля получили `pub(super)` для
существующей сборки сеанса в prepare; конструктор или обёртка не добавлены.

Прочитан `step-3.5-moved.diff` с `--color-moved=dimmed-zebra`:
новые строки — подключение модуля, импорты и видимость.
Весь перенесённый блок после учёта трёх изменений видимости
текстуально совпадает с исходным: сохранены восемь точек `lock()`,
порядок действий, области guard и существующее клонирование сообщения.
Маркер `JOB.TEMP.NESTED_CAPABILITY` перенесён без изменений.

Целевой прогон `job_review_fixes` исполнил все 27 тестов.
Полные последовательные ворота `step-3.5-*.log` прошли: fmt,
Clippy, build, workspace test (1737 passed, 0 failed, 1 ignored),
строгий rustdoc и отдельный JIT-корпус.

## 3.6. Гонки после всех переносов jobs

Каждый из трёх обязательных тестов запущен отдельно через
`cargo test -p open-bsl --lib jobs::worker::tests::<имя> -- --exact --nocapture`.
Во всех трёх логах есть строка соответствующего теста с `... ok`
и итог `1 passed; 0 failed; 36 filtered out`:

- `a_shutdown_during_the_commit_window_rolls_back`;
- `a_terminal_event_in_the_helping_window_is_not_lost`;
- `wait_first_change_parks_without_periodic_wakeups`.

Отдельный прогон `--test scheduler --test http_async --test configuration`
подтвердил 4, 9 и 22 успешных теста соответственно, без отфильтрованных
и проигнорированных проверок. В том числе исполнены вложенное ожидание
на одном worker, ожидание собственного резидента, helping дочернего
HTTP-задания, отмена припаркованного запроса и shutdown резидентов.
Логи: `step-3.6-commit-race.log`, `step-3.6-helping-race.log`,
`step-3.6-first-change.log`, `step-3.6-integration.log`.

Полные последовательные ворота `step-3.6-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий
rustdoc и отдельный JIT-корпус. Исходники на этом шаге не менялись.

## 3.7. Состав тестов и границы jobs

Повторный `cargo test -p bsl-rt -p bsl-vm -p open-bsl -- --list`
сохранён в `jobs-tests.txt`. В нём 679 проверок, как после шага 2.8:
при нормализации только префиксов модулей нет ни пропавших, ни новых
тестов. Относительно исходной базы 672 разница — прежние семь
compile-smoke проверок шага 1.3. Все 23 jobs unit-теста сохранены:
9 у registry, 10 у runtime, 4 у worker. Тела тестов проверены при
соответствующих переносах, assertions и атрибуты не менялись.

Публичные объявления типов, функций и traits jobs совпадают с базой
после нормализации пробелов. Семь публичных типов/traits доступны
через прежний `open_bsl::jobs`; все пять новых модулей приватны.
Добавленная видимость ограничена `pub(super)` для прежних потребителей
внутри jobs; уже существовавшие `pub(crate)`/публичные поля не расширялись.
Поля `RunningJob`, `StartGuard`, таблиц registry и тестовых шлюзов
сохраняют приватность. Реестр импортирует только конфигурацию, DTO и std;
он не владеет Engine/State/ProgramExecution и не импортирует service/worker.

Совокупно по jobs сохранены 60 точек `.lock()`, 31 `.clone(`,
8 употреблений `dyn` и 2 `Box`; равенство переносимых тел на шагах
3.1–3.5 подтверждает не только количество, но и порядок действий.
Все четыре `НЕ ИЗМЕРЕНО`-маркера совпадают с базой.
`Cargo.lock`, манифесты и `.cargo/config.toml` не менялись.

Целевой `cargo test -p open-bsl` исполнил 155 успешных проверок,
включая два smoke-теста прежних публичных путей, без ignored.
Полные последовательные ворота `step-3.7-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий
rustdoc и отдельный JIT-корпус. Задача 3.8 оставлена владельцу.

## 4.1. Динамические фрагменты VM

В приватный `snippet.rs` перенесены `run_dynamic_snippet`,
`DynamicDepthGuard`, `MAX_DYNAMIC_DEPTH` и `remap_chunk_libraries`.
Три production-блока текстуально совпадают с исходными после учёта
видимости типа гарда, его `enter` и функции запуска (`pub(super)`).
Остаток `lib.rs` совпадает полностью после удаления этих блоков и
добавления подключения модуля; горячее ядро не редактировалось.
Маркер `EXEC.DYNAMIC_DEPTH` сохранён вместе с комментариями.

Первая тестовая сборка обнаружила обращение теста изоляции сеансов к
приватной константе глубины. Тест перенесён в `snippet::tests`, без
раскрытия константы; тело и комментарии совпадают после учёта отступа.
Повторный инвентарь VM содержит прежние 254 проверки, включая два
smoke-теста; единственное изменение имени — префикс этого теста.

Целевые прогоны исполнили 254 теста VM, 24 embedding и четыре REPL-теста
(фильтр `repl` также выбрал существующий тест completion).
В VM-логе подтверждены Execute/Eval, передача параметров по ссылке,
`dynamic_fragment_resolves_its_own_component_requirement`,
`recursion_through_execute_is_a_catchable_error_not_a_crash` и
`snippet::tests::two_sessions_do_not_share_dynamic_nesting_depth`.
Полные последовательные ворота `step-4.1-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий
rustdoc и отдельный JIT-корпус. Стадия 2 отмечена в таблице плана VM.

### Решение перед 4.2: видимость полей связывания

Карта стадии 3 требует перенести `HostIo` и `LinkedComponents` в
`linking.rs`. `HostIo` строится и разбирается литералами в ядре/входах,
включая `step_cold`; его пять полей нужны за пределами нового модуля.
Поля `LinkedComponents` читаются в ядре, `jit/mod.rs` и `snippet.rs`
(таблицы методов/свойств, контекст host-сервисов, registry и scope).
Прямой перенос потребует массовой видимости полей внутри крейта:
`pub(super)` в дочернем модуле корня здесь эквивалентен `pub(crate)`.

Владелец 6 сентября разрешил видимость полей этих двух типов внутри
крейта. Принят прямой перенос с `pub(super)` только для существующих
потребителей: пять полей `HostIo` и тринадцать `LinkedComponents`.
`functions` и `constructors` остаются приватными. Внешний API не
расширяется; аксессоры и изменения обращений горячего ядра не нужны.

## 4.2. Связывание компонентов VM

В приватный `linking.rs` перенесены `HostIo`, `LinkedComponents`,
их реализации, обе карты мемоизации, связывание и разрешение компонентных
методов/свойств. `resolve_component_property` остаётся приватной функцией;
остальные необходимые ядру функции и типы доступны через `pub(super)`.
Тестовый путь `link_verified` импортируется в корень только под `cfg(test)`.

Весь блок совпадает с исходным после учёта видимости и переноса строк
двух длинных сигнатур rustfmt. Оставшийся `lib.rs` отличается только
подключением модуля и импортами: тела `step`, `step_cold`, JIT, кэшовых
помощников и snippet не редактировались. Тесты на этом шаге не менялись.

Целевые прогоны исполнили 254 теста VM, 24 embedding и 20 host_environment.
Подтверждены отказ при несовпадении компонента, связывание функций и
конструкторов, свойства/методы/индексы объектов, собственные требования
динамического фрагмента и общие host-потоки в builtin/component/dynamic/JIT.
Полные последовательные ворота `step-4.2-*.log` прошли: fmt, Clippy,
build, workspace test (1737 passed, 0 failed, 1 ignored), строгий rustdoc
и отдельный JIT-корпус. Стадия 3 отмечена в таблице принятого плана VM.

### Решение перед 4.3: область разрешения видимости

Владелец 6 сентября подтвердил разрешение необходимой внутренней
видимости для оставшихся выносов VM. Оно не меняет внешний API,
владение, границы заимствований и обращения горячего ядра.

## 4.3. Планировщик VM

В приватный `scheduler.rs` перенесены конфигурация, задачи, обещания,
HTTP-завершения, `ExecutionWaker`, `AsyncState` с реализациями и
холодные функции возобновления. `TaskPosition`, `task_position`,
`crossed_scheduler_safe_point`, `consume_scheduler_safe_point` перенесены
по явной карте стадии 4 принятого плана: у этих функций не было
закрепляющих inline-атрибутов. Все существующие `#[inline(always)]`
помощники остаются в ядре; три `#[inline(never)]` возобновления
перенесены со своими атрибутами. Новых inline-решений не принималось.

`SchedulerConfig` и `ExecutionWaker` реэкспортированы по прежним путям.
У `Task` необходимые ядру поля доступны через `pub(super)`, у `AsyncState`
открыты только token/tasks/ready/promises/host_waker/sync_wait; каналы,
host-обещания и настройка кванта остаются закрытыми. `SyncWait` сохраняет
приватные поля, `HostCompletion`, `VmHttpSink` и `HostPromise` приватны.

Четыре блока переноса совпадают с исходными после учёта видимости и
переноса строк сигнатуры `consume_scheduler_safe_point` rustfmt.
Отдельное сравнение подтвердило неизменность тел `poll_linked`,
`step`, `step_cold`, `drive*`, регистровых и кэшовых помощников.
Существующие тесты не редактировались и не перемещались.

Целевые прогоны исполнили 254 теста VM, 4 scheduler, 9 http_async,
22 configuration. Полные последовательные ворота `step-4.3-*.log`
прошли: fmt, Clippy, build, workspace test (1737 passed, 0 failed,
1 ignored), строгий rustdoc и отдельный JIT-корпус. Стадия 4 отмечена
в таблице принятого плана VM.

## 4.4. Каталог модулей: перенос

В приватный `modules.rs` перенесены `ModuleState`, `ModuleInitState`,
`ModuleInstance`, `SessionModules`, `CatalogContext`, `ModulesCtx`,
`ROOT_MODULE` и `ensure_module_ready`. Публичные типы и константа
реэкспортированы из корня; внутренние поля доступны существующим
потребителям по разрешению владельца. Владение сеансом не менялось.

Оба перенесённых блока текстуально совпадают после учёта видимости;
остаток `lib.rs` совпадает после удаления блоков и добавления импортов.
Тесты не редактировались. Целевые прогоны исполнили 254 теста VM
и 22 configuration, включая изоляцию экземпляров и импортные вызовы.
Полные последовательные ворота `step-4.4-move-*.log` прошли: fmt,
Clippy, build, workspace test (1737 passed, 0 failed, 1 ignored),
строгий rustdoc и отдельный JIT-корпус.

Это первый коммит задачи 4.4: `catalog_caches` пока не затронут.
Задача не отмечена выполненной до отдельного изменения аксессора.

## 4.4. Каталог модулей: единый аксессор

Отдельным diff добавлен `CatalogContext::execution_parts`: программа,
линковка и кэши выбираются одним номером модуля в прежнем порядке
проверок. В `poll_linked` прежняя сборка тройки заменена одним вызовом;
ветка корневого модуля, `step`/`step_cold`, создание кэшей и политика
прогрева не менялись. `catalog_caches` остаётся во владении
`ProgramExecution`, отдельно от изменяемого сеанса и `ModuleInstance`.
Устаревший комментарий о будущем слиянии заменён описанием этого решения.

Два новых теста у владельца проверяют идентичность ссылок всех трёх
таблиц по одному ключу, независимое изменяемое заимствование сеанса и
прежние тексты/приоритет ошибок отсутствующей программы, линковки и
кэшей. Использован существующий тестовый компилятор `compile_module`;
его тело не менялось, добавлена только видимость внутри тестов крейта.
Инвентарь VM: 254 прежних проверки сохранены, добавлены ровно эти две
(256 всего). Целевые VM/configuration и полные последовательные ворота
`step-4.4-accessor-*.log` прошли: fmt, Clippy, build, workspace test
(1739 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный JIT-корпус.
Задача 4.4 завершена, стадия 5 отмечена в принятом плане VM.

## 4.5. Поддержка отладчика

В приватный `debug.rs` перенесены `DebugAction`, `DebugPosition`,
`DebugValues`, `FrameValues` и `DebugHook` с реализациями. Публичные
интерфейсы реэкспортированы из корня; пять полей `FrameValues` доступны
существующему драйверу через `pub(super)` по решению владельца.
Вычисление выражений продолжает вызывать единственный `run_dynamic_snippet`.
Перенесённый блок совпадает после учёта видимости; остаток `lib.rs`
изменился только в импортах, включая неизменные `step`/`step_cold`.
Тесты не редактировались.

Явно прошли 256 VM-тестов (в том числе оба `debug_hook`),
16 DAP handshake и 13 compiler debug-info. Полные последовательные
ворота `step-4.5-*.log` прошли: fmt, Clippy, build, workspace test
(1739 passed, 0 failed, 1 ignored), строгий rustdoc, отдельный JIT-корпус.
Стадия 6 отмечена в принятом плане VM.
