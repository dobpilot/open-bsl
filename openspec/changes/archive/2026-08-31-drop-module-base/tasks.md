# Задачи

- [x] 1.1 Проверено исполнением, а не чтением: временное утверждение
  `module_base == 0` в единственной читающей функции не сработало ни
  разу — ни в тестах рабочего пространства, ни на 57 фикстурах.
- [x] 1.2 Удалить поле и перевести единственную читающую функцию.
- [x] 1.3 Снять комментарии, объясняющие пересечение модульных слотов
  через `module_base`: связь мнимая.
- [x] 1.4 `cargo fmt`, `clippy -D warnings`, rustdoc, полный workspace.
- [x] 1.5 `emit -> run-bytecode` на 48 фикстурах: расхождений нет.
  Поле в текстовый формат не писалось никогда.
- [x] 1.6 `./benchmarks/hot-code-diff.sh`: горячий путь не изменился.
