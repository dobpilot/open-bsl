# Задачи

- [x] Устранить обход финальной проверки отмены при отказе файлового host. Все ветви perform_file_operation сохраняют Result до проверки; 16 сочетаний подтверждают приоритет отмены и прежние I/O-ошибки без неё. Вложенные проверки и отсутствие отката сохранены; нативные вопросы частичных отказов этим не закрыты. Отчёт — docs/research/compatibility/file-operation-failures-2026-09-17.md.

Приёмка этапа очереди ошибок 2026-09-17: после устранения регрессии
завершения при непустой FIFO прошли полный workspace, plain/optimize,
реестр измерений, API reference, Clippy, rustdoc, fmt и строгий OpenSpec
(11/11). Это не приёмка всего изменения; открытые пункты ниже сохраняются.

- [x] Измерить 12 вариантов обработчика ошибки BeginGettingSize: sync/async, procedure/function, изменение флага и исключение до/после Ждать. Native-сеанс fdkk28ws восстановил CF/БД/XML; 192 сочетания RU/EN, direct/dynamic, plain/optimize, source/bytecode сохраняют первую измеренную ошибку.
- [x] Измерить общий порядок ошибок async-процедур вне callback: вызывающий продолжается, предыдущее оповещение доставляется до ошибки, последующее — после. Native-сеанс p9o3pcz1 восстановлен; восемь сочетаний open-bsl совпадают с префиксом oracle до первой ошибки. Отдельные тесты проверяют FIFO, отмену, InvalidBytecode, замороженный host-вызов и освобождение значений очереди. Отчёт — docs/research/compatibility/async-error-queue-2026-09-17.md.

- [x] Измерить синхронную функцию/async-функцию/async-процедуру успеха и синхронную функцию ошибки: file-begin-handler-kinds, девять совпавших строк, conformance и восстановление CF/БД/XML.
- [x] Добавить явное наблюдение ошибок приложения в native-runner; проверить трёхпараметровую сигнатуру, неизменность признака завершения, прежний режим и восстановление. Все 22 offline-теста и успешные native-пробы прошли. Две попытки с неверной сигнатурой не приняты за oracle; обе восстановлены.
- [x] Измерить одиннадцать стандартных флагов: Ложь/0/"False" подавляют исходную ошибку, остальные восемь сохраняют её. Исправить Boolean-only проверку через общую конверсию; проверить 176 сочетаний и отдельное сохранение IoError при неприводимом флаге.
  - [x] Повторить все 11 значений с отличимой ошибкой доступа BeginGettingSize. Все восемь стандартных ошибок сохранили исходный отказ доступа, а не ошибку преобразования; native-сеанс j4qk2gte восстановлен. Второй oracle проверяется вместе с первым.
- [x] Измерить шесть видов исключений обработчика успеха до/после Ждать и отсутствие перенаправления в обработчик ошибки операции. Native-oracle и 48 сочетаний open-bsl совпали; CF/БД/XML восстановлены. NOTIFY.DELIVERY_EDGES остаётся для async-обработчика ошибки, его исключений и прочих флагов. Отчёт — docs/research/compatibility/file-begin-handler-kinds-2026-09-17.md.

- [x] Согласовать ЭтотОбъект/ThisObject как ссылку на текущий BSL-модуль с экспортными процедурами и функциями; добавить проверяемый контракт в delta до реализации.
- [x] Реализовать ссылку на экземпляр модуля и прямые вызовы экспортных методов; проверить тождество в функциях и динамических фрагментах, изоляцию экземпляров и отказ неэкспортному методу без вызова.
  - [x] Ввести создаваемое вместе с экземпляром модуля значение ЭтотОбъект/ThisObject и тип МодульBSL/BSLModule без конструктора. Проверены тождество при чтении из динамического фрагмента, функции/async/исключение, разные запуски и модули каталога, ключ соответствия, перекрытие переменными и фильтрация экспортов конструктором оповещения. Source/bytecode × plain/optimize проверены; последующие пункты закрыли прямые вызовы и доставку.
  - [x] Расширить открытый вызов сведениями о ссылочных аргументах и пропусках; проверить обычные методы, экспортные модульные вызовы, арность/default/Знач, эффекты, проверку образа и текстовый round-trip.
    - [x] Подготовить представление ResolvedArg/ArgMode, закрепление ссылочных локалей, эффекты и проверку образа; сохранить Instr размером 8 байт двумя тегами открытого вызова и поднять формат 30 → 31. Проверены ссылочные цели, пропуски, отказ неверному образу, оба тега, plain/optimize/round-trip и прежние значения/арности обычных объектных методов. Отчёт — docs/research/compatibility/open-call-arguments-2026-09-17.md.
    - [x] Подключить режимы к объявлению фактического экспортного модульного метода в VM: кадры, Знач/default/byref, async, каталог и динамические фрагменты; проверить изменения исходных переменных и отказ до входа в метод.
      - [x] Подключить прямой экспортный вызов к кадрам и StartAsync; проверить source/bytecode × plain/optimize, Знач и порядок вычисления, пропуск/Undefined, арность, экспорт, глубину, тождество каталога/entry, импортированные аргументы и отказ завершённому экземпляру. Пул с одним worker проверен для поиска/чтения/записи, прямого/динамического I/O и отмены через ЭтотОбъект.
      - [x] Измерить сохранение ссылочного аргумента после исключения на localhost/test; исправить общий путь разматывания, проверить локальный/модульный/импортированный аргумент. Нативная сессия y31kkxa4 восстановила CF/БД и сверила XML.
      - [x] Устранить измеренную задержку видимости модульного аргумента до возврата и раздельные слоты повторного аргумента обычного Call. Все шесть наблюдений module-reference-arguments совпали, включая 31 и 41 вместо прежних 1 и 1. Дополнительно измерены семь динамических наблюдений сервера localhost/test, включая запись перед исключением; сессия qwn6xnhb восстановила CF/БД и сверила XML. Добавлены оба oracle в conformance и проверки переключения задач. Отчёт — docs/research/compatibility/module-aliases-2026-09-17.md.
      - [x] Сохранить ссылочную цель Импорт.Переменная в обычном статическом Call. Красный тест подтвердил копию вместо ссылки; теперь проверены entry/catalog, lazy/eager, plain/optimize/source/bytecode, повторные/вложенные аргументы, Знач/default и запись при исключении. Общая подготовка ленивых владельцев используется тремя видами вызова. Исправлен найденный красным тестом поиск обработчика ошибки инициализации у предыдущей инструкции; проверены два отказа без входа в callee. Отдельный тест сохраняет прежний async-снимок. Отчёт — docs/research/compatibility/static-import-arguments-2026-09-17.md.
      - [x] Проверить динамические чужие модульные аргументы при успехе и исключении в модуле с импортными инструкциями. После перехода RunDynamic в общий execution сохранены links статических чанков, живые алиасы, lazy/eager-инициализация, вложенные фрагменты и callback в entry. Проверено source/bytecode × plain/optimize; непосредственные имена импортов в строке остаются отдельной задачей.
      - [x] Измерить сохранение локальных переменных и параметров после исключения Выполнить. Серверная 8.3.27.2342 localhost/test дала 11/21/31/31/31/31/41/31; семь строк расходятся с текущим open-bsl. Сессия 7myem56y восстановила CF/БД и сверила XML. Raw dynamic-local-exceptions.platform.txt сохранён побайтно, общий oracle не менялся.
      - [x] Устранить потерю записей локальных переменных и параметров после ошибки динамического фрагмента. Измеренный dynamic_local_exceptions добавлен в conformance: семь красных расхождений устранены, все восемь строк совпадают. Проверены прямые/вложенные записи, повторный аргумент и Знач, парковка host, ошибки дочерней задачи до/после возврата корня, source/bytecode × plain/optimize и отладочный evaluate. Отдельный красный тест исправил перезапись поздней модульной записи устаревшим снимком корня. Отчёт — docs/research/compatibility/dynamic-local-exceptions-2026-09-17.md.
      - [x] Проверить чужой модульный аргумент после пойманной и выходящей наружу ошибки динамики в получателе без импортных инструкций. CallImported/открытый вызов, source/bytecode × plain/optimize сохраняют 31 и 41. Это не закрывает выполнение фрагмента в модуле с импортными инструкциями из соседнего незавершённого пункта.
      - [x] Проверить освобождение ячеек модульных аргументов при повторных вызовах. Красный тест показал 25 слотов вместо 5 после двадцати Call; граница освобождения исправлена для Call и CallImported. VM проверяет собственную/импортную цель и открытый вызов, возврат/исключение, plain/optimize — 16 комбинаций без накопления слотов.
      - [x] Передать обычному динамическому вызову контекст чужих экземпляров и сохранить владельца возвращаемого BSL-обещания. Нативные 18 наблюдений dynamic-async-context совпадают: результаты и ошибки при повторном Ждать, без ожидания дочерней задачи самим фрагментом. Отчёт — docs/research/compatibility/dynamic-execution-2026-09-17.md.
        - [x] Отделить принадлежащие запуску связанные таблицы от заимствования реестра. Проверить, что таблицы переживают исходные Program/RuntimeRegistry, разделяют ячейку кэша без копирования и освобождают host-возможности с последним владельцем. Это внутреннее основание для живущего между poll динамического образа, не реализация общего планировщика.
        - [x] Исполнять обычный RunDynamic кадром исходного запуска с общими каталогом/состояниями/AsyncState и живыми локальными ссылками. DynamicImage удерживается кадрами, включая async-задачу после возврата фрагмента; Weak проверяет освобождение после завершения и Drop отменённого исполнения. Динамический нулевой чанк не меняет статус инициализации модуля. Прежний DAP step-over сохранён; отдельный блокирующий исполнитель оставлен только отладочному evaluate.
        - [x] Передать компилятору динамической строки точное окружение разрешённых импортов, не весь каталог. Отдельная таблица объявленных импортов сохраняется в формате 32; проверены псевдонимы, затенение, экспортность, неиспользованные импорты, смешанные статические/динамические связи, повторные запуски и eager-init загруженного каталога. Отчёт — docs/research/compatibility/dynamic-imports-2026-09-17.md. Прежний запрет прямого async CallImported сохранён; отладочное evaluate остаётся следующим отдельным пунктом.
        - [x] Выровнять отладочное evaluate с текущим исполнением в части каталога и BSL-обещаний, сохранив контракт остановленного кадра. Выбранная программа/линковка, ссылочные локали и строки, ЭтотОбъект, импорты, записи при исключении и исходный владелец обещаний проверены из исходника и байткода. Задержанный файловый host, соседняя остановленная задача и позднее пробуждение покрыты отдельно. Исправлены пустая таблица локальных и строка при ленивой инициализации. Полный workspace, Clippy, rustdoc, fmt, строгий OpenSpec и сравнение машинного кода step/step_cold прошли. Отчёт — docs/research/compatibility/debug-evaluate-context-2026-09-17.md.
        - [x] Согласовать запрет Ждать внутри динамического текста с четырьмя наблюдениями dynamic-async-await. Delta добавлена до кода; обе ветви compile_dynamic_snippet используют синхронный резолвер, чанк не наследует is_async. RU/EN × Execute/Eval × sync/async caller × registry/без registry проверены; исходное обещание остаётся пригодным для внешнего Ждать. Все четыре строки oracle и прежние 18 наблюдений возвращаемых обещаний совпадают. Полный workspace, Clippy и rustdoc прошли. Отчёт — docs/research/compatibility/dynamic-await-rejection-2026-09-17.md.
      - [x] Устранить коллизию кэша динамических фрагментов из функций разных модулей каталога. DynamicScope.module отделяет владельца от номера фрагмента и сохраняется при вложенности. CLI-проба вместо 22/44 вернула 22/33. Проверены тела/функции/вложенные фрагменты, оба порядка, повторные запуски, source/bytecode × plain/optimize; unit-тест доказывает разделение ключей и повторное использование Rc в одной области. Отчёт — docs/research/compatibility/dynamic-module-scope-2026-09-17.md. Динамические импорты/каталог и BSL-обещания этим не закрыты.
  - [x] Проверить отсутствие цикла владения самоссылкой через Weak на кэш ModuleState. После этапа значения ссылки прошли fmt, Clippy, полный workspace, rustdoc, API reference, реестр измерений, plain/optimize/round-trip и строгий OpenSpec (11 items). Отчёт — docs/research/compatibility/module-reference-2026-09-17.md; прямые вызовы/Begin остаются отдельными незавершёнными этапами.
- [x] Подключить модульного получателя к ОписаниеОповещения и доставке Begin в VM; проверить состояние модуля, аргументы по ссылке, флаг ошибки, отмену и работу соседнего задания при одном worker. Этап 2026-09-17: обычная задача Await/CallObjectProcedure, каталог и динамические вызовы; single-worker matrix с Begin проверяет сообщения callback, успех/отмену и отсутствие отката I/O. Это не закрывает ошибки самих обработчиков и небулевой флаг.

- [x] Отдельно измерить 56 клиентских RU/EN-вызовов НайтиФайлыАсинх с 28 значениями рекурсии; подтвердить ошибку при Ждать и восстановление CF/XML. Сравнить все результаты в четырёх режимах CLI, проверить прямые/динамические вызовы без начала host-обхода при Да/Нет. Отчёт: docs/research/compatibility/file-search-recursion-arguments-2026-09-16.md.

## Асинхронные варианты

- [x] Реализовать измеренные 13 Begin-методов, проверку описания до мутации и default-отказ старого host; сохранить тождество инициализации. Проверить 208 сочетаний имён/входов/оптимизации/байткода, ссылочный флаг, исходную ошибку и вложенную регистрацию без дубликатов имён. Повторный нативный file-begin-delivery дал девять совпавших строк, CF/БД/XML восстановлены. Отчёт: docs/research/compatibility/file-begin-delivery-2026-09-17.md.
- [x] Завершить `NOTIFY.DELIVERY_EDGES`: дополнительная клиентская матрица измерила дату, тип, перечисление, соответствие, Файл, двоичные данные, буфер, UUID, описание типов, описание оповещения и BSL-модуль; все сохраняют исходную ошибку операции. Вместе с прежними 11 значениями закрыты категории конверсии стандартного флага. Sync/async-функции и процедуры успеха/ошибки, исключения до/после Ждать и арности обработчиков уже покрыты отдельными oracle. Полнота методов конкретных объектных получателей остаётся отдельным пунктом, не DELIVERY_EDGES.

- [x] Измерить описание BeginInitialization: девять отказов неверному типу до мутации, две принятые пустые формы RU/EN и успешный контроль отложенной доставки в соответствие с исходным объектом. Сессия v48sqmyg восстановила CF/БД и сверила XML; raw сохранён побайтно. Добавлена delta только для измеренной инициализации. Begin ещё не реализован; ошибки операции с пустым описанием и ошибки самих обработчиков остаются открытыми. Отчёт — docs/research/compatibility/file-begin-description-client-2026-09-17.md.

- [x] Измерить арности всех 13 Begin-методов и разделение ошибки вызова/операции: file-begin-arity, 98 RU/EN-вызовов, 62 ошибки арности без callback, 36 доставок после возврата. Четыре setter отвергают массив через callback, инициализация принимает. Сохранены raw, CF/XML восстановлены; тест проверяет полноту свидетельства без заявления о реализации Begin в VM.

- [x] Отдельно измерить 224 клиентских случая аргументов атрибутов на localhost/test 2026-09-16. Да/Нет и остальные неверные значения отвергаются при Ждать без изменения файла; успех даёт Undefined. Общий подготовитель четырёх входов исправлен по двум oracle. Проверены source/optimize/оба bytecode, прямые и динамические вызовы, повторное ожидание отказа и отсутствие вызова host-setter. CF/XML восстановлены; точные ошибки и порядок отказа доступа остаются открытыми. Отчёт: docs/research/compatibility/file-attribute-arguments-2026-09-16.md.

- [x] Измерить реальную доставку в обычные объекты: шесть клиентских операций localhost/test 2026-09-16, Add/Set/Insert массива, Insert соответствия и две последовательные инициализации одного Файл. Сохранены raw oracle и восстановление CF/XML. Результаты и дополнительные данные сохраняют ссылки; окончательный путь виден в обоих результатах инициализации. Табличная клиентская проба не скомпилировалась и исключена, её восстановление также подтверждено. Это измерение, не реализация Begin; FIFO и ошибки обработчиков не установлены. Отчёт: docs/research/compatibility/file-callback-object-delivery-client-2026-09-16.md.

- [x] Сверить 126 async-проб поиска пробельных имён с серверным sync-контрактом; сохранить исходный oracle и восстановление CF/XML. Общий алгоритм проверен на реальном дереве в plain/optimize/bytecode; create.spaces возвращён в обе фикстуры создания (теперь 26 sync и 24 async). Это не закрывает прочие границы обхода и сортировки.

- [x] Измерить 25 граничных путей СоздатьКаталогАсинх на клиенте серверной localhost/test 2026-09-16: подтверждены общий с sync алгоритм создания, исходная строка в результате и стадии ошибок. Сохранён raw oracle, CF/БД восстановлены и XML сверены. 23 случая перенесены в conformance; отдельно оставлены расхождение поиска пробельных имён и порядок «…». Host-тесты проверяют нормализованный путь, исходный результат, отказ без записи и отмену после получения разделителя. См. docs/research/compatibility/file-create-paths-server-2026-09-16.md.

- [x] Реализовать объектные формы ОписаниеОповещения/NotifyDescription 0/2/3/5 и пять readonly-свойств: общий 64-строчный сценарий notification-object-constructor совпал с нативным oracle; дополнительные host-тесты проверяют динамические имена, отсутствие исполнения/проверки арности и сохранение ссылок. Базовая принадлежность проверяется по 1 027 измеренным ячейкам без второй таблицы имён. NOTIFY.EDGE_ARGUMENTS оформлен с реестром и measure-строкой. Это не закрывает BSL-модули, доставку и отсутствующие обработчики коллекций.
- [x] Завершить применимость и исполнение объектных обработчиков: нормализованные маски всех 18 получателей совпадают с нативными 79 именами; отказ отдельному индексу таблицы сохранён как отказ типу. Отсутствовавшие методы массива, таблицы, списка, коллекции индексов и фиксированных коллекций проверены отдельными oracle; выходной `Свойство` дополнительно покрыт локальной, модульной, импортированной и индексной целью во всех режимах.
  - [x] Измерить и реализовать дополнительные типы имени обработчика: 46 строк клиента localhost/test подтвердили общую `Строка` для объектных и примитивных классов. Все пять членов TextEncoding в роли ошибочного имени подменяют имя успешного, проверяют ошибочного получателя по подменённому имени и делают оба модульных getter нечитаемыми; три чужих enum-типа отвергаются. Все строки совпадают в source/bytecode × plain/optimize; unit-тест проверяет внутренние ссылки и отказ чужому получателю. CF/БД/XML восстановлены; `NOTIFY.EDGE_ARGUMENTS` сужен до opaque-получателей и точных диагностик.
  - [x] Измерить семь отсутствующих пар методов СписокЗначений и полные арности Add/Insert: 60 результатов на клиенте localhost/test, приведение индексов, тождество, отсутствие мутации при ошибках, чужие элементы и независимая Copy. CF/БД восстановлены, XML совпал. Oracle: notification-value-list-methods-client.*.
  - [x] Реализовать измеренные методы СписокЗначений через его компонентную таблицу: семь пар имён, Add 0…4, Insert 1…5, поля Пометка/Картинка, преобразование индексов, чужие элементы и независимая Copy. Красный/зелёный тест проверяет прямые и параметрические вызовы plain/optimize, раннюю применимость ОписаниеОповещения и реальную Begin-доставку source/bytecode × plain/optimize. API reference регенерирован. Пустая Картинка пока представлена Неопределено до отдельной модели типа Картинка.
  - [x] Измерить коллекцию индексов ТаблицыЗначений и отказ отдельному индексу: 36 результатов клиента localhost/test, арности/вид вызова, тождество, ошибки колонок, копирование и ранняя проверка получателя. Итоговый сеанс восстановил CF/БД и подтвердил XML; oracle — notification-table-index-methods-client.*.
  - [x] Реализовать измеренную коллекцию индексов без неподтверждённого ускоряющего индекса строк. Прямые вызовы, тождество, независимость копии и оба класса получателей проверены source/bytecode × plain/optimize. API reference регенерирован.
  - [x] Измерить конструкторы, мелкий снимок, readonly-поверхность и арности фиксированных структуры/соответствия: 21 результат клиента localhost/test, CF/БД/XML восстановлены. Oracle — notification-fixed-collections-client.*.
  - [x] Реализовать фиксированные коллекции как мелкие снимки; проверить конструкторы, методы, неизменяемость и матрицу получателей source/bytecode × plain/optimize. Нормализованные маски всех 18 типов совпали с платформой.
  - [x] Измерить и реализовать выходной второй параметр `Структура/ФиксированнаяСтруктура.Свойство`: 12 строк сервера localhost/test подтвердили найденное/отсутствующее поле, RU/EN, допустимый литерал и индекс массива; CF/БД/XML восстановлены. Локальные, модульные, импортированные и индексные цели проверены source/bytecode × plain/optimize; формат 34 сохраняет `byindex` и отвергает формат 33.
  - [x] Измерить и реализовать Get таблицы/колонок/строки и Set строки: 155 RU/EN-проб сервера localhost/test 2026-09-16, сохранённый oracle и восстановление CF/XML. Все строки совпали; отдельно сохранён отказ -0.9 до усечения, в отличие от массива. Проверены отказ без записи, прямые/параметрические вызовы с оптимизацией, существующее приведение типа и invalidation строки; TABLE.METHOD_INDEX оформлен маркером, реестром и измерительной строкой. Отчёт: docs/research/compatibility/notification-table-access-server-2026-09-16.md. Доставка Begin и полная матрица исполнения этим не закрыты.
    - [x] Дополнить безопасные строковые границы Get таблицы и Set строки: 36 серверных результатов подтвердили `0.9`, `-0.0`, знак, ведущий ноль и отказ запятой, отрицательной дроби, экспоненте и пустой строке без мутации. Oracle совпадает в source/bytecode × plain/optimize; CF/БД/XML восстановлены. Предельные индексы и точные тексты ошибок сохраняют TABLE.METHOD_INDEX открытым. Отчёт — docs/research/compatibility/notification-table-index-edges-server-2026-09-17.md.
  - [x] Измерить и реализовать Вставить/Insert массива: 48 RU/EN-проб сервера localhost/test 2026-09-16, сохранённый oracle, восстановленные CF/XML. Поддержаны расширение с Undefined, default значения, измеренные индексы и отказ в выражении до мутации. Проверены alias/ссылочное значение, отказ выделения без изменения и прямой/параметрический вызовы с оптимизацией. ARRAY.METHOD_INDEX остаётся открытым; доставка Begin и прочие получатели этим не закрыты. Отчёт: docs/research/compatibility/notification-array-insert-server-2026-09-16.md.

- [x] Дополнительно измерить СравнениеЗначений, КлючИЗначение и ЗаписьТекста по 79 именам: 237 сочетаний, четыре принятых метода записи/закрытия, 233 отсутствующих, без ошибок создания или отказов типу получателя. Сохранены notification-extra-receivers.*; исходная конфигурация восстановлена, XML сверена. Это только клиентская проверка доступности, не исполнение методов и не реализация callback.

- [x] Измерить 42 случая Получить/Get и Установить/Set массива: девять индексов в RU/EN, ошибки арности и setter в выражении. Raw notification-array-access.platform.txt сохранён, CF/XML восстановлены и сверены. Обнаружены преобразования 1.9/"1"/Истина и необходимость отказа setter до изменения массива; простая переадресация к текущему индексатору не подходит.
- [x] Реализовать измеренный доступ массива с подготовкой индекса отдельно от []: все 42 строки oracle совпали, добавлена conformance-фикстура. Setter даже для статически известного массива компилируется в существующий открытый путь с result_required, поэтому выражение отвергается до записи без изменения формата. Проверены прямые RU/EN-вызовы, публичный runtime-вход, неизменность [] и отказ до мутации. Прочие преобразования оформлены ARRAY.METHOD_INDEX с реестром и одной measure-строкой. Полные workspace, plain/optimize/round-trip, fmt, Clippy, rustdoc, API reference и OpenSpec прошли. Это зависимость корректного объектного callback, не реализация всех коллекционных методов.
  - [x] Дополнить безопасные границы Get/Set: 48 серверных результатов подтвердили усечение -0.9/0.9, пробелы, `+1`, ведущий ноль, дробь с точкой и отказ запятой, экспоненте и пустой строке без мутации. Oracle совпадает в source/bytecode × plain/optimize; CF/БД/XML восстановлены. Предел расширения Insert и точные тексты ошибок сохраняют ARRAY.METHOD_INDEX открытым. Отчёт — docs/research/compatibility/notification-array-index-edges-server-2026-09-17.md.

- [x] Снять полную матрицу текущих BUILTIN_METHOD_NAMES: 18 типов × 79 имён, 181 принятый метод, 1083 отсутствующих, 158 отказов самому типу получателя (индекс таблицы и UUID). Сохранены notification-core-methods-client.*; тест проверяет полноту имён и различает ошибки получателя/поиска. Отдельная серверная проверка показала неизвестный тип ОписаниеОповещения и остановила матрицу; её отрицательный вывод сохранён. Во всех сессиях CF/XML восстановлены и сверены. Это измерение наличия, не реализация и не проверка исполнения методов.

- [x] Выделить чистый find_method_from_table для компонентных дескрипторов и использовать его в строковом вызове runtime и промахе кэша VM. Тесты подтверждают отсутствие вызова обработчика, сохранение исходного дескриптора, RU/EN, регистр/Unicode, отсутствие обрезания пробелов и независимость от арности. Проверен полиморфный кэш; API reference регенерирован; полный workspace, fmt, Clippy, rustdoc и строгий OpenSpec прошли. Методы базовых значений и экспорты BSL-модуля этим пунктом не закрыты.

- [x] Измерить 13 объектов-получателей в двух ролях: встроенные методы массива, структуры, соответствия и Файл принимаются; свойства и примитивы отвергаются. Сохранены notification-receivers.*; свежие CF/XML восстановлены и сверены. Архитектура описания не должна ограничиваться специальным объектом BSL-модуля.
- [x] Обеспечить проверку наличия метода конкретного объекта без пробного исполнения и без дублирующей таблицы: компонентные дескрипторы используют ObjectProtocol, базовые методы — единую BuiltinMethod-таблицу, экспорты BSL-модуля — его проверенную ссылку; доставка использует те же пути вызова.
- [x] Добавить ObjectProtocol::has_method и делегирование ObjectRef: умолчание использует общий поиск статических дескрипторов, динамический host явно объявляет имена. Точечные тесты проверяют RU/EN, регистр, пробелы, отсутствие вызова обработчиков и совместимость прежнего строкового host без переопределения. Это канал проверки host-объектов, не реализация базовых методов, ссылки на BSL-модуль или самого ОписаниеОповещения.

- [x] Измерить раннюю проверку обработчиков: 14 вариантов в основной и ошибочной роли, включая экспортность, арности 0–3/необязательный параметр, функцию/async, регистр и пробелы; дополнительно четыре имени ссылки на клиентскую форму. Raw notification-handlers.platform.txt сохранён, CF/XML восстановлены и сверены. Добавлена delta, запрещающая подменять проверку экспорта проверкой будущей доставки; сам механизм доставки остаётся незавершённым.

- [x] Реализовать Arity::one_of для точного набора без принятия пропусков; сохранить exact/range и границы min/max. Проверены все значения u8, неверные наборы, методная проверка, sema для функций/конструкторов и InvalidBytecode при связывании заранее скомпилированных вызовов. Генератор справочника показывает полный набор во всех трёх таблицах; API reference регенерирован. Явно учтён рост статического MethodDescriptor 40→64 байта. Полные workspace, fmt, Clippy, rustdoc и OpenSpec прошли; это не регистрация ОписаниеОповещения и не измерение скорости.

- [x] Измерить устройство ОписаниеОповещения: 19 форм/ошибок конструктора, 20 чтений/записей RU/EN-свойств и тождество; сохранить 42 исходные строки notification-description.platform.txt. Подтверждены имя NotifyDescription, набор арностей 0/2/3/5, ранний поиск экспортного обработчика, readonly-свойства и ссылочные данные. Восстановление CF/XML проверено.
- [x] Перед регистрацией ОписаниеОповещения спроектировать точное представление набора арностей 0/2/3/5 и проверяемую ссылку на модуль; не разрешать 1/4 диапазоном 0–5 и не откладывать поиск отсутствующего экспортного обработчика до callback. Sema/runtime/API reference и измеренные классы проверены; неизмеренные классы остаются отдельным NOTIFY.DELIVERY_EDGES.

- [x] Измерить 13 немобильных Begin-методов Файл в RU/EN (26 вызовов): чтение совпало с синхронным, setters вызывают одноаргументный обработчик, инициализация сохраняет тождество. Сохранены file-callback-methods.* и восстановление CF/XML проверено. Linux-невидимость остаётся Ложь после успешной установки Истина; это не ошибка измерителя. Добавлены delta для проверенной формы доставки, без объявления полного callback-контракта завершённым.

- [x] Измерить первые восемь Begin-вызовов на клиенте localhost/test: семь результатов с двумя аргументами, ошибка с тремя аргументами и изменяемым булевым флагом, немедленное изменение пути инициализации. Сохранены file-callbacks.bsl и оригинальные 28 строк; CF/XML восстановлены и сверены. Исправлена клиентская область модульных переменных генератора с красным/зелёным тестом. Это измерение, не реализация Begin-интерфейса.
- [x] Завершить перечисленный callback-контракт: прямые RU/EN-вызовы всех 13 немобильных Begin-методов, точные арности и разделение call/operation errors подтверждены двумя native-матрицами; успехи и ошибки sync/async-обработчиков, изменяемый стандартный флаг, FIFO и первая ошибка закреплены отдельными oracle. Тождество инициализации, пустое/объектное ОписаниеОповещения и ссылка на текущий BSL-модуль проверены в CLI/embedding, direct/dynamic, source/bytecode и plain/optimize. `NOTIFY.DELIVERY_EDGES` и полнота методов отдельных объектных получателей остаются самостоятельными пунктами и этим закрытием не снимаются.
- [x] Реализовать ОписаниеОповещения и измеренные Begin-операции с доставкой BSL-обработчиков в исполнении VM, дополнительными данными и изменяемым флагом ошибки; проверить конформанс, динамические вызовы, отмену и отсутствие блокировки worker. Оставшийся неизмеренный callback-контракт перечислен отдельным пунктом выше.

- [x] Подтвердить исправленный поиск ссылок отдельно на клиентской 1С: 132 вызова RU/EN через Вычислить, обещание, Ждать, порядок и прерывания совпали с open-bsl и прежними 66 серверными синхронными случаями. Исходный вывод file-search-links-async сохранён; CF/XML восстановлены и сверены. Это не меняет политику удаления ссылок и не закрывает прочие FIND.TRAVERSAL.

- [x] Проверить границы объединения форм динамического фрагмента: отсутствие собственной формы не компенсируется статической таблицей, итоговый индекс 65535 допустим, 65536 отвергается до исполнения. Тест dynamic_structure_indices_are_checked_before_and_after_relocation и полный workspace прошли; формат образа и API не менялись.

- [x] Измерить RU/EN-маски DeleteFilesAsync на клиенте серверной базы: 18 случаев с фактическим состоянием дерева, обещанием и результатом Неопределено. Восстановление CF/XML проверено; исходный вывод file-delete-async-types.platform.txt сохранён.
- [x] Подключить измеренное преобразование асинхронной маски к общему форматировщику; зарегистрировать существующий тип Обещание без конструктора. Все 18 строк oracle совпали без ослабления проверок; API reference обновлена.
- [x] Исправить обнаруженную измерителем потерю статических форм при сборке динамического Program: сохранить исходные формы и проверяемо перенумеровать формы фрагмента. Воспроизводящий тест был красным и стал зелёным; статическая, динамическая и вложенная структуры проверены. Полный workspace, plain/optimize/round-trip, fmt, Clippy, rustdoc и строгий OpenSpec прошли.

- [x] Добавить необязательный FileSystem::background_access без требования Send/Sync к существующим host; проверить default None без I/O и общие данные SystemFileSystem при доступе из другого потока. Это только host-граница, не реализация обещаний.
- [x] Измерить клиентские обещания поиска и чтения метаданных: типы результатов, ошибку третьего аргумента при Ждать, возвращаемый Файл ИнициализироватьАсинх; сохранить raw oracle и сверить восстановление базы.

- [x] Проверить серверную доступность НайтиФайлыАсинх: на 8.3.27.2342 RU/EN распознаются, но вызов отказывает с причиной «только на клиенте»; исходник и оригинальный вывод сохранены, конфигурация восстановлена и сверена.
- [x] Проверить клиентский контракт НайтиФайлыАсинх и реализовать обещание массива Файл: RU/EN, общий фоновый обход, ошибки при Ждать, host-bound объекты, повторное ожидание и отмена при Drop Execution. Это клиентский контракт CLI/embedding, не серверная доступность 1С.
- [x] Проверить поиск в реальном пуле с одним worker: короткое задание завершается до освобождения файлового I/O, completion пробуждает поиск; отмена завершает задание до возврата I/O и не читает элементы после него. Тест single_worker_pool_runs_other_jobs_and_wakes_or_cancels_file_waiters использует явный host-профиль и управляемые каналы, без предположения о скорости диска.
- [x] После измерения и документирования клиентского контекста добавить `УдалитьФайлыАсинх`, `СоздатьКаталогАсинх`, `КаталогВременныхФайловАсинх` с разными результатами обещаний.
  - [x] Измерить десять дополнительных объектных масок DeleteFilesAsync: девять прямых удалений и контрольная дата подтвердили общий форматировщик, сохранение корня и Undefined после Ждать. Source/bytecode × plain/optimize совпадают с клиентским oracle.
- [x] Измерить и реализовать прямые вызовы трёх глобальных методов: исходная строка создания, Undefined удаления, строка temp-каталога; общие алгоритмы с синхронным API и доставка ошибок через обещание. Это не закрывает динамический путь следующего пункта.
  - [x] Измерить безопасные корневые формы СоздатьКаталогАсинх и объект Файл: десять обещаний возвращают исходное значение, включая ссылочно тот же Файл. Проба доказала отсутствие создания пути внутри Файл, но не проверяла каталог с представлением самого объекта; последующее измерение типов исправило этот вывод. Готовый строковый no-op остаётся в VM-потоке через чистый публичный подготовитель bsl-rt; sync/async oracle совпадают в четырёх режимах, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-create-directory-roots-server-2026-09-17.md.
  - [x] Реализовать измеренные типы СоздатьКаталогАсинх: изолированная клиентская проба подтвердила `Строка(значение)` и фактическое создание для 17 непустых представлений, ошибки трёх пустых представлений и возврат исходного значения с тем же тождеством. Worker получает только строку, исходное значение хранится mapper-ом в VM; готовыми остаются только строковые no-op. Красный conformance теперь проходит в source/bytecode × plain/optimize, старый тест переведён в приватный cwd; unit-тест проверяет общий форматировщик. Последующая серверная проба подтвердила тот же форматирующий контракт для sync. Raw oracle сохранён; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-create-directory-types-client-2026-09-18.md.
  - [x] Расширить тот же готовый путь на двенадцать измеренных составных форм `.`, `..` и разделителей. Все исходные строки сохранены, десять прежних sync/Async-отказов устранены; произвольные неизмеренные формы не обобщены.
- [x] Исправить владение файловыми обещаниями из Вычислить/Выполнить: регистрация у внешнего AsyncState через заимствованный HostIo. Исходный file-async-globals.bsl больше не даёт чужой ExecutionToken; проверены вложенный Вычислить, copy-back, host, повторное ожидание, отмена и пробуждение в пуле с одним worker. Полный workspace, Clippy, rustdoc, fmt, OpenSpec и конформанс plain/optimize/round-trip прошли.
- [x] Добавить `ЗапуститьПриложениеАсинх` и `ФайловыеПотоки.СоздатьВременныйФайлАсинх` в согласованном контексте; тесты исключений и неблокирующего ожидания. RunAppAsync проверен отдельно; CreateTempFileAsync использует переносимый host-результат без синхронного fallback. 70 RU/EN-случаев проходят в plain/optimize и source/bytecode; задержанное создание уступает соседней async-задаче. Политика позднего результата при закрытии State остаётся отдельным открытым пунктом temporary-file-lifecycle.
  - [x] Измерить 70 клиентских RU/EN-вызовов CreateTempFileAsync: 32 успеха, 36 ошибок при первом/повторном Ждать, два отказа арности без обещания. Тождество потока сохраняется после Close. Нативный сеанс vn9r62a1 восстановил CF/БД и сверил XML; oracle и тест полноты сохранены. Delta находится в extend-temporary-file-lifecycle; обычная передача открытого дескриптора реализована, а владение поздним ресурсом при закрытии State остаётся отдельным открытым пунктом.
  - [x] Измерить 35 клиентских случаев RunAppAsync на localhost/test 2026-09-16, сохранить raw и подтвердить восстановление CF/XML в обеих сессиях. Подключить RU/EN, defaults, обещание Undefined/кода, ошибки при Ждать, NUL и async-NotFound без изменения sync-127. 33 случая совпали в CLI plain/optimize/обоих bytecode; два расхождения cwd/shell сохранены явно. Host- и реальные процессные тесты проверяют внешнее владение динамическим обещанием, соседнюю задачу, один worker, отмену/shutdown без завершения приложения, reap и выход CLI без ожидания. См. docs/research/compatibility/file-runapp-async-client-2026-09-16.md. Windows, документы и оставшиеся RUNAPP-границы этим не закрыты.

- [x] Реализовать весь асинхронный интерфейс Файл по инвентаризации; проверены все 42 RU/EN-метода, арности, стадии ошибок и результаты обещаний. Успешный мобильный host не входит в стандартный runtime и сохранён как `FILE.MOBILE_LIBRARY`.
  - [x] Измерить четыре мобильных члена в тонком клиенте Linux: RU/EN распознаны, три нуль-арных формы отказывают при вызове, Async не создаёт обещание; Begin имеет точную арность 1 вопреки начальной инвентаризации и с одним аргументом отказывает по контексту. Оба native-сеанса восстановили CF/БД и подтвердили XML; успешный мобильный host остаётся `FILE.MOBILE_LIBRARY`.
  - [x] Опубликовать четыре мобильных члена в дескрипторе `Файл` для немобильного runtime: измеренные RU/EN, арности 0/0/0/1 и непосредственная ловимая ошибка контекста без host-I/O. Интеграционный тест различает проверку арности, вызов обработчика и отсутствие обещания у Async. API reference регенерирована. Это не реализация успешного мобильного host.
  - [x] Зафиксировать исчерпывающий инвариант всех 42 методов `Файл`: unit-тест сопоставляет каждую каноническую RU/EN-пару, точную арность и функцию/процедуру с сохранённой инвентаризацией и отказывает при лишнем или отсутствующем дескрипторе. Нативные результаты и обещания немобильных Async/Begin покрыты отдельными oracle-тестами; успешный мобильный host остаётся единственным пробелом этого пункта.
- [x] Измерить четыре setter и ИнициализироватьАсинх: 36 проб клиента localhost/test сохранены в file-async-write.platform.txt; конфигурация восстановлена и XML сверена. Зафиксирована немедленная мутация исходного Файл и псевдонима; тождество проверено отдельной пробой ниже.
- [x] Измерить тождество и повторное ожидание ИнициализироватьАсинх: оба обещания видят текущий путь общего объекта, независимый Файл того же пути не равен; Bool/Null/Date/Array используют Строка. Дополнительные девять клиентских типов подтвердили то же правило, немедленную мутацию и тождество результата. Оба raw oracle сохранены после восстановления и сверки XML; `FILE.INIT_ARGUMENTS` закрыт.
- [x] Реализовать измеренный контракт инициализации: общее состояние пути, стабильное ссылочное равенство/хэш, готовое обещание у внешнего владельца динамического вызова, сохранённые ФС/зона/разделитель без background_access и I/O. Проверены прямой/вложенный динамический вызов, псевдонимы, повторное ожидание, ключ соответствия, нулевая арность без мутации, отказ сокращённого контекста, отсутствие повторного чтения разделителя и снимок пути начатого metadata-запроса. Конформанс, полный workspace, Clippy, rustdoc, fmt, API и строгий OpenSpec прошли.
- [x] Реализовать четыре асинхронных setter атрибутов/времени через общий с синхронными методами подготовитель даты и host-операции. 28 conformance-строк совпали с нормализованным oracle; проверены RU/EN, динамические вызовы, повторное ожидание, ошибка Неопределено при Ждать, отказ без background_access, отмена до/после успешной и неуспешной записи без отката, неблокирующая запись и отмена в пуле с одним worker. API, полный workspace, Clippy, rustdoc, fmt, реестр измерений и строгий OpenSpec прошли.
- [x] Измерить 48 случаев чтения Файл на клиенте серверной localhost/test: восемь методов, RU/EN, файл/каталог/отсутствующий путь, сравнение с синхронными результатами и стадия ошибок. Сохранить file-async-read.platform.txt; восстановить CF и сверить XML.
- [x] Реализовать восемь асинхронных методов чтения Файл через общий преобразователь метаданных и исходные ФС/зону объекта. Проверены 48 conformance-случаев, задержанный host, RU/EN, прямой/динамический вызов, повторное ожидание, неизвестные атрибуты, отсутствие background_access, отмена до/после I/O и работа/отмена в реальном пуле с одним worker. Полный workspace (включая plain/optimize/round-trip), Clippy, rustdoc, fmt, API reference и строгий OpenSpec прошли. Setters и ИнициализироватьАсинх этим пунктом не закрыты.
- [x] Подключить файловую регистрацию компонентных методов через CallContext и HostPromiseSpawner с совместимым alias HttpPromiseSpawner. Прямой, динамический и вложенный динамический вызовы проверены тестом component_file_promises_survive_direct_and_dynamic_calls: отложенный I/O, исходный файловый host, повторное ожидание, точная ошибка подготовки при Ждать. Проверены прежняя HTTP-only реализация и сокращённый контекст; полный workspace, Clippy, rustdoc, fmt, API reference и строгий OpenSpec прошли. Это только канал регистрации, не реализация асинхронных членов Файл.

## Приёмка

- [x] Регенерировать API reference и проверить RU/EN-имена, арности, типы вызовов, REPL и неизвестные методы.
- [x] Проверить plain/optimize, текстовый round-trip и отказ старому формату, если он менялся.
- [x] Выполнить fmt, Clippy, workspace tests, rustdoc и проверку реестра измерений; ZIP/stream/Connector/job-регрессии включены в полный workspace-прогон 18.09.2026.
- [x] Провести review безопасности путей, возможностей запуска и владения временными ресурсами. Новых достижимых дефектов не найдено; SystemFileSystem остаётся несandboxed, launcher — явным и без shell, системное temp-владение недостижимо из BSL до решения same-UID. Отчёт — docs/research/compatibility/filesystem-security-review-2026-09-18.md.
- [ ] Проверить OpenSpec строго, перенести принятые delta в действующие specs и архивировать только после завершения согласованного объёма.
