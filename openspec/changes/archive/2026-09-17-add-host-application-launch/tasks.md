# Задачи

## Запуск

- [x] Согласовать разделение передачи документа и завершения приложения; недоступное ожидание явно отклоняется, выход gio не подменяет выход приложения.
- [x] Реализовать подтверждение передачи документа без ожидания на Linux: ApplicationResult различает Started/Exited/DocumentOpened, запрос содержит wait_for_exit. Изолированная ассоциация проверяет 32 сочетания RU/EN, sync/async, direct/dynamic, plain/optimize и source/bytecode, живой обработчик после CLI, ошибки ассоциации/отсутствия gio и отказ ожиданию до открытия с сохранением выходной переменной. Отдельные регрессии проверяют запуск executable и документа при выходе CLI с не ожидаемым обещанием; барьер spawn сохранён. Это не приёмка Windows или нового контекста 1С. Отчёт — docs/research/compatibility/document-association-implementation-2026-09-16.md.

- [x] Подключить ApplicationSync к парковке VM и блокирующему компонентному пути; шесть тестов проверяют отложенное/немедленное завершение, оба вида ошибок, поток mapper, wakeup, позднюю доставку и реальную VM с обработкой исключения. Автоматическая отмена приложения отсутствует в этом пути. BSL RunApp, его выходной аргумент, отмена задания и пул с одним worker проверяются отдельно.

- [x] Добавить явный from_unix_command_line без shell: кавычки, склейка частей, пустые аргументы, Unicode, backslash/LF, отказ NUL и незавершённым формам без раскрытия команды. Проверен реальный дочерний процесс с буквальными метасимволами в имени и аргументах. Windows-синтаксис, BSL RunApp, by-ref и парковка этим пунктом не реализованы.

- [x] Дополнить delta для выбранного host API и выходных аргументов до изменения compiler/VM. Присваивание RunApp выражено существующим Assign до SSA; формат байт-кода не менялся.
- [x] Добавить явную Rust host-возможность запуска: HostEnv по умолчанию без launcher, StateBuilder внедряет сервис; fake-host проверяет argv/cwd, скрытие аргументов в Debug и отложенное завершение после освобождения окружения без процесса ОС. BSL-отказ и CLI-подключение проверяются последующими пунктами.
- [x] Реализовать согласованный `ЗапуститьПриложение` без shell; argv, cwd, wait и выходной `КодВозврата` проверены fake-host, реальным Linux-процессом и BSL-вызовами ниже.
- [x] Подключить Unix RunApp/ЗапуститьПриложение: plain/optimize проверяют локальный/модульный/импортированный выход, параметр по ссылке, литерал, однократный порядок аргументов, пропуски, строгие отказы, ошибки завершения и немедленный динамический вызов. Оставшийся объём предыдущего пункта: платформенные границы RUNAPP.*, Windows-разбор и согласованные сценарии документов.
- [x] Реализовать системный Rust-адаптер executable: реальным дочерним процессом проверить точные argv/cwd, код 17, ошибку spawn и независимый reap после потери получателя на Linux. Это не проверка BSL-ожидания или закрытия runtime.
- [x] Проверить запуск по ассоциации отдельно от исполняемого файла; на этой Linux-машине собственная XDG-ассоциация отделена от executable, а отсутствие GUI/ассоциации и `gio` отвергается. Прочие desktop-среды и ОС не заявлены.
  - [x] Проверить Linux-gio с собственной XDG-ассоциацией, независимым тестовым обработчиком, отсутствием ассоциации и утилиты. Пользовательские ассоциации не меняются. Проверки прочих desktop-сред/порталов и ОС этим не выполнены.
- [x] Проверить отмену ожидания, закрытие runtime и уборку реального процесса без его автоматического завершения при одном worker. application_system проверяет direct/Выполнить глубиной 3 × complete/cancel/shutdown: соседнее задание успешно, ожидатель остановлен до ответа, ребёнок продолжает работу, возвращает 17 и отсутствует в /proc после wait/reap.
- [x] Сохранять динамический фрагмент между конечными poll. Проверены Выполнить глубиной до 5, вложенный Вычислить с вызовом функции модуля, локальные/ссылочные/модульные значения, ошибка и заморозка соседних задач. В пуле одного worker проверены direct/Выполнить глубиной 1 и 3, завершение/cancel/shutdown, поздний sink после Drop и отмена async-функции после возврата корневого тела.
- [x] Проверить BSL RunApp с отложенным fake-host: единственный worker исполняет соседнее задание, cancel/shutdown завершают ожидателя до доставки результата; поздняя доставка после закрытия State безопасна. Реальный дочерний процесс продолжает работу после выхода CLI без ожидания; совмещённая проверка с отменой задания также выполнена.
- [x] Измерить базовую доступность RunApp на сервере 8.3.27.2342: RU/EN, ожидание и его отсутствие, отсутствующий executable; сохранить исходник и оригинальный вывод, проверить восстановление конфигурации. Синхронный запуск доступен в измеренном контексте.
- [x] Измерить тип выходного параметра, пропуск/явное Неопределено/0/1/Null для ожидания, литерал в четвёртой позиции и безопасные shell-операторы; сохранить оригинальный вывод и внести независимые от shell требования выходного параметра в delta.
- [x] Измерить поле, индекс, необъявленное имя и порядок вычисления выходной цели. Дванадцать строк runapp-output-targets совпали в source/bytecode × plain/optimize; адрес вычисляется один раз после команды, код записывается после host-ответа. `RUNAPP.OUTPUT_TARGETS` снят; байткод не менялся.
- [x] Сопоставить серверные и клиентские сценарии с 1С, включая тип выходного параметра, ошибки и командную строку. Дополнительный клиентский sync-oracle отделил контекст от метода: true/false, no-wait, типовые ошибки, кавычки и shell совпали с сервером; отсутствующая программа дала Undefined вместо серверного 127, `exit 17` — Undefined вместо 17, несуществующий cwd — 0. CLI сохраняет согласованный server-like контракт без shell и не объявлен полным повторением ни одного контекста. Отчёт — docs/research/compatibility/file-runapp-context-comparison-2026-09-17.md.

- [x] Проверить запуск в стандартном CLI без дополнительного флага (исходник, текстовый байт-код и REPL) и отказ embedding без сервиса host. Для фоновых заданий embedding сервис задаётся явным host-профилем.

## Приёмка

- [x] После Linux-ассоциаций и исправления гонки не ожидаемого обещания повторить fmt, Clippy --all-targets, полный workspace с conformance/plain/optimize/round-trip/реестром, rustdoc, git diff --check и строгий OpenSpec: всё прошло, 11 OpenSpec items. API reference регенерирована. Логи — /tmp/open-bsl-documents-*.log. Это проверка реализованного этапа, не завершение оставшегося полного объёма ниже.

- [x] Регенерировать API reference и проверить RU/EN-имена, арности, типы вызовов, REPL и неизвестные методы. Генератор и checked-in тест прошли после финальных изменений.
- [x] Проверить plain/optimize и текстовый round-trip. Формат байт-кода не менялся: RunApp использует существующие открытый вызов и Assign, поэтому нового отказа старому формату нет; общий round-trip и оптимизационный корпус прошли.
- [x] Выполнить fmt, Clippy, workspace tests, rustdoc и проверку реестра измерений; не пропускать ZIP/stream/Connector/job-регрессии. Финальный прогон 2026-09-17 прошёл; workspace повторён вне sandbox только для разрешения loopback HTTP.
- [x] Провести review безопасности путей, возможностей запуска и владения временными ресурсами. Подтверждены явная host-возможность, отсутствие shell, раздельные argv, абсолютный аргумент документа для gio, скрытие argv из Debug/ошибок, отказ ожиданию документа до spawn и независимый reap. Рабочий каталог намеренно остаётся полномочием host; автоматического завершения процесса нет по контракту.
- [x] Проверить OpenSpec строго, перенести принятые delta в действующие specs и архивировать только после завершения согласованного объёма. Все 11 items прошли строго; перенос выполняется штатным archive после этого пункта.
