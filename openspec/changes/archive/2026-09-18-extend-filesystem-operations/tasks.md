# Задачи

- [x] Проверить выбранную host-политику частичного удаления: 128 BSL-сочетаний sync/async, RU/EN, direct/dynamic, plain/optimize, source/bytecode. Ошибка перечисления не начинает удаление; первый отказ remove_path прерывает дальнейшие вызовы, уже сделанные изменения не откатываются, повторное Ждать не повторяет I/O. Серверный oracle позднее подтвердил отсутствие отката. Отчёт — docs/research/compatibility/file-operation-failures-2026-09-17.md.
  - [x] Подтвердить частичный отказ на серверной 1С: при маске `*` оба обычных файла удаляются до ошибки на непустом каталоге с правами `000`, закрытый каталог сохраняется. Oracle совпал с open-bsl в source/bytecode × plain/optimize; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-delete-partial-failure-server-2026-09-18.md.

- [x] Проверить защитное удаление на реальной ФС и исправить обход no-follow через конечный / на Unix; сохранить NotADirectory для обычного файла с /, проверить цели ссылок и отсутствие root confinement. Шесть системных тестов и 96 BSL-комбинаций sync/async, RU/EN, dynamic, plain/optimize, bytecode. Промежуточный security review: docs/research/compatibility/filesystem-security-review-2026-09-16.md; финальный review всего плана остаётся открытым.

- [x] Измерить 56 RU/EN-значений флага рекурсии на сервере localhost/test и исправить отказ Да/Нет через общий файловый подготовитель. Сохранить raw и подтверждённое восстановление CF/XML; проверить plain/optimize и оба байт-кода. Все условно преобразуемые классы и общий отказ объектов покрыты. Отчёт: docs/research/compatibility/file-search-recursion-arguments-2026-09-16.md.
- [x] Измерить приоритет ошибки рекурсии и корневого `PermissionDenied`: 8 RU/EN sync/async-вызовов на localhost/test показали проверку третьего аргумента до I/O, пустой результат при допустимой рекурсии и async-ошибку на `Ждать`. Исправлен только поиск; удаление по маске сохраняет ошибку host. Source/bytecode × plain/optimize совпали с oracle; CF/БД/XML восстановлены. Отчёт: docs/research/compatibility/file-search-access-order-client-2026-09-18.md.

- [x] Измерить 126 случаев поиска пробельных имён и 140 лексических случаев Файл на 8.3.27.2342; сохранить oracle, внешний манифест и восстановление CF/XML. Исправить общий обход без изменения удаления и расширения правил СоздатьКаталог; проверить маски исходных имён, дубликаты, исходный тип, пропуск только отсутствующего нормализованного потомка и приоритет отмены. Отчёт: docs/research/compatibility/file-search-spaces-server-2026-09-16.md. FIND.TRAVERSAL и FILE.EDGE целиком остаются открытыми.

## Подготовка плана

- [x] Сопоставить текущие дескрипторы и host API с полными страницами Unica.
- [x] Найти существующие измерения, механизм масок ZIP и файловые потоки.
- [x] Выделить временные имена, временный каталог, очистку и временный поток.
- [x] Записать неоднозначность запуска на сервере и границы клиентских API.

## До реализации

- [x] Согласовать запуск без дополнительного флага в CLI и через сервис host в embedding.
- [x] Согласовать State/вызов, callback host и отсутствие очистки после уничтожения процесса.
- [x] Зафиксировать ОС, версию платформы, literal/effective compatibility и контекст каждой пробы. Отчёты файловой серии указывают Linux, 8.3.27.2342, сервер/тонкий клиент localhost/test, literal `Version8_3_27` и измеренный effective `НеИспользовать`; raw oracle сохраняют строки `context.*`. Непроверенные 8.5, Windows и файловая база явно не обобщаются.
- [x] Создать безопасные общие измерители по матрице `design.md`; проверить завершающие маркеры и scratch-границы. Native-runner требует begin/end, отвергает setup/cleanup_error, выдаёт серверу доступ только к приватному I/O и публикует результат после восстановления; сценарии создают отдельные temp-корни, а внешние снимки проверяют точный root.
  - [x] Добавить воспроизводимый native runner localhost/test без Unica: свежие CF/XML, повторная сверка до загрузки, восстановление после частичной загрузки/ошибки/перехваченного сигнала, проверка XML и публикация результата только после восстановления. 17 offline-тестов проверили двойник native, реальные ACL и остановку собственного потомка, игнорирующего SIGTERM. Описание — tests/conformance/measure/1c/README.md; лог — /tmp/open-bsl-server-runner-tests-native-acl.log.
  - [x] Подтвердить новый runner реальным round-trip на localhost/test: 2026-09-15 повторный file-globals совпал с прежним oracle после нормализации случайной части имён; затем выполнены две пробы срока жизни. Все три завершились восстановлением CF/БД и сверкой XML. Кластер запущен пользователем напрямую от kan; отчёт — docs/research/compatibility/file-temp-return-server-2026-09-15.md.
- [x] Сохранить платформенные результаты, сопоставить с open-bsl и уточнить delta по каждому новому правилу. Принятые сценарии имеют соседние `.platform.txt`, отчёт с SHA-256/восстановлением и oracle-derived тест либо явно зафиксированное отсутствие изменения runtime; неуспешные первые попытки сохранены отдельно и не используются как oracle.
- [x] Для оставленных предположений оформить маркер, реестр и единственную измерительную строку; измеренные якоря не переписывать по догадке. `open_questions_registry_matches_source_markers` и `measure_script_covers_every_registry_id` проходят после снятия измеренных вопросов; marker/registry/measure изменяются одним шагом.

## Файловое ядро

- [x] Измерить и исправить аргументы двух setter атрибутов: 224 серверных случая localhost/test 2026-09-16, 28 значений × RU/EN × два атрибута × два исходных состояния. Строки Да/Нет отвергаются без изменения, в отличие от общего условия BSL; остальные измеренные преобразования совпали. Сохранены raw oracle и восстановление CF/XML, добавлены сравнение в четырёх режимах CLI и контроль отсутствия вызова host-setter. Расширенные строки и порядок ошибок закрыты двумя последующими пробами; локализованный текст ошибки отнесён к общей диагностике. Отчёт: docs/research/compatibility/file-attribute-arguments-2026-09-16.md.
  - [x] Измерить расширенные строковые границы setter атрибута: U+001C…U+001F входят в снимаемые Unicode-пробелы, U+200B не входит, yes/no означают Истина/Ложь. Отдельный преобразователь подключён ко всем sync/Async/Begin setter-ам без изменения обычного условия и рекурсии НайтиФайлы. Все 38 проб совпадают в четырёх режимах; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-attribute-string-edges-server-2026-09-17.md.
  - [x] Измерить приоритет ошибки аргумента и отказа доступа на отсутствующем пути: 16 RU/EN sync/async-случаев подтвердили конверсию до host; async доставляет оба ошибки при `Ждать`. Все структурные результаты совпали; локализованный текст отнесён к общей диагностике runtime. Native-сеанс `os2_na2c` восстановил CF/БД и сверил XML; отчёт — docs/research/compatibility/file-attribute-access-order-2026-09-18.md.

- [x] Измерить удаление ссылок на независимых одинаковых деревьях native/openbsl: 18 случаев file-delete-links, подтверждение сохранности/удаления целей, восстановление CF/XML. 9 строк совпали; выявлены различия прямого удаления оборванной ссылки и удаления дерева через ссылку.
- [x] Получить отдельное решение по намеренному no-follow удаления. Пользователь сохранил действующий no-follow: open-bsl удаляет саму ссылку и не очищает целевое дерево, в отличие от измеренной 1С. Контракт SystemFileSystem::remove_path и его системные/BSL-проверки остаются без изменений.

- [x] Выполнить первый серверный замер поиска и метаданных символических ссылок в отдельном дереве; сохранить исходный вывод и проверить восстановление базы. `file-search-links.platform.txt`: метаданные и явно заданный корень-ссылка совпали, смешанный обход дал необъяснённые расхождения.
- [x] Разделить расхождение file-search-links шестью контрольными деревьями без ссылок, с одним видом ссылки и сочетаниями: 24 результата file-search-link-controls подтвердили рекурсивное следование по существующей ссылке на каталог и отдельную аномалию оборванной ссылки. Исходный вывод сохранён, восстановление проверено.
- [x] Реализовать измеренное следование по ссылке на каталог и выдачу совпадений текущего уровня перед рекурсивными результатами, сохранив пути через имя ссылки, host, отмену и пределы. No-follow смысл DirEntry::is_dir для ZIP сохранён; отдельный признак ссылки включает metadata цели. Все 16 контрольных строк без оборванной ссылки совпали; 11 тестов поиска и полный workspace, fmt, Clippy, rustdoc, API reference и строгий OpenSpec прошли.
- [x] Проверить восемь масок при первой и последней позиции оборванной ссылки и без неё: 48 строк file-search-broken-masks сохранены, восстановление проверено. Результаты зависят от порядка: первая позиция даёт ноль для */*.*, последняя сохраняет предшествующие результаты; точное имя находит ссылку в обеих позициях.
- [x] Уточнить среднюю позицию оборванной ссылки и первую найденную запись: 42 пробы file-search-broken-middle подтвердили раздельное прерывание выдачи совпадений и списка каталогов. Измеренные правила реализованы; все 133 строки четырёх измерителей совпали, добавлены десять детерминированных сценариев host. Полный workspace, fmt, Clippy, rustdoc и строгий OpenSpec прошли.
- [x] Уточнить поздние не совпадающие с маской ссылки и несколько оборванных ссылок: 128 RU/EN-вызовов по четырём деревьям и восьми маскам на серверной localhost/test 2026-09-16 совпали с текущим алгоритмом, включая порядок. Сохранены нативный вывод, упорядоченный снимок дерева и подтверждение восстановления CF/XML. Добавлен детерминированный host-тест по всем строкам oracle; реальное дерево проверено в plain/optimize и обоих текстовых round-trip. Отчёт: docs/research/compatibility/file-search-late-links-server-2026-09-16.md.
- [x] Уточнить прочие ошибки metadata, иные маски и сочетания ссылок с пробельными именами; не объявлять измеренные правила универсальной обработкой любых ошибок и не снимать FIND.TRAVERSAL целиком. Измерены классы/escape масок, пробельные ссылки, PermissionDenied/ELOOP, цикл и границы `sfile`; все описанные ниже матрицы совпадают. `FIND.TRAVERSAL` сохранён для иных host-ошибок, форм цикла и `sfile`, а не обобщён по догадке.
  - [x] Измерить расширения чистой файловой маски: 12 POSIX-классов, отрицание, пять escape-форм и четыре повреждённых класса дали 40 совпавших результатов open-bsl/серверной 1С. Имя `*` исключено как sfile, не обычный файл; прочие escape-сочетания остаются открытыми. CF/БД/XML восстановлены.
  - [x] Закрыть `FILE.MASK_EXTENSIONS` по измеренной границе области: обычный байтовый сопоставитель полностью покрыт `file-masks-single` и `file-mask-extensions`; литеральная звёздочка создаёт `sfile://`, а обратная косая черта разбирается как разделитель пути. Неизвестный отбор sfile остаётся только в `FIND.TRAVERSAL`; алгоритм сопоставителя не менялся.
  - [x] Измерить сочетания ссылок с пробельными именами: два порядка оборванной/действующей ссылки, восемь масок, flat/recursive и RU/EN дали 64 упорядоченных совпадения open-bsl/серверной 1С. Детерминированный host-тест сверяет весь oracle; реальное дерево проверено в source/bytecode × plain/optimize. PermissionDenied, циклы, остальные маски и управляющие пробелы остаются открытыми. Отчёт — docs/research/compatibility/file-search-link-spaces-server-2026-09-17.md.
  - [x] Измерить `PermissionDenied` вложенного каталога и `ELOOP` самоссылочной ссылки: 24 RU/EN-вызова показали пропуск недоступного потомка и применение правил оборванной ссылки к Linux `ELOOP`. Исправлены 16 расхождений; корневой PermissionDenied позднее измерен отдельно и скрывается только поиском. Host-oracle и реальное дерево проверены в source/bytecode × plain/optimize. Отчёт — docs/research/compatibility/file-search-metadata-errors-server-2026-09-17.md.
  - [x] Измерить цикл ссылки на каталог: ссылка с 80-байтовым именем обратно в родительскую ветку дала успешные 42 результата и остановилась на Linux `ELOOP` по прежнему правилу оборванной ссылки. Серверная 1С и open-bsl совпали в source/bytecode × plain/optimize; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-search-directory-cycle-server-2026-09-18.md. Иные ошибки host и sfile остаются FIND.TRAVERSAL.
  - [x] Отделить `sfile` от обычного glob на внешнем дереве ОС: `?` остаётся обычным именем, `\` становится разделителем, а шесть форм со `*` дают особые несуществующие объекты с усечённым именем. Тридцать девять результатов и неизменившийся host-снимок сохранены; CF/БД/XML восстановлены. Маска `*` не выдаёт такие записи, но вопросительные маски ведут себя неодинаково, поэтому общее правило ещё остаётся FIND.TRAVERSAL. Отчёт — docs/research/compatibility/file-search-sfile-server-2026-09-18.md.
  - [x] Измерить отбор `sfile`: 13 физических имён и 16 масок дали 208 результатов. Маска с литералом использует байтовый файловый сопоставитель; чистые `?` требуют точной длины не меньше трёх; чистые и смешанные `*`/`?` без литерала не совпадают; экранированная звёздочка литеральна. Реализация отделяет результат поиска от физического пути удаления, а четыре режима сверяют полный oracle. CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-search-sfile-selection-server-2026-09-18.md. Рекурсия каталога со звёздочкой измерена следующим пунктом; иные sfile, ошибки и циклы остаются FIND.TRAVERSAL.
  - [x] Измерить физический каталог `a*`: на текущем уровне он имеет те же свойства и отбор sfile, но серверная 1С не входит в него при рекурсии даже для масок `*` и `*.txt`. Устранены два расхождения; host-тест проверяет отсутствие второго read_dir, четыре режима совпадают с oracle. Сессия восстановила CF/БД/XML. Отчёт — docs/research/compatibility/file-search-sfile-directory-server-2026-09-18.md. Ссылочные варианты измерены следующим пунктом; иные формы sfile, ошибки и циклы остаются FIND.TRAVERSAL.
  - [x] Измерить имя `a*` как ссылку на каталог, файл и отсутствующую цель: все три вида имеют одинаковый отбор и свойства специального объекта, не раскрывают цель и не обходятся рекурсивно. 24 результата совпали без изменений runtime; host-тест и четыре CLI-режима проверяют oracle. Обе успешные сессии восстановили CF/БД/XML; финальная исключила локале-зависимый Boolean. Отчёт — docs/research/compatibility/file-search-sfile-links-server-2026-09-18.md.

- [x] Инвентаризировать полный интерфейс `Файл` по Unica: страницы, RU/EN-имена, арности, свойства, методы и контексты; см. `docs/research/compatibility/file-interface-inventory-2026-09-09.md`.
- [x] Дополнить начальный `measure/filesystem/file-object.bsl` измерительными пробами каждого контракта и граничных случаев. Серверное ядро измерено в `file-object.bsl`; клиентские `Асинх`/`Начать`, мобильные члены и границы разнесены по специализированным сценариям, поскольку один контекст 1С их не измерит. `every_file_member_name_occurs_in_the_platform_probe_suite` доказывает покрытие всех RU/EN-имён, inventory-тест — арности и виды вызова. Семантические остатки `FILE.*` этим не снимаются. Карта — docs/research/compatibility/file-interface-probe-coverage-2026-09-18.md.
  - [x] Покрыть каждое RU/EN-имя конструктора, свойства и метода хотя бы одной специализированной платформенной пробой; единый серверный скрипт не используется для клиентских Async/Begin и мобильного контекста. Полноту таблиц `METHODS`/`PROPERTIES` проверяет unit-тест; карта и ограничения — `docs/research/compatibility/file-interface-probe-coverage-2026-09-18.md`. Семантические границы остаются в родительском пункте и маркерах `FILE.*`.
- [x] Расширить FileMetadata явным признаком обычного файла и необязательным размером, сохранив прежние конструкторы; проверить размер, следование по ссылке и отличие Unix-сокета от файла.
- [x] Добавить host-установку времени изменения: безопасный default Unsupported, секунды UTC, сохранность содержимого и времени доступа, отказ отсутствующему пути. Проверить отрицательные секунды, дробное время до epoch, каталог и ссылку на Linux. BSL-преобразования даты этим пунктом не закрываются.
- [x] Добавить необязательный host-атрибут read_only и set_read_only с безопасным default. Проверить старые конструкторы, сохранность данных и остальных прав, отсутствие выдачи записи группе/остальным при снятии, следование по ссылке и отказ отсутствующему пути. BSL-методы и chmod-поведение 1С остаются частью полного интерфейса ниже.
- [x] Добавить необязательный host-атрибут hidden и set_hidden с безопасным default. Проверены прежние конструкторы, отсутствие побочных операций в default и измеренный Linux-no-op для .hidden с сохранением содержимого и прав; отсутствие пути вызывает ошибку. Другие ОС не получают выдуманного false.
- [x] Измерить необязательный аргумент конструктора `Файл`, преобразование значений и лексические свойства пути: 24 серверные пробы, восстановление конфигурации, oracle-derived conformance и тест виртуальной ФС. Исправлены арность 0…1, форматирование аргумента, лишние относительные `..` и сохранение корня `///`; API reference регенерирована. Полный workspace, fmt, Clippy, rustdoc и строгая проверка OpenSpec прошли; см. `docs/research/compatibility/file-constructor-server-2026-09-10.md`.
  - [x] Проверить ещё одиннадцать объектных типов конструктора без файлового I/O: все используют `Строка(Аргумент)`; пустой СписокЗначений даёт пустой путь, а не имя типа. Исправлено единственное файловое расхождение, четыре режима CLI и unit-тест прошли. Язык представления значения Тип остаётся отдельным общим контрактом. Отчёт — docs/research/compatibility/file-constructor-more-types-server-2026-09-17.md.
- [x] Завершить полный синхронный интерфейс `Файл` по oracle. Подключены конструктор, пять свойств, Существует/ЭтоФайл/ЭтоКаталог/Размер, методы атрибутов и четыре метода времени; проверены реальная и виртуальная ФС, актуальность размера, RU/EN и read-only свойства. Весь `bsl-rt` и inventory проходят. Границы `FILE.EDGE_BEHAVIOR` и строковое/конструкторное продолжение `FILE.TIME_EDGE_BEHAVIOR` сохранены как явно неизмеренные границы, а не пробелы публичного интерфейса.
  - [x] Измерить FIFO, Unix-сокет, корневые формы и четыре дополнительных отказа metadata: серверная 1С считает FIFO и сокет файлами размера 0; Существует скрывает ELOOP, ENOTDIR, ENAMETOOLONG и PermissionDenied, остальные методы отказывают. Исправлены SystemFileSystem и запрос существования без обобщения на другие специальные объекты или Other-ошибку. 46 строк совпадают в четырёх режимах; системные и unit-тесты прошли. Обе сессии восстановили CF/БД/XML. Отчёт — docs/research/compatibility/file-object-host-edges-server-2026-09-18.md.
  - [x] Измерить символьные устройства Linux: `/dev/null`, `/dev/zero`, `/dev/full`, `/dev/random` и `/dev/urandom` серверная 1С считает файлами размера 0, не каталогами. `SystemFileSystem` распознаёт измеренный `is_char_device`, не обобщая правило на блочные устройства. 22 строки совпадают в source/bytecode × plain/optimize; native-сессия восстановила CF/БД/XML. Отчёт — docs/research/compatibility/file-object-devices-server-2026-09-18.md.
  - [x] Измерить операционный путь необычных имён: 22 серверных случая подтвердили обрезание по NUL и удаление шести конечных пробельных символов до I/O при сохранении лексического NUL. Исправлены методы Файл и ДвоичныеДанные.Записать через общий нормализатор; oracle совпадает в source/bytecode × plain/optimize, виртуальный host видит только нормализованные пути. CF/БД/XML восстановлены; отчёт — docs/research/compatibility/file-object-io-edges-server-2026-09-17.md. Ошибки доступа, прочие I/O-конструкторы и корни остаются FILE.EDGE_BEHAVIOR.
  - [x] Распространить операционный путь на файловых читателей: 11 серверных случаев для Новый ДвоичныеДанные, Новый ФайловыйПоток и ФайловыеПотоки.ОткрытьДляЧтения подтвердили общее обрезание и обязательный отказ каталогу. Один Rust-помощник используется до host, лексическое ИмяФайла сохраняется; oracle совпадает в четырёх режимах, виртуальные host-тесты проходят. CF/БД/XML восстановлены; отчёт — docs/research/compatibility/file-read-io-edges-server-2026-09-17.md. Записывающие режимы потоков, прочие I/O-конструкторы, доступ и корни остаются FILE.EDGE_BEHAVIOR.
  - [x] Проверить записывающие потоки: 44 изолированных случая четырёх способов открытия подтвердили тот же операционный путь по фактическим байтам базового файла. Создание, запись с позиции 0, дописывание и сохранённый отдельный суффикс различимы. Oracle совпадает в четырёх режимах без изменения runtime; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-write-io-edges-server-2026-09-18.md. Другие I/O-конструкторы и классы ошибок остаются FILE.EDGE_BEHAVIOR.
  - [x] Подключить `Новый ЗаписьТекста` к тому же операционному пути: 11 серверных случаев различили перезапись базового файла по BOM от создания соседнего имени. NUL и шесть краевых пробельных символов снимаются, U+007F/U+200B/U+FEFF сохраняются, начальный NUL отказывает на каталоге. Исправлено подтверждённое расхождение; oracle совпадает в source/bytecode × plain/optimize, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-text-writer-io-edges-server-2026-09-18.md.
  - [x] Подключить `ЧтениеJSON.ОткрытьФайл` и `ЗаписьJSON.ОткрытьФайл`: 21 безопасный серверный случай подтвердил общий операционный путь. Первый прогон выявил зависание платформы при чтении каталога после начального NUL и был восстановлен без oracle; сокращённый прогон завершился и восстановил CF/БД/XML. Исправлены оба направления, системный oracle совпадает в четырёх режимах, виртуальный host видит нормализованные имена. Отчёт — docs/research/compatibility/file-json-io-edges-server-2026-09-18.md.
  - [x] Подключить `ТекстовыйДокумент.Прочитать/Записать`: 21 серверный случай подтвердил тот же операционный путь по прочитанному тексту и фактическим байтам с BOM. Оба направления исправлены через существующий helper; oracle совпадает в source/bytecode × plain/optimize, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-textdoc-io-edges-server-2026-09-18.md.
  - [x] Подключить `ЧтениеXML.ОткрытьФайл` и `ЗаписьXML.ОткрытьФайл`: 21 серверный случай подтвердил общий операционный путь по имени прочитанного элемента и фактическим байтам с BOM. Оба направления исправлены через существующий helper; oracle совпадает в source/bytecode × plain/optimize, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-xml-io-edges-server-2026-09-18.md.
  - [x] Подключить `СоздатьФабрикуXDTO(Путь)`: десять серверных случаев подтвердили общий операционный путь по факту успешного разбора минимальной XSD. Локале-зависимое имя типа исключено повторным нативным прогоном, не подменой oracle. Фабрика исправлена через существующий helper; четыре режима совпадают, обе сессии восстановили CF/БД/XML. Отчёт — docs/research/compatibility/file-xdto-factory-io-edges-server-2026-09-18.md.
  - [x] Подключить `ТабличныйДокумент.Прочитать/Записать`: 21 безопасный серверный случай подтвердил общий операционный путь по тексту ячейки и изменению базового MXL. Оба метода исправлены через существующий helper; oracle совпадает в source/bytecode × plain/optimize. Финальная сессия восстановила CF/БД/XML; предварительный compile-check также восстановил CF и сверил XML без служебного `ConfigDumpInfo.xml`. Отчёт — docs/research/compatibility/file-spreadsheet-io-edges-server-2026-09-18.md.
  - [x] Подключить файловые цели чтения/записи архивов: 84 серверных случая покрыли конструктор и `Открыть` для `ЧтениеZipФайла`/`ЧтениеФайлаАрхива` и `ЗаписьZipФайла`/`ЗаписьФайлаАрхива`. Общие `read_source` и `writer_write` исправлены через существующий helper; oracle совпадает в source/bytecode × plain/optimize, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-archive-io-edges-server-2026-09-18.md.
  - [x] Подключить пути `Добавить` и `ИзвлечьВсе`: 20 серверных случаев по составу архива и фактическому каталогу распаковки подтвердили общий путь. Завершающий разделитель `Добавить` сохранён как ранее измеренный сигнал `*`; его сочетания с пробелами не обобщаются. Oracle совпадает в четырёх режимах, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-archive-content-io-edges-server-2026-09-18.md.
  - [x] Подключить `ДокументPDF.Прочитать/Записать`: 21 серверный случай подтвердил общее снятие шести конечных пробельных символов и асимметрию NUL: reader обрезает, writer отказывает. Оба прямых host-пути исправлены без скрытия writer-ошибки; oracle совпадает в source/bytecode × plain/optimize, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-pdf-io-edges-server-2026-09-18.md.
  - [x] Подключить оба файловых TLS-конструктора: 20 серверных случаев с невалидным содержимым изолировали чтение пути от позднего TLS-разбора. Оба типа повторили общий reader-путь; исправлены до host-чтения. Oracle совпадает в четырёх CLI-режимах, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-http-certificate-io-edges-server-2026-09-18.md.
  - [x] Закрыть `FILE.INIT_ARGUMENTS` для смежного async-инициализатора: девять дополнительных клиентских категорий подтвердили общую `Строка`, немедленную мутацию и ссылочное тождество. Нативный oracle, conformance и восстановление CF/БД/XML сохранены; отчёт — docs/research/compatibility/file-initialize-arguments-client-2026-09-17.md.
  - [x] Измерить сравнение, арифметику, ключи и сериализацию двух дат с нулевыми полями: 65 проб, два побайтно совпавших серверных прогона, восстановление CF/БД/XML. Сохранены file-zero-date.bsl/platform.txt и отчёт file-zero-date-server-2026-09-17.md. Один sentinel и empty() не соответствуют oracle.
  - [x] Дополнить oracle соседними mtime и долями секунды, обычными контрольными датами, равенством результатов арифметики, порядком пары, повторным чтением и явной локалью форматирования. Три прежних прогона покрывают 494 строки; отдельный file-zero-date-order подтвердил `local_min < utc_min` и тождество после +0. Его native-сеанс cq1xarop восстановил CF/БД/XML, восемь строк совпали в четырёх режимах open-bsl.
  - [x] Описать и реализовать представление граничной даты в bsl-rt: сырой `u64` тактов сохраняет различие, сравнение и хеш; циклическая знаковая разность воспроизводит oracle. seconds/from_seconds/diff_seconds сохраняют секунды. Внутренняя/XML/JSON-сериализация нормализует значение к пустой дате; строковое форматирование остаётся отдельным открытым правилом.
    - [x] Реализовать измеренную основу обычных дробных дат: три oracle, шаг 1/10000, округление, насыщение BSL-арифметики, точное вычитание и UTC-getter; seconds/from_seconds сохраняют секунды. 210 native-проб проверены в четырёх режимах, ключи и checked Rust-методы — unit-тестом. Нулевые поля остаются отдельным незавершённым этапом.
    - [x] Сохранить дробные константы в байткоде формата 33, проверить точный round-trip и отказ версии 32; API reference регенерирован.
  - [x] Подключить измеренное представление ко всем методам времени. Синхронный UTC-getter, поля, сравнение/ключи, арифметика, границы, три сериализации и обе четырнадцатизначные нулевые формы проверены по 62 строкам oracle. Отдельные Async и Begin oracle подтверждают RU/EN, dynamic, равенство sync, порядок, повторное Ждать и возврат до callback; все проходят plain/optimize и source/bytecode. Строковые представления и точная ошибка числового конструктора остаются узким продолжением FILE.TIME_EDGE_BEHAVIOR, но не блокируют доставку значения методами времени.
    - [x] Измерить строковые аргументы обоих setter-ов: 80 строк подтвердили 8/14 цифр, обе нулевые формы, шесть снимаемых краевых пробелов, отказ U+200B/U+FEFF и завершение по NUL. Исправлен отдельный файловый преобразователь без изменения общего конструктора Дата; oracle совпадает в source/bytecode × plain/optimize. CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-time-string-arguments-server-2026-09-18.md.
    - [x] Измерить точные границы периода и огромные сдвиги. file-zero-date-week показал общий сырой результат `НачалоНедели` -298955.1616; date-shift-limits подтвердил знаковые младшие 64 бита тактов. Оба oracle проходят в четырёх CLI-режимах; `DATE.SHIFT_LIMITS` снят из реестра.
- [x] Завершить `СоздатьКаталог`: RU/EN builtin через host реализован; измеренные типы, пустые значения, разделители, корни, относительные и специальные имена, а также два частичных Linux-отказа покрыты. Общий `Строка` подключён отдельным HostEffect::FileCreate; `CREATEDIR.PATH_SEMANTICS` закрыт.
  - [x] После активации лицензии 2026-09-16 измерить 26 серверных случаев и 14 случаев с внешним снимком до выхода: нормализация точек/разделителей, пустые значения, NUL и конечные ASCII-пробелы реализованы через общий разбор пути. Физические имена проверяются отдельно от НайтиФайлы; 25 серверных случаев перенесены в conformance, четыре лексические строки — в отдельную фикстуру. Поиск пробельных имён, двоеточие и sfile остаются открытыми. Отчёт — docs/research/compatibility/file-create-paths-server-2026-09-16.md.
  - [x] Измерить конечные управляющие символы физического пути: 11 серверных случаев через удаление базового имени подтвердили общий набор снятия NUL/TAB/LF/CR/SPACE/U+0085/NBSP. Создание использует общий операционный путь; U+0001/U+007F/U+200B/U+FEFF сохраняются. Oracle совпадает в четырёх режимах, виртуальный host проверяет путь, CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-create-directory-edges-server-2026-09-17.md. На этом этапе типы, корни, двоеточие и частичные отказы ещё оставались открытыми.
  - [x] Измерить корневые формы и объект Файл sync/async СоздатьКаталог: `/`, `//`, `///`, `.`, `./`, `..`, `../` успешны. Первоначальные пробы доказали возврат того же Файл и отсутствие создания пути внутри объекта, но не исключили каталог `Строка(Файл)`; последующие изолированные клиентская и серверная пробы типов подтвердили такое создание. CF/БД/XML восстановлены. Отчёты — docs/research/compatibility/file-create-directory-roots-server-2026-09-17.md, file-create-directory-types-client-2026-09-18.md и file-create-directory-types-server-2026-09-18.md.
  - [x] Реализовать серверные типы СоздатьКаталог: 17 непустых представлений создают каталог через общий `Строка`, три пустых представления отказывают без создания; ни preexisting-skip, ни cleanup-error в oracle нет. Специальный no-op Файл удалён, отдельный форматирующий host-effect проверен unit-тестом. Conformance проходит source/bytecode × plain/optimize; CREATEDIR.PATH_SEMANTICS удалён из кода, реестра и общего измерителя.
  - [x] Измерить двенадцать составных относительных/корневых форм sync/Async: 1С принимает все и Async возвращает исходную строку. Исправлены десять отказов open-bsl через точный no-op-список до host; четыре режима и прямой подготовитель проверены. CF/БД/XML восстановлены.
  - [x] Измерить два частичных отказа на Linux: компонент длиной 300 байт оставляет уже созданный префикс, а `PermissionDenied` внутри приватного scratch не создаёт ребёнка. Серверный oracle совпал с open-bsl в source/bytecode × plain/optimize; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-create-directory-failures-server-2026-09-18.md. Прочие типы позднее закрыты отдельной матрицей.
  - Подготовлены 27 изолированных проб file-create-paths.bsl. Две попытки 2026-09-15 остановлены до загрузки конфигурации: сначала License not found на повторной XML-выгрузке, затем License not found on the 1C:Enterprise server на DumpCfg. Кластер доступен; oracle отсутствует, изменения поведения не внесены. Свидетельства и точная команда продолжения — docs/research/compatibility/file-create-paths-attempt-2026-09-15.md.
- [x] Выделить общий механизм масок без изменения измеренного ZIP-диалекта; `bsl_rt::simple_mask_matches`, 48 ZIP-тестов прошли, включая 18 случаев характеризации до/после переноса.
- [x] Завершить `НайтиФайлы` с массивом `Файл`: RU/EN builtin, host-привязка, маски, рекурсия и отмена из VM подключены; аргументы закрыты общим форматировщиком и измеренным условным преобразованием. Порядок host, текущий уровень, ссылки, пробельные имена, измеренные ошибки, один Linux-цикл, свойства и отбор измеренных sfile, включая каталог `a*` и три ссылочных вида, реализованы. 21 host-тест и десять oracle-derived CLI-матриц проходят. Иные формы sfile, циклов и ошибок остаются явной границей `FIND.TRAVERSAL`, а не незавершённым публичным API.
  - [x] Измерить десять дополнительных объектных типов маски одновременно для sync/Async: девять прямых совпадений и контрольная дата дали 20 успешных вызовов. Локале-зависимый текст не включён в контракт. Финальная сессия восстановила CF/БД/XML; первый compile-time отказ и таймаут завершённого без shutdown клиента также восстановлены.
- [x] Измерить девять типов маски удаления на сервере с проверкой фактически оставшихся файлов; сохранить оригинальный вывод и сверить восстановленную конфигурацию. `file-delete-types.platform.txt` подтверждает преобразование чисел, булевых, массива и структуры через Строка и удаление корня для Null.
- [x] Исправить подтверждённые синхронные расхождения `file-delete-types.platform.txt`: общий BSL-форматировщик для маски, oracle-derived conformance, виртуальная ФС и сохранение отмены. Публичные Rust-входы `call_builtin_files` и `call_builtin_delete_files` сохранены совместимыми; первый аргумент не изменён по замеру второго. Девять случаев совпали с oracle; полный workspace, fmt, Clippy, rustdoc и строгий OpenSpec прошли. Асинхронное преобразование требует отдельного замера.
- [x] Проверить ещё десять типов маски: Дата, Соответствие, фиксированные коллекции, ТаблицаЗначений, UUID, Файл, ДвоичныеДанные, перечисление и Тип приняты серверной 1С и сохраняют несовпадающий сосед. Текущий общий форматировщик уже совпал в четырёх режимах; CF/БД/XML восстановлены. Отчёт — docs/research/compatibility/file-delete-more-types-server-2026-09-17.md. Точное представление остальных значений остаётся общим контрактом `Строка`, не файловым белым списком.
  - [x] Повторить те же десять типов для DeleteFilesAsync на клиенте: девять безопасных представлений фактически удалены, дата принята на контрольном имени; все обещания вернули Undefined и сохранили корень. Oracle совпал в четырёх режимах, CF/БД/XML восстановлены.
- [x] Завершить маску `УдалитьФайлы`: серверные и клиентские oracle покрывают выбор, преобразования маски, ссылки, read-only и частичный Linux-отказ; общий форматировщик обслуживает все поддерживаемые значения без файлового белого списка. Для ссылок сохранено согласованное защитное no-follow-отличие от 1С. Host-тесты фиксируют один снимок целей, позднюю запись, исчезнувшую цель, отмену, отсутствие отката и повторного I/O; прежние DELETEFILES-фикстуры входят в полный workspace-прогон.

## Временные имена и каталог

- [x] Измерить и исправить расширения `ПолучитьИмяВременногоФайла`, не нарушив отсутствие заранее созданного файла и host-изоляцию. Проверены default/Неопределено, пустое, xml/.xml, несколько точек, оба разделителя и отказ числу; системный host резервирует первый компонент без создания родителей, тест коллизии сохраняет соседние данные.
- [x] Добавить host-метод `FileSystem::temporary_directory`; проверить безопасный default, отсутствие создания путей и общий выбор TEMP/TMP. `bsl-rt/tests/temporary_directory.rs`: default через trait object и семь изолированных сценариев; red/green подтверждены.
- [x] Добавить `КаталогВременныхФайлов / TempFilesDir` через host; проверены виртуальная ФС, ловимый отказ неподдерживающего host, арность и завершающий разделитель без дублирования. Стандартный host проверен в восьми изолированных вариантах окружения.
- [x] Проверить TEMP → TMP → системный каталог; пустые и отсутствующие переменные, ошибка операции без fallback и создания каталога. `bsl-rt/tests/temporary_directory.rs`: семь изолированных сценариев, red/green подтверждены.

## Приёмка

- [x] Регенерировать API reference и проверить RU/EN-имена, арности, типы вызовов, REPL и неизвестные методы.
- [x] Проверить plain/optimize, текстовый round-trip и отказ старому формату, еси он менялся.
- [x] Выполнить fmt, Clippy, workspace tests, rustdoc и проверку реестра измерений; ZIP/stream/Connector/job-регрессии включены в полный workspace-прогон 18.09.2026.
- [x] Провести review безопасности путей, возможностей запуска и владения временными ресурсами. Подтверждены отсутствие sandbox у SystemFileSystem, no-follow конечной ссылки, валидация host-имён поиска, явная capability запуска без shell и недоступность незавершённого системного temp-владения из BSL. Отчёт — docs/research/compatibility/filesystem-security-review-2026-09-18.md.
- [x] Проверить OpenSpec строго, перенести принятые delta в действующие specs и архивировать только после завершения согласованного объёма. Перед архивированием прошли `cargo test --workspace`, fmt, Clippy workspace `-D warnings`, rustdoc `-D warnings`, `openspec validate --all --strict` (11/11) и `git diff --check`.
