# Дописывание канонического числа

Добавить `BslNumber::append_canonical(&self, out: &mut String)` для
сериализации без отдельной строки результата на каждое число.
`to_canonical` использует тот же алгоритм; BSL API и формат не меняются.
