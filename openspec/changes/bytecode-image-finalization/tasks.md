# Задачи

## 1. Проверка невозможного состояния

- [x] 1.1 Тест: образ с разметкой от прежней редакции инструкций
  отвергается. Номера чанка в сообщении НЕТ: модуль сообщает
  статическими строками, и назвать номер можно только с типизированной
  ошибкой — отдельная задача плана (см. `design.md`).
- [x] 1.2 Тест: образ с разметкой, посчитанной с чужим пересечением
  модульных слотов, отвергается.
- [x] 1.3 Тест: согласованный образ проверку проходит.

## 2. Единая точка

- [x] 2.1 Ввести публичную финализацию `Program` в `bsl-bytecode`,
  вычисляющую пересечение модульных слотов изнутри образа.
- [x] 2.2 Перевести на неё компилятор, текстовый парсер и фоновые
  задания.
- [x] 2.3 Перевести тестовые обвязки `bsl-bytecode`, `bsl-compiler` и
  `bsl-vm`.
- [x] 2.4 Убедиться, что вне `bsl-bytecode` не осталось присваиваний
  производных таблиц.

## 3. Проверка

- [x] 3.1 `cargo fmt`, `clippy -D warnings`, rustdoc, полный workspace.
- [x] 3.2 Круговой текстовый корпус побайтово не изменился.
- [x] 3.3 `bundle::verify` по всему конформанс-корпусу.
- [x] 3.4 Сверка JIT с интерпретатором на всём корпусе.
- [x] 3.5 Чередующийся A/B: цена проверки при связывании +0,007 % на
  `empty_for` (канарейка старта) и +0,173 % на самом коротком прогоне
  `simple_parquet_reader`; на длинных — шум.

## 4. Инлайн-кэши

- [x] 4.1 Тест: короткий и длинный кэш свойств и методов отвергаются до
  первой инструкции; зубы подтверждены отключением проверки.
- [x] 4.2 Проверка длин добавлена в проверку образа (коммит `1382c3b`).

## 5. Производные таблицы только на чтение

- [x] 5.1 Поля закрыты до `pub(crate)`, наблюдатели `bundle_len()`,
  `touches_objects()`, `prop_cache()`, `method_cache()` под
  `#[inline(always)]`. Литералом `Chunk { .. }` снаружи больше не
  собрать — введён `Chunk::new`.
- [x] 5.2 Три отрицательных теста, которым нужна ПОРЧА производного
  состояния, перенесены в `image::tests`; снаружи остались те, что
  портят образ законными средствами (правкой инструкций после
  финализации).
- [x] 5.3 Названы две операции для одиночного чанка:
  `finalize_lone_chunk` (подстановка библиотек — там `None` верен) и
  `finalize_lone_chunk_unbundled` (динамический фрагмент — разметку ему
  считать нельзя, решение измеренное: +9,6 % на eval-в-цикле, а с
  `None` она утверждала бы независимость ложно).
- [x] 5.4 `./benchmarks/hot-code-diff.sh 30b4411`: `step` 0xb099/8970 и
  `step_cold` 0x55d3/4040 — совпадают с операндами и длинами.

## 6. Что вскрыл перевод

- [x] 6.1 Фрагмент `Выполнить`/`Вычислить` оставался БЕЗ ячеек
  инлайн-кэша: их заполнял литерал компилятора, а финализация
  программы до одиночного чанка не доходит. Проверка образа отвергала
  такой фрагмент — поймано тестами REPL.
- [x] 6.2 Устранение копий удаляло ячейки кэшей вслепую. Проходы над
  готовым байт-кодом идут ДО финализации, то есть таблиц может ещё не
  быть; поставлена та же защита по длине, что у разметки.

## 7. Границы модульных слотов

- [x] 7.1 Проверка образа отвергает `GetModuleVar`/`SetModuleVar` с
  номером вне таблицы модульных переменных; прежде это ловила только VM
  и только на исполнении инструкции.
- [x] 7.2 Тест с зубами на оба вида инструкции.

## 8. По замечаниям ревью

- [x] 8.1 Финализация СБРАСЫВАЕТ инлайн-кэши, а не сохраняет прогретые:
  ячейка помнит форму и слот, но не имя поля, поэтому пережившая правку
  инструкций запись вернула бы слот прежнего свойства при совпавшей
  форме.
- [x] 8.2 `finalize_lone_chunk_unbundled` очищает разметку: на уже
  финализированном чанке «без бандлов» вопреки имени означало «с
  прежними бандлами».
- [x] 8.3 Проверка модульных слотов спрашивает ЕДИНСТВЕННУЮ таблицу
  эффектов (`ModSet::One`), а не второй разбор по опкодам.
- [x] 8.4 `corrupt_program` в `bsl-vm` финализируется: прежде периметр
  отказывал на длине инлайн-кэша раньше проверяемого дефекта, и тесты
  проходили по посторонней причине.
- [x] 8.5 Утверждение о полноте `verify` снято как неверное: `LoadConst`
  вне таблицы констант и регистровые операнды периметр не проверяет.
  Причина названа: `RegSet` не отличает адресацию от насыщения
  (`RunDynamic` читает «всё»), тогда как `ModSet` отличает.
