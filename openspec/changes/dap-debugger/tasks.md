# Задачи

Предпосылка: изменение `bytecode-line-table` принято и влито. Без таблицы
строк пункты 3 и 4 нечем выполнить.

## 1. Ключи

- [x] 1.1 Три ключа разбираются в `parse_debug_flags`; `--debug` —
  модификатор по образцу `--optimize`, снимается из аргументов до
  разбора команды и скрипту не достаётся. Обе записи значения приняты:
  `--debug-port 5005` и `--debug-port=5005`.
- [x] 1.2 Есть, на обоих уровнях: разбор (`an_address_without_debug_is_refused`)
  и сам бинарник (`an_address_flag_without_debug_is_refused`).
- [x] 1.3 Умолчания `127.0.0.1:4711` проверены на разборе; занятый порт
  проверен на бинарнике — отказ называет порт и не паникует.
- [x] 1.4 Описаны; умолчания печатаются ИЗ ТЕХ ЖЕ констант, что читает
  разбор, и это проверено тестом — подсказка, называющая не тот порт,
  хуже отсутствующей.
- [ ] 1.5 Тест на сочетание ключей: `--debug --optimize` и
  `--debug --optimize=copy-elim` отказывают с ненулевым кодом, а
  `--debug --optimize=const-fold` (проход не удаляет) работает.

  Сам отказ уже есть и проверен в `bytecode-line-table` на уровне
  компилятора и фасада; здесь проверяется только то, что ключи CLI до
  него доходят и что сообщение видно пользователю.

## 2. Транспорт

- [x] 2.1 `dap::wire`, восемь тестов: целый кадр, два склеенных, разрыв
  по КАЖДОМУ возможному месту, длина в байтах (тело из 13 байт и 10
  символов — иначе тест ничего не доказывал бы), заголовок без
  `Content-Length`, нечисловая длина, абсурдная длина и регистр имени.
- [x] 2.2 Сокет читает отдельный `std::thread`, он же режет поток на
  кадры и отдаёт СТРОКИ каналом. Через границу потока не ходит ни одно
  BSL-значение — только кадры протокола.
- [x] 2.3 Добавлен; `cargo tree -e normal` не показывает `serde` ни у
  `bsl-vm`, ни у `open-bsl`, ни у `bsl-bytecode`.
- [ ] 2.4 Тест: `pause` доходит до прогона, крутящегося в цикле без
  остановок.

## 3. Крючок в VM

- [x] 3.1 `DebugHook` объявлен в `bsl-vm`, зовётся из ВНЕШНЕГО цикла
  перед `step`. Положен не в `HostIo`, а полем прогона
  (`ProgramExecution::set_debug_hook`): так он не протаскивается через
  шесть публичных сигнатур и через фасад, а `HostIo` остаётся тем, чем
  был. Кадры отдаются как `(модуль, чанк, pc)` только на чтение — `Frame`
  наружу не открывается.
- [x] 3.2 Непучкован (`image::finalize_unbundled` при сборке со
  сведениями об отладке) и с `merge_linear = false` — его выключает
  сама установка крючка: при сцеплении `step` не возвращается во
  внешний цикл между бандлами, а крючок зовётся именно оттуда.
  Непучкованность СДЕЛАНА: `image::finalize_unbundled`, и образ со
  сведениями об отладке собирается им — внутри бандла остановиться
  негде. Проверено тестом, причём с предусловием, что обычный образ
  разметку имеет. `merge_linear` — вместе с крючком.

  Круг через текстовый формат тоже обязан сохранять непучкованность,
  и сперва не сохранял: разбор звал обычную финализацию, и
  сохранённый отладочный образ возвращался размеченным. Поймал
  круговой тест.
- [x] 3.3 Без крючка поведение прежнее: весь набор (1715 тестов) зелёный,
  включая сверку JIT с интерпретатором на всём корпусе. Два теста самого
  крючка: он видит каждую инструкцию по порядку (первый вызов — нулевая
  инструкция нулевого чанка, дальше `pc` растёт, то есть зовётся ПЕРЕД
  инструкцией) и умеет прекратить прогон ровно там, где просили.

## 4. Сеанс

- [ ] 4.1 Запросы: `initialize`, `attach`, `setBreakpoints`,
  `configurationDone`, `threads`, `stackTrace`, `scopes`, `variables`,
  `continue`, `next`, `stepIn`, `stepOut`, `pause`, `evaluate`,
  `disconnect`, `terminate`.
- [ ] 4.2 События: `initialized`, `stopped`, `terminated`, `exited`,
  `output`.
- [ ] 4.3 Компиляция выражения идёт через существующий
  `DynamicCompiler`, второй семантики выражений не заводится.
- [ ] 4.7 Операция VM, вычисляющая фрагмент в ВЫБРАННОМ кадре.
  Существующий `run_dynamic_snippet` берёт `frame: &Frame` вызывающего
  `Выполнить`; DAP присылает `frameId` любого кадра стека, и верхним он
  не обязан быть.
- [ ] 4.8 Тест: остановка во вложенном вызове, вычисление выражения в
  ТЕКУЩЕМ кадре и в РОДИТЕЛЬСКОМ дают разные и правильные значения для
  переменной с одним именем в обоих.
- [ ] 4.4 `Сообщить` в отладочном прогоне дублируется событием `output`,
  не переставая писать в stdout.
- [ ] 4.6 Кадр внутри `Выполнить`/`Вычислить` показывается со СВОИМ
  источником — текстом фрагмента, отдаваемым редактору по запросу, — а
  не отображается на файл. Строки во фрагменте считаются от начала его
  собственного текста (решение изменения `bytecode-line-table`), поэтому
  наложенные на файл они указали бы не туда.
- [ ] 4.5 Сквозной тест: скрипт, точка останова на строке, остановка,
  чтение переменной, шаг, продолжение, завершение — без редактора,
  подставным клиентом DAP.

## 5. Ворота

- [x] 5.1 `./benchmarks/hot-code-diff.sh 509a1ab`: `step` — 0xb099, 8970
  инструкций; `step_cold` — 0x55d3, 4040. Оба СОВПАДАЮТ с крючком в
  дереве. Утверждение «крючок не в горячем цикле» этим и проверено, а не
  обещано.
- [ ] 5.2 Альтернирующий A/B на `empty_for`, `call_overhead`,
  `pi_leibniz`: обычный прогон не подорожал. Числа записываются сюда.
- [ ] 5.3 Сверка JIT с интерпретатором на всём корпусе.
- [ ] 5.4 `cargo fmt`, `clippy -D warnings`, rustdoc, полный workspace.
- [ ] 5.5 Справочник API перегенерирован, если публичная поверхность
  изменилась, и его диф просмотрен.
