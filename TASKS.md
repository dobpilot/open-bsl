# Backlog

Line format (order top-to-bottom by priority):

    - [ ] (slug) [hard] short description

- `(slug)` is a stable id with no spaces. The arbiter ticks the box by matching
  it, so it must be unique. Reuse OpenQuestion IDs where one exists.
- `[hard]` is optional. Tag a task hard to trigger the two-reviewer panel (two
  independent reviewers, both must PASS) and max effort on review. Use it for
  subtle platform semantics, decimal edge cases, and VM/bytecode correctness.
- The reviewer flips `- [ ]` to `- [x]` — never do it by hand mid-run.

## Open

Этап 6 из docs/std-library-plan.md — PDF (16.4): свой писатель и
читатель PDF без внешних крейтов, в связке с «ТабличныйДокумент»;
потоки — FlateDecode поверх готовых `deflate.rs`/`inflate.rs` этапа 4.
Область ID `PDF.*`. Засеян 2026-08-14. Электронные подписи — за
границей проекта («Что сознательно не делаем»): честная ошибка, не
молчание. После этапа глава 16 закрыта; остаются кандидаты вне этапов:
GZIP и TAR поверх inflate, `ПолучитьЭлементПоИдентификатору` при
внутреннем DTD-подмножестве, порядок свойств в `ЗаписатьXML` фабрики.
Когда здесь не останется ни одной строки `- [ ]`, планировщик напишет
в PLAN.md `ALL_TASKS_DONE`, и цикл остановится на первой же итерации.

- [x] (pdf-writer-core) [hard] этап 6: писатель PDF в новом
      `bsl-rt/src/pdf.rs`, без внешних крейтов и пока без поверхности
      BSL: объектная модель файла (нумерованные объекты, словари,
      потоки, xref, трейлер), страницы, контент-потоки с FlateDecode
      поверх готового `deflate.rs`, текст и примитивы (линии,
      прямоугольники, заливка) в объёме, нужном табличному документу.
      Кириллица обязательна: решение о шрифтах (встраивание,
      подмножество, кодировка) принять явно, сверив с тем, как пишет
      платформа — снять её файл простого документа через
      `Записать(..., ТипФайлаТабличногоДокумента.PDF)` и разобрать
      руками, — и записать выбор в комментарий модуля. Юнит-тесты:
      структурная валидность (разбор собственного вывода), проверка
      внешним инструментом из наличных в системе (pdftotext, qpdf,
      python3 — что найдётся; команду происхождения в комментарий).
      Ошибки — RtError, не panic.
- [x] (pdf-spreadsheet-write) [hard] запись табличного документа в
      PDF: `Записать(Путь, ТипФайлаТабличногоДокумента.PDF)` у
      «ТабличныйДокумент» (точное имя члена перечисления и арность —
      измерить); раскладка из `spreadsheet.rs` — колонки, строки,
      объединения, шрифты и начертания, рамки, выравнивания,
      ориентация и поля страницы (какие свойства табдока платформа
      учитывает — пробами). Перекрёстный критерий обязателен: наш PDF
      платформа открывает своим «ДокументPDF» и отдаёт число страниц
      (проба в measure-скрипте задачи); текст нашего и платформенного
      PDF одного и того же документа совпадает после извлечения
      наличным инструментом. Фикстура `pdf-write` с эталоном, снятым
      прогоном на платформе.
- [x] (pdf-doc-read) [hard] чтение PDF: разбор контейнера в `pdf.rs`
      (xref и xref-поток, объекты, потоки FlateDecode через
      `inflate.rs`, зашифрованный файл — честная ошибка) и поверхность
      «ДокументPDF» (конструктор и способ открытия файла — измерить;
      «ЧтениеPDF»/«ЗаписьPDF» из инвентаря главы — выяснить пробами,
      типы это или методы; страницы и их число, свойства документа —
      что найдётся). Через все таблицы-единственные-источники; новые
      опкоды — через text.rs, round-trip корпус и классификацию
      эффектов в bundle.rs. Фикстура `pdf-read` с эталоном с
      платформы; входной PDF-фикстура в дереве с записанным
      происхождением (сгенерировать платформой).
- [ ] (pdf-attachments) [hard] вложения PDF: прочитать список и
      содержимое вложений у «ДокументPDF», добавить вложение и
      сохранить документ (имена методов, арности и форму результата —
      измерить); электронные подписи — честная ошибка
      «не поддерживается». Круговые пробы в measure-скрипте задачи:
      платформа кладёт вложение — мы читаем; мы кладём — платформа
      читает.
- [ ] (pdf-measure) сводный tests/conformance/measure/measure-pdf.bsl
      по всем открытым `PDF.*` вопросам — по пяти правилам из
      AGENTS.md (одна строка `М()` на ID, литеральные ID, всё
      рискованное в `Попытка`, скрипт обязан исполняться и платформой,
      и этим интерпретатором); прогон через run-on-1c.sh, построчный
      дифф платформенного вывода, закрытие вопросов переносом якорей в
      `MEASURED_ANCHORS`.

## Closed

Этапы 1–5 (этап 0 — JSON — выполнен и измерен раньше, до этого
бэклога).

- [x] (vm-call-by-name) [hard] этап 1: вызов BSL-функции по имени из рантайма
      — узкий API поверх машинерии `Выполнить`/`Вычислить` в bsl-vm (кэш
      скомпилированных фрагментов уже есть, см. `run_isolated` и
      `CompiledSnippet`); юнит-тесты на вызов процедуры и функции по имени,
      передачу аргументов, ошибку на неизвестном имени.
- [x] (json-callbacks) [hard] функции преобразования/восстановления
      `ПрочитатьJSON`/`ЗаписатьJSON` поверх vm-call-by-name: снять заглушки
      «появятся позже» в `bsl-rt/src/builtin.rs` / `json.rs`. Семантика — по
      статье (глава 16.2): функция преобразования зовётся только для
      несериализуемых значений, есть параметр `Отказ`; порядок обхода функции
      восстановления описан в статье таблицей на 24 позиции — оформить его
      фикстурой и снять её `.expected` прогоном на платформе.
- [x] (bin-binary-data) этап 2: `ДвоичныеДанные` — иммутабельный буфер,
      `Размер()`, `РазделитьДвоичныеДанные`/`СоединитьДвоичныеДанные`,
      конструктор из файла.
- [x] (bin-buffer) [hard] `БуферДвоичныхДанных`: фиксированный размер,
      индексация по байтам, `ПрочитатьЦелое8/16/32/64` и парные записи,
      `ПорядокБайтов`, `Разделить`/`Соединить`, побитовые методы
      (`ЗаписатьПобитовоеИ`/`Или`/`ИсключающееИли`/`ИНе`, `Инвертировать`).
- [x] (bin-streams) потоки: `ПотокВПамяти`, `ФайловыйПоток`, менеджер
      `ФайловыеПотоки`, `Перейти`/`ТекущаяПозиция`/`Размер`/`ДоступнаЗапись`/
      `ДоступноЧтение`/`ДоступноИзменениеПозиции`; перечисления
      `РежимОткрытияФайла`, `ДоступКФайлу`, `ПозицияВПотоке`.
- [x] (bin-rw) [hard] `ЧтениеДанных`/`ЗаписьДанных` с собственными буферами;
      чередование с прямой сменой позиции потока обязано давать исключение
      (поведение платформы, описано в главе 16.3.6 — но текст ошибки и
      границы не измерены: маркеры).
- [x] (bin-global-fns) глобальные функции: `ЧислоИзШестнадцатеричнойСтроки`,
      `ЧислоИзДвоичнойСтроки`, `ПроверитьБит`, `ПроверитьПоБитовойМаске`,
      `УстановитьБит`, `ПобитовоеИ`/`Или`/`Не`/`ИНе`/`ИсключительноеИли`,
      `ПобитовыйСдвигВправо`/`Влево` (операнды — 32-разрядные беззнаковые,
      иначе исключение), `ПолучитьДвоичныеДанныеИзСтроки`/
      `ПолучитьБуферДвоичныхДанныхИзСтроки` и обратные — кодировки через
      существующий `bsl-rt/src/encoding.rs`.
- [x] (bin-measure) `tests/conformance/measure/measure-binary.bsl`: пробы по
      каждому заведённому `BIN.*` вопросу, по правилам из AGENTS.md (одна
      строка `М()` на ID, литеральные ID, всё рискованное — в `Попытка`,
      скрипт обязан исполняться и этим интерпретатором).
- [x] (dom-model) [hard] DOM на чтение поверх `ЧтениеXML`:
      `ПостроительDOM` (чтение из `ЧтениеXML` и из файла — состав методов
      измерить), `ДокументDOM`, модель узлов (элемент, атрибут, текст,
      комментарий, CDATA, инструкция обработки), навигация по дереву,
      доступ к атрибутам, пространства имён (URI/префикс/локальное имя).
      Область `DOM.*`; фикстура с `.expected`, снятым с платформы.
- [x] (dom-write) создание и мутация DOM (создание узлов у
      `ДокументDOM`, вставка и удаление — состав измерить) и сериализация
      дерева обратно в `ЗаписьXML` (имя типа-писателя измерить). Круговая
      фикстура XML -> DOM -> XML, эталон с платформы.
- [x] (xsd-parse) [hard] разбор XSD через DOM в объектную модель:
      `СхемаXML`, `НаборСхемXML`, `ПостроительСхемXML`; объявления
      элементов и атрибутов, простые и комплексные типы, наследование,
      фасеты (длины, границы, перечисление; `pattern` хранить строкой —
      движка регулярных выражений в дереве нет, применение фасета
      отложено до xdto-validation). Область `XSD.*`.
- [x] (xdto-type-model) модель типов поверх схемы: `ТипЗначенияXDTO`,
      `ТипОбъектаXDTO`, `СвойствоXDTO`; маппинг встроенных типов XML
      Schema (строка, числа, дата, булево, base64Binary) в типы BSL — по
      пробам на платформе, не по спецификации W3C.
- [x] (xdto-factory) [hard] `ФабрикаXDTO` и `СоздатьФабрикуXDTO` (из
      пути к XSD, из `СхемаXML` и `НаборСхемXML` — арности измерить);
      поиск типа по URI пространства имён и имени; создание экземпляров.
      Глобальное свойство `ФабрикаXDTO` — честная ошибка про отсутствие
      метаданных конфигурации («Что сознательно не делаем» в плане).
- [x] (xdto-instances) экземпляры: `ОбъектXDTO` (чтение и установка
      свойств, неустановленные значения, множественные свойства),
      `СписокXDTO`, `ПоследовательностьXDTO`, `ЗначениеXDTO` для простых
      типов; семантику доступа к отсутствующему свойству — измерить.
- [x] (xdto-xpath) [hard] подмножество XPath над DOM: метод вычисления
      выражения (у документа или узла — измерить), разыменователь
      пространств имён, тип результата и обход — пробами. Границы
      поддержанной грамматики зафиксировать явно: неподдержанное —
      честная ошибка, а не молчаливый пустой результат. Область
      `XPATH.*`.
- [x] (xdto-xml-io) [hard] `ПрочитатьXML`/`ЗаписатьXML` фабрики: разбор
      XML в `ОбъектXDTO`/`ЗначениеXDTO` по типам схемы и запись обратно;
      формы значений (даты, десятичные, base64) — построчно с
      платформой. Круговая фикстура на составном документе с вложенными
      объектами.
- [x] (xdto-serializer) `СериализаторXDTO` поверх фабрики из XSD:
      `ЗаписатьXML`/`ПрочитатьXML` значений BSL; для типов, которым
      нужны метаданные конфигурации, — явная диагностика
      «не поддерживается».
- [x] (xdto-validation) правила проверки (16.1.7): метод проверки у
      типов и экземпляров (имя измерить), проверка фасетов при чтении и
      записи; фасет `pattern` без движка регулярных выражений — честная
      ошибка «не поддерживается», не молчаливый пропуск.
- [x] (xdto-measure) сводный tests/conformance/measure/measure-xdto.bsl
      по всем открытым `DOM.*`/`XSD.*`/`XDTO.*`/`XPATH.*` вопросам — по
      пяти правилам из AGENTS.md; закрытие замерами, перенос анкеров в
      `MEASURED_ANCHORS`.
- [x] (regex-engine) [hard] этап 3: собственный backtracking-движок
      регулярных выражений в новом `bsl-rt/src/regex.rs`, без внешних
      крейтов и пока без поверхности BSL. Подмножество ICU по плану:
      литералы и экранирование, `.`, классы `[...]`/`[^...]` с
      диапазонами, `\d`/`\w`/`\s` и их отрицания, `\b`/`\B`, якоря
      `^`/`$`, кванторы `*`/`+`/`?`/`{n}`/`{n,}`/`{n,m}` — жадные и
      ленивые, группы захвата и незахватывающие `(?:...)`, альтернация
      `|`, флаги `(?i)`/`(?m)`, Unicode-свойства как минимум
      `\p{L}`/`\p{Nd}`. Совпадение по кодовым точкам поверх
      UTF-16-строк рантайма (суррогатная пара — одна точка); ошибка
      разбора шаблона — RtError, не panic. Юнит-тесты на каждый
      элемент грамматики, включая жадность/ленивость и порядок групп
      (первая, затем её вложенные, затем вторая — слева направо).
      Семантические решения без замера — как всегда, тройка
      `НЕ ИЗМЕРЕНО(REGEX.*)`; пробы, которым нужна ещё не существующая
      поверхность BSL, — через `Вычислить` в `Попытка`, чтобы
      measure-all исполнялся и платформой, и этим интерпретатором.
- [x] (regex-builtins) [hard] поверхность BSL поверх движка: глобальные
      функции `СтрНайтиПоРегулярномуВыражению`,
      `СтрНайтиВсеПоРегулярномуВыражению`,
      `СтрЗаменитьПоРегулярномуВыражению`,
      `СтрПодобнаПоРегулярномуВыражению` (арности, необязательные
      параметры и английские синонимы — измерить на платформе, не
      выдумывать) и типы результатов
      `РезультатПоискаПоРегулярномуВыражению` /
      `ГруппаРезультатаПоискаПоРегулярномуВыражению` (состав свойств,
      `ПолучитьГруппы()` и порядок групп в нём — измерить; создаются
      ли они через `Новый` — проверить пробой и лишь тогда заводить в
      `NEW_TYPES`). Через все таблицы-единственные-источники из
      AGENTS.md. `СтрЗаменитьПоРегулярномуВыражению` не поддерживает
      ссылки на группы в строке замены — воспроизводить, не
      «улучшать». Фикстура `regex` с `.expected`, снятым прогоном на
      платформе: корпус — 14 примеров из 16.5.4, а если текст статьи
      недоступен — представительные пробы по каждому элементу
      подмножества (классы, жадные и ленивые кванторы, вложенные
      группы, альтернация, якоря с `(?m)`, `(?i)` на кириллице,
      `\p{L}`/`\p{Nd}`, замена со спецсимволами в строке замены,
      полное и частичное совпадение у `СтрПодобна…`).
- [x] (regex-measure) сводный
      tests/conformance/measure/measure-regex.bsl по всем открытым
      `REGEX.*` вопросам — по пяти правилам из AGENTS.md (одна строка
      `М()` на ID, литеральные ID, всё рискованное в `Попытка`, скрипт
      обязан исполняться и платформой, и этим интерпретатором); прогон
      через run-on-1c.sh, построчный дифф платформенного вывода,
      закрытие вопросов переносом якорей в `MEASURED_ANCHORS`.
- [x] (zip-inflate) [hard] этап 4: inflate (RFC 1951) в bsl-rt рядом с
      `deflate.rs` — сжатие там уже есть, распаковки нет. Все три вида
      блоков: без сжатия, фиксированные и динамические коды Хаффмана;
      окно 32К, длины и дистанции по спецификации. Испорченный поток —
      RtError с внятным текстом, не panic и не бесконечный цикл (лимит
      на размер выхода обязателен). Юнит-тесты: круговой прогон через
      наш же deflate на текстах и несжимаемом шуме, поток с каждым
      видом блока, обрезанный и битый вход; эталонные байты сверить с
      готовым инструментом (python3 zlib), команду происхождения — в
      комментарий, как у таблиц Unicode движка регулярных выражений.
      Поверхности BSL в этой задаче нет.
- [x] (zip-reader) [hard] читатель контейнера ZIP в `zip.rs` (сейчас
      там только писатель для XLSX): поиск EOCD с комментарием архива,
      central directory, локальные заголовки, data descriptor, Zip64
      (записи и каталог), способы хранения 0 (stored) и 8 (deflate —
      из zip-inflate), проверка CRC-32, кодировки имён (бит UTF-8
      против однобайтовой; какую кодовую страницу берёт платформа —
      вопрос zip-read-api, здесь хранить сырые байты), флаг
      «зашифрован» — распознавать и отдавать честную ошибку, без
      расшифровки. Rust-API уровня крейта и юнит-тесты на архивах,
      собранных нашим писателем и внешним инструментом (python3
      zipfile, происхождение в комментарии).
- [x] (zip-read-api) [hard] поверхность BSL чтения: `ЧтениеФайлаАрхива`
      (конструкторы — из имени файла, из потока или двоичных данных;
      состав и арности измерить), коллекция элементов и
      `ЭлементФайлаАрхива` со свойствами, включая пары `Исходное*`,
      подстановку `_` в именах и разрешение дублей — семантику каждого
      измерить на платформе, не выдумывать; методы извлечения и
      закрытия (имена и арности измерить), перечисления чтения — что
      найдётся пробами (`РежимВосстановленияПутейФайловZIP` и соседи).
      Через все таблицы-единственные-источники; новые опкоды — через
      text.rs, round-trip корпус и классификацию эффектов в bundle.rs.
      Фикстура `zip-read` с эталоном, снятым прогоном на платформе;
      входной архив-фикстура лежит в дереве с записанным
      происхождением.
- [x] (zip-write-api) [hard] поверхность BSL записи:
      `ЗаписьФайлаАрхива` (конструкторы и арности измерить), `Добавить`
      с маской и рекурсией, три режима сохранения путей, `Записать()`,
      `ПолучитьДвоичныеДанные()`, перечисления записи (`МетодСжатияZIP`,
      `УровеньСжатияZIP`, `РежимСохраненияПутейZIP`; шифрование — только
      честная ошибка «не поддерживается») поверх существующего
      deflate-писателя. Перекрёстный критерий обязателен: архив,
      собранный нами, платформа читает своим `ЧтениеФайлаАрхива` —
      список имён, размеры и CRC совпадают (проба в measure-скрипте
      задачи); фикстура `zip-write` с эталоном с платформы.
- [x] (zip-measure) сводный tests/conformance/measure/measure-zip.bsl
      по всем открытым `ZIP.*` вопросам — по пяти правилам из AGENTS.md
      (одна строка `М()` на ID, литеральные ID, всё рискованное в
      `Попытка`, скрипт обязан исполняться и платформой, и этим
      интерпретатором); прогон через run-on-1c.sh, построчный дифф
      платформенного вывода, закрытие вопросов переносом якорей в
      `MEASURED_ANCHORS`.

## Notes for the loop

- Платформа 1С на машине ЕСТЬ — измеряй, а не предполагай. Полный цикл:
  проба в measure-скрипт -> `./tests/conformance/measure/1c/run-on-1c.sh
  <скрипт>` (~6 секунд; окружение и ручки — в шапке самого скрипта) ->
  построчный дифф платформенного вывода с выводом этого интерпретатора ->
  закрытие вопроса переносом из `OPEN_QUESTIONS` в `MEASURED_ANCHORS`.
  `.expected` для фикстур снимается ТОЛЬКО прогоном фикстуры на платформе
  (`run-on-1c.sh путь/фикстура.bsl` кладёт `<фикстура>.platform.txt` рядом)
  и НИКОГДА — выводом этого интерпретатора. Решение, которое замером
  закрыть не удалось, — тройка `НЕ ИЗМЕРЕНО(ID)` + запись в
  `crates/bsl-rt/src/open_questions.rs` + одна строка в measure-скрипте
  (реестровый тест `open_questions_registry_matches_source_markers`
  проверяет все три стороны).
- Грабли платформы, уже стоившие таймаутов: встроенную ФУНКЦИЮ (например,
  `Вычислить`) нельзя вызвать как процедуру-оператор — ошибка компиляции
  всего модуля, симптом «пустой вывод, exit 0»; необработанное исключение
  и модальное окно из прогона выглядят одинаково — таймаут 180 с, поэтому
  каждую пробу — в `Попытка`. `--ingest-measurements` пишет `platform.tsv`
  РЯДОМ с поданным файлом: ингест `measure-*.platform.txt` из каталога
  measure затирает оракул `measure-all`.
- Каждый новый тип проходит все таблицы-единственные-источники:
  `BUILTIN_FN_NAMES`/`BUILTIN_METHOD_NAMES` + `arity_range` +
  `call_builtin_fn` в builtin.rs, `NEW_TYPES` в resolver.rs, `enums.rs`;
  новые опкоды — ещё и `text.rs` (`write_instr`/`parse_instr`/`OPCODES`) с
  классификацией эффектов в `bundle.rs`.
- Перед сдачей задачи: `cargo fmt --all -- --check`, `cargo clippy
  --workspace --all-targets -- -D warnings`, `cargo test --workspace` —
  все зелёные. Комментарии и rustdoc — русской технической прозой.
- Prefer empirical fixtures captured from the real platform over doc claims.
- Numeric/format tasks assert exact strings (NBSP separator, stripped trailing
  zeros), not approximate equality.
- Фикстура `json-dates.expected` снята с платформы в зоне Europe/Moscow и
  зависит от часового пояса машины (см. её шапку): если конформанс-тест
  падает ТОЛЬКО на ней на машине с другой зоной — это смена исходных данных
  пробы, а не регрессия; не «чинить» код под неё.
