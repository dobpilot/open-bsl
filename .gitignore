/target
*.svg
perf.data
# напечатанный байт-код (--emit-bytecode) — артефакт, не исходник
*.bslc
# личные настройки Claude Code для этого проекта — не общекомандные
/.claude/settings.local.json
# памятка для Claude Code — тоже личная, а не общекомандная: конвенции
# проекта живут в AGENTS.md
/CLAUDE.md
