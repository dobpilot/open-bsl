// Точка входа замерного прогона. Платформа запускается БЕЗ /Execute — сама
// обработка «Замеры» и её форма живут в конфигурации одноразовой ИБ (см.
// шапку run-on-1c.sh), поэтому старт задаём стандартным обработчиком
// приложения, а не параметром командной строки.
Процедура ПриНачалеРаботыСистемы(Отказ, ПараметрыЗапуска)
	ОткрытьФорму("Обработка.Замеры.Форма");
КонецПроцедуры
