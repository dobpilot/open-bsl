Перем ЧислоОшибочныхОбработчиков;
Перем ТикиОшибочныхОбработчиков;
Перем ФайлОшибочныхОбработчиков;

Процедура ЗавершитьОшибочныеОбработчики() Экспорт
    ТикиОшибочныхОбработчиков = ТикиОшибочныхОбработчиков + 1;
    Если ТикиОшибочныхОбработчиков < 5 Тогда Возврат; КонецЕсли;
    Сообщить("callback.count" + Символ(9) + Строка(ЧислоОшибочныхОбработчиков));
    Сообщить("file.end" + Символ(9) + "file-begin-handler-errors-1");
    Попытка Выполнить("ЗавершитьРаботуСистемы(Ложь);"); Исключение КонецПопытки;
КонецПроцедуры

Процедура НеожиданнаяОшибкаОперации(Знач ИнформацияОшибочногоОбработчика, СтандартнаяОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    Сообщить("callback.rerouted" + Символ(9) + ДанныеОшибочногоОбработчика);
    СтандартнаяОшибочногоОбработчика = Ложь;
КонецПроцедуры

Процедура ОшибкаСинхПроцедуры(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    ВызватьИсключение "handler-error:procedure";
КонецПроцедуры

Функция ОшибкаСинхФункции(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    ВызватьИсключение "handler-error:function";
КонецФункции

Асинх Процедура ОшибкаАсинхПроцедурыДо(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    ВызватьИсключение "handler-error:async.procedure.before";
КонецПроцедуры

Асинх Функция ОшибкаАсинхФункцииДо(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    ВызватьИсключение "handler-error:async.function.before";
КонецФункции

Асинх Процедура ОшибкаАсинхПроцедурыПосле(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    СуществуетОшибочногоОбработчика = Ждать ФайлОшибочныхОбработчиков.СуществуетАсинх();
    Сообщить("callback.await" + Символ(9) + ДанныеОшибочногоОбработчика + "|" + ?(СуществуетОшибочногоОбработчика, "1", "0"));
    ВызватьИсключение "handler-error:async.procedure.after";
КонецПроцедуры

Асинх Функция ОшибкаАсинхФункцииПосле(Знач РезультатОшибочногоОбработчика, Знач ДанныеОшибочногоОбработчика) Экспорт
    ЧислоОшибочныхОбработчиков = ЧислоОшибочныхОбработчиков + 1;
    Сообщить("callback.enter" + Символ(9) + ДанныеОшибочногоОбработчика);
    СуществуетОшибочногоОбработчика = Ждать ФайлОшибочныхОбработчиков.СуществуетАсинх();
    Сообщить("callback.await" + Символ(9) + ДанныеОшибочногоОбработчика + "|" + ?(СуществуетОшибочногоОбработчика, "1", "0"));
    ВызватьИсключение "handler-error:async.function.after";
КонецФункции

Сообщить("file.begin" + Символ(9) + "file-begin-handler-errors-1");
ЧислоОшибочныхОбработчиков = 0;
ТикиОшибочныхОбработчиков = 0;
ФайлОшибочныхОбработчиков = Новый Файл("/open-bsl-handler-errors-no-file-20260917");
Попытка
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаСинхПроцедуры", ЭтотОбъект, "procedure", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаСинхФункции", ЭтотОбъект, "function", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаАсинхПроцедурыДо", ЭтотОбъект, "async.procedure.before", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаАсинхФункцииДо", ЭтотОбъект, "async.function.before", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаАсинхПроцедурыПосле", ЭтотОбъект, "async.procedure.after", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    ФайлОшибочныхОбработчиков.НачатьИнициализацию(Новый ОписаниеОповещения("ОшибкаАсинхФункцииПосле", ЭтотОбъект, "async.function.after", "НеожиданнаяОшибкаОперации", ЭтотОбъект), ФайлОшибочныхОбработчиков.ПолноеИмя);
    Сообщить("callback.return" + Символ(9) + Строка(ЧислоОшибочныхОбработчиков));
    Попытка Выполнить("ПодключитьОбработчикОжидания(""ЗавершитьОшибочныеОбработчики"", 1);"); Исключение КонецПопытки;
Исключение
    Сообщить("file.setup_error" + Символ(9) + ИнформацияОбОшибке().Описание);
    ТикиОшибочныхОбработчиков = 5;
    ЗавершитьОшибочныеОбработчики();
КонецПопытки;
