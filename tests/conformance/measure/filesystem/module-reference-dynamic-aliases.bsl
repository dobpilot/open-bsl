Перем ОбщаяСвязьПробы;

Процедура ВложеннаяДинамическаяСвязь(АргументСвязи)
    Выполнить("АргументСвязи = 11; Сообщить(""dynamic.module.inside"" + Символ(9) + Строка(ОбщаяСвязьПробы)); ОбщаяСвязьПробы = 12; Сообщить(""dynamic.parameter.inside"" + Символ(9) + Строка(АргументСвязи));");
    Сообщить("dynamic.parameter.after" + Символ(9) + Строка(АргументСвязи));
КонецПроцедуры

Процедура ВнешняяСвязьПробы(АргументСвязи)
    ВложеннаяДинамическаяСвязь(АргументСвязи);
    Сообщить("nested.parameter.after" + Символ(9) + Строка(АргументСвязи));
    Попытка
        Выполнить("АргументСвязи = 31; ВызватьИсключение ""ошибка пробы"";");
    Исключение
    КонецПопытки;
    Сообщить("dynamic.parameter.error" + Символ(9) + Строка(АргументСвязи));
    Сообщить("dynamic.module.error" + Символ(9) + Строка(ОбщаяСвязьПробы));
КонецПроцедуры

Процедура ЗапуститьСвязьПробы()
    ОбщаяСвязьПробы = 1;
    ВнешняяСвязьПробы(ОбщаяСвязьПробы);
    Сообщить("dynamic.module.after" + Символ(9) + Строка(ОбщаяСвязьПробы));
КонецПроцедуры

Сообщить("file.begin" + Символ(9) + "module-reference-dynamic-aliases-1");
Попытка
    ЗапуститьСвязьПробы();
Исключение
    Сообщить("file.setup_error" + Символ(9) + ИнформацияОбОшибке().Описание);
КонецПопытки;
Сообщить("file.end" + Символ(9) + "module-reference-dynamic-aliases-1");
