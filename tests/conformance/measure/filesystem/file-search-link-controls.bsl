// Только чтение подготовленного дерева; все цели ссылок принадлежат этой пробе.
Процедура ПробаПоискаСсылок(ИдСсылок, ПутьСсылок, МаскаСсылок, РекурсияСсылок, КореньСсылок)
	Попытка
		НайденныеСсылки = НайтиФайлы(ПутьСсылок, МаскаСсылок, РекурсияСсылок);
		ТекстСсылок = "ok|count=" + Строка(НайденныеСсылки.Количество());
		Для Каждого ЭлементСсылок Из НайденныеСсылки Цикл
			ТекстСсылок = ТекстСсылок + "|" + СтрЗаменить(ЭлементСсылок.ПолноеИмя, КореньСсылок, "<root>");
		КонецЦикла;
		Сообщить(ИдСсылок + Символ(9) + ТекстСсылок);
	Исключение
		Сообщить(ИдСсылок + Символ(9) + "error|" + ИнформацияОбОшибке().Описание);
	КонецПопытки;
КонецПроцедуры

КореньСсылок = "/tmp/open-bsl-find-link-controls-NhS1hY/tree";
Сообщить("file.begin" + Символ(9) + "file-search-link-controls-1");
ПробаПоискаСсылок("controls.plain.flat", КореньСсылок + "/plain", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.plain.recursive", КореньСсылок + "/plain", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.plain.flat.txt", КореньСсылок + "/plain", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.plain.recursive.txt", КореньСсылок + "/plain", "*.txt", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.file.flat", КореньСсылок + "/file", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.file.recursive", КореньСсылок + "/file", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.file.flat.txt", КореньСсылок + "/file", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.file.recursive.txt", КореньСсылок + "/file", "*.txt", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.dir.flat", КореньСсылок + "/dir", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.dir.recursive", КореньСсылок + "/dir", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.dir.flat.txt", КореньСсылок + "/dir", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.dir.recursive.txt", КореньСсылок + "/dir", "*.txt", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.broken.flat", КореньСсылок + "/broken", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.broken.recursive", КореньСсылок + "/broken", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.broken.flat.txt", КореньСсылок + "/broken", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.broken.recursive.txt", КореньСсылок + "/broken", "*.txt", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.valid.flat", КореньСсылок + "/valid", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.valid.recursive", КореньСсылок + "/valid", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.valid.flat.txt", КореньСсылок + "/valid", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.valid.recursive.txt", КореньСсылок + "/valid", "*.txt", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.mixed.flat", КореньСсылок + "/mixed", "*", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.mixed.recursive", КореньСсылок + "/mixed", "*", Истина, КореньСсылок);
ПробаПоискаСсылок("controls.mixed.flat.txt", КореньСсылок + "/mixed", "*.txt", Ложь, КореньСсылок);
ПробаПоискаСсылок("controls.mixed.recursive.txt", КореньСсылок + "/mixed", "*.txt", Истина, КореньСсылок);
Сообщить("file.end" + Символ(9) + "file-search-link-controls-1");
