// Замеры `ТекстовыйДокумент`, снимаемые на 8.3.27.
//
//   ./tests/conformance/measure/1c/run-on-1c.sh \
//       tests/conformance/measure/measure-textdoc.bsl
//
// Правила те же, что у соседних скриптов: строка `ИД<таб>ЗНАЧЕНИЕ`, скрипт
// исполняется И НАШИМ интерпретатором, отказ — маркером `<ошибка>`.
//
// Пробы написаны прямым кодом: тип разбирается обеими сторонами. Снимался
// замер иначе — через `Выполнить`, потому что до реализации `Новый
// ТекстовыйДокумент` наш резолвер отвергал на этапе компиляции и сорвал бы
// весь прогон.

Перем Ответ;

Функция Видимо(Текст)
	Р = СтрЗаменить(Текст, Символ(13), "<ВК>");
	Р = СтрЗаменить(Р, Символ(10), "<ПС>");
	Р = СтрЗаменить(Р, Символ(9), "<Т>");
	Возврат Р;
КонецФункции

Процедура М(Ид, Значение)
	Сообщить(Ид + Символ(9) + Видимо(Строка(Значение)));
КонецПроцедуры

// Исполняет фрагмент, который обязан положить результат в `Ответ`.
Функция Код(Фрагмент)
	Ответ = "<ошибка>";
	Попытка
		Выполнить(Фрагмент);
	Исключение
	КонецПопытки;
	Возврат Ответ;
КонецФункции

// Значение выражения либо `<ошибка>`.
Функция Проба(Выражение)
	Попытка
		Возврат Строка(Вычислить(Выражение));
	Исключение
		Возврат "<ошибка>";
	КонецПопытки;
КонецФункции


// =====================================================================
// ИМЕНА ТИПА И ЗНАЧЕНИЯ
// =====================================================================

Р = "<ошибка>";
Попытка
	Р = Строка(ТипЗнч(Новый ТекстовыйДокумент));
Исключение
КонецПопытки;
М("TEXTDOC.TYPE_NAME", Р);
Р = "<ошибка>";
Попытка
	Р = Строка(Новый ТекстовыйДокумент);
Исключение
КонецПопытки;
М("TEXTDOC.VALUE_NAME", Р);

// =====================================================================
// ТЕКСТ ЦЕЛИКОМ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.SET_GET", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Ответ = "[" + Д.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.EMPTY_TEXT", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.EMPTY_COUNT", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.COUNT_TWO_LINES", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10));
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.COUNT_TRAILING_NEWLINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст(Символ(10));
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.COUNT_ONLY_NEWLINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("");
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.COUNT_EMPTY_STRING", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(13) + Символ(10) + "два");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.CRLF_ROUND_TRIP", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(13) + Символ(10) + "два");
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.CRLF_COUNT", Ответ);

// =====================================================================
// СТРОКИ ПО НОМЕРАМ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Ответ = Д.ПолучитьСтроку(1);
Исключение
КонецПопытки;
М("TEXTDOC.GET_LINE_FIRST", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Ответ = Д.ПолучитьСтроку(2);
Исключение
КонецПопытки;
М("TEXTDOC.GET_LINE_SECOND", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Ответ = "[" + Д.ПолучитьСтроку(0) + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_LINE_ZERO", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Ответ = "[" + Д.ПолучитьСтроку(5) + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_LINE_BEYOND", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.ДобавитьСтроку("раз");
	Д.ДобавитьСтроку("два");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.ADD_LINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.ДобавитьСтроку("раз");
	Д.ДобавитьСтроку("два");
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.ADD_LINE_COUNT", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.ДобавитьСтроку("раз" + Символ(10) + "два");
	Ответ = Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.ADD_LINE_WITH_NEWLINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "три");
	Д.ВставитьСтроку(2, "два");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.INSERT_LINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Д.ЗаменитьСтроку(1, "ноль");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.REPLACE_LINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Д.УдалитьСтроку(1);
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.DELETE_LINE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Д.Очистить();
	Ответ = "[" + Д.ПолучитьТекст() + "] " + Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.CLEAR", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Д.ВставитьСтроку(9, "девять");
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.INSERT_BEYOND", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Д.УдалитьСтроку(9);
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.DELETE_BEYOND", Ответ);

// =====================================================================
// МАКЕТЫ И ОБЛАСТИ — ГЛАВНЫЙ ВОПРОС
// =====================================================================

Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("#Область Шапка" + Символ(10) + "привет" + Символ(10) + "#КонецОбласти");
	О = Д.ПолучитьОбласть("Шапка");
	Ответ = Строка(ТипЗнч(О)) + ": " + О.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("#Region Head" + Символ(10) + "hi" + Символ(10) + "#EndRegion");
	О = Д.ПолучитьОбласть("Head");
	Ответ = Строка(ТипЗнч(О)) + ": " + О.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_EN", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("просто текст");
	Ответ = Строка(ТипЗнч(Д.ПолучитьОбласть("НетТакой")));
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_MISSING", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два" + Символ(10) + "три");
	О = Д.ПолучитьОбласть(2, 3);
	Ответ = Строка(ТипЗнч(О)) + ": " + О.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_BY_LINES", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("привет, [Имя]");
	Д.Параметры.Имя = "мир";
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.PARAMETERS", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Ответ = Строка(ТипЗнч(Д.Параметры));
Исключение
КонецПопытки;
М("TEXTDOC.PARAMETERS_TYPE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Ист = Новый ТекстовыйДокумент;
	Ист.УстановитьТекст("кусок");
	Д.Вывести(Ист);
	Ответ = Д.ПолучитьТекст();
Исключение
КонецПопытки;
М("TEXTDOC.OUTPUT_AREA", Ответ);

// =====================================================================
// МАКЕТЫ: ГРАНИЦЫ ОБЛАСТЕЙ И ПОДСТАНОВКА
// =====================================================================

Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("до" + Символ(10) + "#Область Тело" + Символ(10) + "внутри" + Символ(10) + "#КонецОбласти" + Символ(10) + "после");
	Ответ = "[" + Д.ПолучитьОбласть("Тело").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_SUBSET", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("#Область А" + Символ(10) + "первая" + Символ(10) + "#КонецОбласти" + Символ(10) + "#Область Б" + Символ(10) + "вторая" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + Д.ПолучитьОбласть("Б").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_TWO", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("#Область Шапка" + Символ(10) + "привет" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + Д.ПолучитьОбласть("шапка").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_CASE", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("до" + Символ(10) + "#Область Тело" + Символ(10) + "внутри" + Символ(10) + "#КонецОбласти");
	Ответ = Д.ПолучитьОбласть("Тело").КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.AREA_COUNT", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "строка" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.OUTPUT_OF_AREA", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "строка" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.OUTPUT_TWICE", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "привет, [Имя]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	О.Параметры.Имя = "мир";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_ON_OUTPUT", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "привет, [Имя]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_MISSING", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "текст" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	О.Параметры.НетТакого = "х";
	Ответ = "установлен";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_UNKNOWN_NAME", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Тело" + Символ(10) + "итого [Сумма]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Тело");
	О.Параметры.Сумма = 1000.5;
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_NUMBER", Ответ);

// =====================================================================
// МАКЕТЫ: ШИРИНА ПОЛЯ, НЕСКОЛЬКО ПАРАМЕТРОВ, ВЛОЖЕНИЕ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[Имя]|" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.Имя = "оченьдлинноезначение";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_LONGER", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[А] и [Б]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.А = "раз";
	О.Параметры.Б = "два";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_TWO", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[А]-[А]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.А = "х";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_REPEATED", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "привет, [Имя]" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.Имя = "мир";
	Ответ = "[" + О.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.PARAM_IN_AREA_TEXT", Ответ);
Ответ = "<ошибка>";
Попытка
	Ист = Новый ТекстовыйДокумент;
	Ист.УстановитьТекст("кусок");
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(Ист);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.OUTPUT_PLAIN_DOC", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Внешняя" + Символ(10) + "а" + Символ(10) + "#Область Внутренняя" + Символ(10) + "б" + Символ(10) + "#КонецОбласти" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + М.ПолучитьОбласть("Внутренняя").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_NESTED", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Внешняя" + Символ(10) + "а" + Символ(10) + "#Область Внутренняя" + Символ(10) + "б" + Символ(10) + "#КонецОбласти" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + М.ПолучитьОбласть("Внешняя").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_NESTED_OUTER", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#область Т" + Символ(10) + "тело" + Символ(10) + "#конецобласти");
	Ответ = "[" + М.ПолучитьОбласть("Т").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_DIRECTIVE_CASE", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("  #Область Т" + Символ(10) + "тело" + Символ(10) + "  #КонецОбласти");
	Ответ = "[" + М.ПолучитьОбласть("Т").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.AREA_INDENTED", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "первая" + Символ(10) + "вторая" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "] " + Р.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.OUTPUT_KEEPS_LINES", Ответ);

// =====================================================================
// ОСТАТКИ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(13) + Символ(10) + "два");
	Ответ = "[" + Д.ПолучитьСтроку(1) + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_LINE_CRLF", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Д.ДобавитьСтроку("два");
	Ответ = "[" + Д.ПолучитьТекст() + "] " + Д.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.ADD_AFTER_SET", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз");
	Д.ЗаменитьСтроку(9, "девять");
	Ответ = "[" + Д.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.REPLACE_BEYOND", Ответ);

// =====================================================================
// МАКЕТ: КОНСТРУКЦИИ ИЗ СТАТЬИ ИТС
// =====================================================================

Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[А]" + Символ(10) + "<[Б]>" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.А = "раз";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.ANGLE_LINE_EMPTY", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[А]" + Символ(10) + "<[Б]>" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.А = "раз";
	О.Параметры.Б = "два";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.ANGLE_LINE_FILLED", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[    ]Товар" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.Товар = "гвозди";
	Р = Новый ТекстовыйДокумент;
	Р.Вывести(О);
	Ответ = "[" + Р.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.EMPTY_BRACKETS", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область Т" + Символ(10) + "[    ]Товар" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("Т");
	О.Параметры.НетТакого = "х";
	Ответ = "установлен";
Исключение
КонецПопытки;
М("TEXTDOC.EMPTY_BRACKETS_UNKNOWN", Ответ);

// =====================================================================
// ПОВТОРНОЕ ИСПОЛЬЗОВАНИЕ ДОКУМЕНТА
// =====================================================================

Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область А" + Символ(10) + "тело" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("А");
	М.УстановитьТекст("другое");
	Ответ = "[" + М.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.SET_TEXT_AFTER_GET_AREA", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область А" + Символ(10) + "тело" + Символ(10) + "#КонецОбласти");
	О = М.ПолучитьОбласть("А");
	М.УстановитьТекст("#Область Б" + Символ(10) + "другое" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + М.ПолучитьОбласть("Б").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_AFTER_SET_TEXT", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область А" + Символ(10) + "тело" + Символ(10) + "#КонецОбласти");
	М.УстановитьТекст("#Область Б" + Символ(10) + "другое" + Символ(10) + "#КонецОбласти");
	Ответ = "[" + М.ПолучитьОбласть("Б").ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_AFTER_SET_TEXT_NO_AREA_TAKEN", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("#Область А" + Символ(10) + "тело" + Символ(10) + "#КонецОбласти");
	О1 = М.ПолучитьОбласть("А");
	О2 = М.ПолучитьОбласть("А");
	Ответ = "[" + О2.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.GET_AREA_TWICE", Ответ);
Ответ = "<ошибка>";
Попытка
	М = Новый ТекстовыйДокумент;
	М.УстановитьТекст("первый");
	М.УстановитьТекст("второй");
	Ответ = "[" + М.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("TEXTDOC.SET_TEXT_TWICE", Ответ);

// =====================================================================
// КОДИРОВКИ: ЧЛЕНЫ ПЕРЕЧИСЛЕНИЯ
// =====================================================================

М("ENC.MEMBER.UTF8", Проба("Строка(КодировкаТекста.UTF8)"));
М("ENC.MEMBER.UTF16", Проба("Строка(КодировкаТекста.UTF16)"));
М("ENC.MEMBER.ANSI", Проба("Строка(КодировкаТекста.ANSI)"));
М("ENC.MEMBER.OEM", Проба("Строка(КодировкаТекста.OEM)"));
М("ENC.MEMBER.SYSTEM", Проба("Строка(КодировкаТекста.Системная)"));
М("ENC.MEMBER.UTF16LE", Проба("Строка(КодировкаТекста.UTF16LE)"));
М("ENC.MEMBER.UTF16BE", Проба("Строка(КодировкаТекста.UTF16BE)"));
М("ENC.MEMBER.KOI8R", Проба("Строка(КодировкаТекста.KOI8R)"));
М("ENC.MEMBER.WINDOWS1251", Проба("Строка(КодировкаТекста.Windows1251)"));

// =====================================================================
// КОДИРОВКИ: ЧТО ЛОЖИТСЯ В ФАЙЛ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-default.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П);
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.DEFAULT", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-utf8.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, КодировкаТекста.UTF8);
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.UTF8", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-utf16.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, КодировкаТекста.UTF16);
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.UTF16", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-ansi.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, КодировкаТекста.ANSI);
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.ANSI", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-oem.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, КодировкаТекста.OEM);
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.OEM", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_utf8.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "UTF-8");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_UTF8", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_1251.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "windows-1251");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_1251", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_866.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "cp866");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_866", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_koi8.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "KOI8-R");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_KOI8", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_utf16le.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "UTF-16LE");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_UTF16LE", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-str_bad.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "нет-такой-кодировки");
	Ответ = "записан";
Исключение
КонецПопытки;
М("ENC.WRITE.STR_BAD", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-rt.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "windows-1251");
	Ч = Новый ТекстовыйДокумент;
	Ч.Прочитать(П, "windows-1251");
	Ответ = Ч.ПолучитьТекст();
Исключение
КонецПопытки;
М("ENC.ROUND_TRIP_1251", Ответ);
Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-enc-rt2.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("Аб");
	Д.Записать(П, "windows-1251");
	Ч = Новый ТекстовыйДокумент;
	Ч.Прочитать(П, "UTF-8");
	Ответ = "[" + Ч.ПолучитьТекст() + "]";
Исключение
КонецПопытки;
М("ENC.READ_WRONG", Ответ);

// =====================================================================
// ФАЙЛЫ
// =====================================================================

Ответ = "<ошибка>";
Попытка
	П = "/tmp/open-bsl-measure-textdoc.txt";
	Д = Новый ТекстовыйДокумент;
	Д.УстановитьТекст("раз" + Символ(10) + "два");
	Д.Записать(П);
	Ч = Новый ТекстовыйДокумент;
	Ч.Прочитать(П);
	Ответ = "[" + Ч.ПолучитьТекст() + "] " + Ч.КоличествоСтрок();
Исключение
КонецПопытки;
М("TEXTDOC.WRITE_READ", Ответ);
Ответ = "<ошибка>";
Попытка
	Д = Новый ТекстовыйДокумент;
	Д.Прочитать("/tmp/open-bsl-нет-такого-файла.txt");
	Ответ = "прочитан";
Исключение
КонецПопытки;
М("TEXTDOC.READ_MISSING", Ответ);
