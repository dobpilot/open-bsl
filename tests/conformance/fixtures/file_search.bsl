// Количества из measure/filesystem/file-globals.platform.txt, find.*.
// Порядок выдачи намеренно не проверяется: его полный контракт не измерен.
КореньПоисковогоТеста = ПолучитьИмяВременногоФайла(".find");
Попытка
	СоздатьКаталог(КореньПоисковогоТеста + "/branch/leaf");
	ДанныеПоисковогоТеста = ПолучитьДвоичныеДанныеИзСтроки("x", КодировкаТекста.UTF8, Ложь);
	Для Каждого ИмяПоисковогоТеста Из СтрРазделить("a.txt,b.TXT,plain,.hidden,я.txt,branch/leaf/deep.txt", ",") Цикл
		ДанныеПоисковогоТеста.Записать(КореньПоисковогоТеста + "/" + ИмяПоисковогоТеста);
	КонецЦикла;
	Сообщить(НайтиФайлы(КореньПоисковогоТеста).Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/a.txt").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/missing").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "").Количество());
	Сообщить(FindFiles(КореньПоисковогоТеста, Неопределено).Count());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "*").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "*.*").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "?.txt").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "[a-z].*").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, "*.txt", Истина).Количество());
	// file-search-edges.platform.txt: find.missing.*, find.file.* и явное Undefined.
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/missing", "*").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/missing", "*", Истина).Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/a.txt", "*").Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста + "/a.txt", "*", Истина).Количество());
	Попытка
		РезультатГраницыПоиска = НайтиФайлы(КореньПоисковогоТеста, "*.txt", Неопределено);
		Сообщить("accepted");
	Исключение
		Сообщить("error");
	КонецПопытки;
	// file-search-types.platform.txt: преобразования маски и пропуски.
	ДанныеПоисковогоТеста.Записать(КореньПоисковогоТеста + "/17");
	ДанныеПоисковогоТеста.Записать(КореньПоисковогоТеста + "/Массив");
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, 17)[0].Имя);
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, Новый Массив)[0].Имя);
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, Null).Количество());
	Сообщить(НайтиФайлы(КореньПоисковогоТеста, , ).Количество());
Исключение
	УдалитьФайлы(КореньПоисковогоТеста);
	ВызватьИсключение;
КонецПопытки;
УдалитьФайлы(КореньПоисковогоТеста);
