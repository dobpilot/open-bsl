Процедура ИзменитьЛокальнуюПробы(ПараметрПробы, ДубликатПробы)
    Попытка
        Выполнить("ПараметрПробы = 31; Сообщить(""dynamic.local.alias.inside"" + Символ(9) + Строка(ДубликатПробы)); ВызватьИсключение ""ошибка пробы"";");
    Исключение
    КонецПопытки;
    Сообщить("dynamic.local.parameter.error" + Символ(9) + Строка(ПараметрПробы));
    Сообщить("dynamic.local.alias.error" + Символ(9) + Строка(ДубликатПробы));
КонецПроцедуры

Функция ИзменитьКопиюПробы(Знач КопияПробы)
    Попытка
        Выполнить("КопияПробы = 41; ВызватьИсключение ""ошибка пробы"";");
    Исключение
    КонецПопытки;
    Возврат КопияПробы;
КонецФункции

Процедура ЗапуститьЛокальныеПробы()
    ЛокальнаяПробы = 1;
    Попытка
        Выполнить("ЛокальнаяПробы = 11; ВызватьИсключение ""ошибка пробы"";");
    Исключение
    КонецПопытки;
    Сообщить("dynamic.local.direct.error" + Символ(9) + Строка(ЛокальнаяПробы));
    Попытка
        Выполнить("Выполнить(""ЛокальнаяПробы = 21;""); ВызватьИсключение ""ошибка пробы"";");
    Исключение
    КонецПопытки;
    Сообщить("dynamic.local.nested.error" + Символ(9) + Строка(ЛокальнаяПробы));
    ИзменитьЛокальнуюПробы(ЛокальнаяПробы, ЛокальнаяПробы);
    Сообщить("dynamic.local.caller.error" + Символ(9) + Строка(ЛокальнаяПробы));
    Сообщить("dynamic.local.byvalue.inside" + Символ(9) + Строка(ИзменитьКопиюПробы(ЛокальнаяПробы)));
    Сообщить("dynamic.local.byvalue.caller" + Символ(9) + Строка(ЛокальнаяПробы));
КонецПроцедуры

Сообщить("file.begin" + Символ(9) + "dynamic-local-exceptions-1");
Попытка
    ЗапуститьЛокальныеПробы();
Исключение
    Сообщить("file.setup_error" + Символ(9) + ИнформацияОбОшибке().Описание);
КонецПопытки;
Сообщить("file.end" + Символ(9) + "dynamic-local-exceptions-1");
