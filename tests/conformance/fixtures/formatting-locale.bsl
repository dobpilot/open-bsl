// Форматирование по умолчанию (Строка() без явной форматной строки) —
// локале-зависимое: группировка разрядов и разделитель NBSP, см.
// bsl-format::NBSP. Отдельно от arithmetic.bsl (канонический ЧГ=0; ЧРД=.),
// потому что расхождение здесь может значить баг локали, а не арифметики.
Сообщить(Строка(1000.5));
Сообщить(Строка(Число(Строка(1000000))));
Сообщить(Строка(Истина));
Сообщить(Строка(Новый Массив));
Сообщить(СтрДлина(Строка(1/3)));
