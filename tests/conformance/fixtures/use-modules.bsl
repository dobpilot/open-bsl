//@используй(modules/use-service.bsl как Служба)
// Расширение open-bsl: подключение файла общим модулем. Платформа тут не
// оракул — семантика задана планами bsl-use-modules и bsl-background-jobs:
// тело модуля выполняется до главного скрипта (post-order), экспортные
// символы доступны только квалифицированно, изменение переменной модуля
// видно последующим чтениям.
Сообщить(Служба.Удвоить(Служба.Счётчик));
Сообщить(Служба.Счётчик);
Служба.Счётчик = Служба.Счётчик * 100;
Сообщить(Служба.Удвоить(Служба.Счётчик));
