// Oracle: filesystem/file-async-write.platform.txt, 28 проб setters.
Асинх Функция ПробаЗаписиАсинх(ИдЗаписи, ПутьЗаписи, ВызовЗаписи, ЧтениеЗаписи, КореньЗаписи)
	ФайлЗаписиПробы = Новый Файл(ПутьЗаписи);
	СтадияЗаписи = "call";
	ТипОбещанияЗаписи = "none";
	Попытка
		ОбещаниеЗаписи = Вычислить("ФайлЗаписиПробы." + ВызовЗаписи);
		ТипОбещанияЗаписи = Строка(ТипЗнч(ОбещаниеЗаписи));
		СтадияЗаписи = "await";
		РезультатЗаписи = Ждать ОбещаниеЗаписи;
		СтадияЗаписи = "read";
		ЗначениеПослеЗаписи = Вычислить("ФайлЗаписиПробы." + ЧтениеЗаписи + "()");
		Если ТипЗнч(ЗначениеПослеЗаписи) = Тип("Дата") Тогда
			ТекстПослеЗаписи = Формат(ЗначениеПослеЗаписи, "ДФ=yyyyMMddHHmmss");
		Иначе
			ТекстПослеЗаписи = ?(ЗначениеПослеЗаписи, "1", "0");
		КонецЕсли;
		ВыводЗаписи = "ok|undefined=" + ?(РезультатЗаписи = Неопределено, "1", "0") + "|after=" + ТекстПослеЗаписи;
	Исключение
		ОшибкаЗаписи = ИнформацияОбОшибке();
		ВыводЗаписи = "error|stage=" + СтадияЗаписи;
	КонецПопытки;
	Если ТипОбещанияЗаписи <> "Promise" И ТипОбещанияЗаписи <> "Обещание" Тогда ВызватьИсключение "ожидалось обещание"; КонецЕсли;
	Сообщить(ИдЗаписи + Символ(9) + ВыводЗаписи);
	Возврат Неопределено;
КонецФункции

Асинх Процедура ИзмеритьЗаписьАсинх()
	КореньЗаписиПробы = ПолучитьИмяВременногоФайла(".async-write");
	Сообщить("file.begin" + Символ(9) + "file-async-write-1");
	Попытка
		СоздатьКаталог(КореньЗаписиПробы + "/directory");
		ДанныеЗаписиПробы = ПолучитьДвоичныеДанныеИзСтроки("abc", КодировкаТекста.UTF8, Ложь);
		ДанныеЗаписиПробы.Записать(КореньЗаписиПробы + "/a");
		ДанныеЗаписиПробы.Записать(КореньЗаписиПробы + "/b");
		Ждать ПробаЗаписиАсинх("write.a.readonly.ru", КореньЗаписиПробы + "/a", "УстановитьТолькоЧтениеАсинх(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.readonly.en", КореньЗаписиПробы + "/a", "SetReadOnlyAsync(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.hidden.ru", КореньЗаписиПробы + "/a", "УстановитьНевидимостьАсинх(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.hidden.en", КореньЗаписиПробы + "/a", "SetHiddenAsync(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.time.ru", КореньЗаписиПробы + "/a", "УстановитьВремяИзмененияАсинх(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.time.en", КореньЗаписиПробы + "/a", "SetModificationTimeAsync(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.timeutc.ru", КореньЗаписиПробы + "/a", "УстановитьУниверсальноеВремяИзмененияАсинх(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.a.timeutc.en", КореньЗаписиПробы + "/a", "SetModificationUniversalTimeAsync(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.readonly.ru", КореньЗаписиПробы + "/directory", "УстановитьТолькоЧтениеАсинх(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.readonly.en", КореньЗаписиПробы + "/directory", "SetReadOnlyAsync(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.hidden.ru", КореньЗаписиПробы + "/directory", "УстановитьНевидимостьАсинх(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.hidden.en", КореньЗаписиПробы + "/directory", "SetHiddenAsync(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.time.ru", КореньЗаписиПробы + "/directory", "УстановитьВремяИзмененияАсинх(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.time.en", КореньЗаписиПробы + "/directory", "SetModificationTimeAsync(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.timeutc.ru", КореньЗаписиПробы + "/directory", "УстановитьУниверсальноеВремяИзмененияАсинх(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.directory.timeutc.en", КореньЗаписиПробы + "/directory", "SetModificationUniversalTimeAsync(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.readonly.ru", КореньЗаписиПробы + "/missing", "УстановитьТолькоЧтениеАсинх(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.readonly.en", КореньЗаписиПробы + "/missing", "SetReadOnlyAsync(Истина)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.hidden.ru", КореньЗаписиПробы + "/missing", "УстановитьНевидимостьАсинх(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.hidden.en", КореньЗаписиПробы + "/missing", "SetHiddenAsync(Истина)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.time.ru", КореньЗаписиПробы + "/missing", "УстановитьВремяИзмененияАсинх(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.time.en", КореньЗаписиПробы + "/missing", "SetModificationTimeAsync(Дата(2020, 1, 2, 6, 4, 5))", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.timeutc.ru", КореньЗаписиПробы + "/missing", "УстановитьУниверсальноеВремяИзмененияАсинх(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.missing.timeutc.en", КореньЗаписиПробы + "/missing", "SetModificationUniversalTimeAsync(Дата(2020, 1, 2, 3, 4, 5))", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.undefined.readonly", КореньЗаписиПробы + "/a", "УстановитьТолькоЧтениеАсинх(Неопределено)", "ПолучитьТолькоЧтение", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.undefined.hidden", КореньЗаписиПробы + "/a", "УстановитьНевидимостьАсинх(Неопределено)", "ПолучитьНевидимость", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.undefined.time", КореньЗаписиПробы + "/a", "УстановитьВремяИзмененияАсинх(Неопределено)", "ПолучитьВремяИзменения", КореньЗаписиПробы);
		Ждать ПробаЗаписиАсинх("write.undefined.timeutc", КореньЗаписиПробы + "/a", "УстановитьУниверсальноеВремяИзмененияАсинх(Неопределено)", "ПолучитьУниверсальноеВремяИзменения", КореньЗаписиПробы);
	Исключение
		Сообщить("file.setup_error" + Символ(9) + ИнформацияОбОшибке().Описание);
	КонецПопытки;
	Попытка
		Для Каждого ПутьОчистки Из НайтиФайлы(КореньЗаписиПробы, "*", Истина) Цикл
			ПутьОчистки.УстановитьТолькоЧтение(Ложь);
		КонецЦикла;
		УдалитьФайлы(КореньЗаписиПробы);
	Исключение
		Сообщить("file.cleanup_error" + Символ(9) + ИнформацияОбОшибке().Описание);
	КонецПопытки;
	Сообщить("file.end" + Символ(9) + "file-async-write-1");
	Попытка
		Выполнить("ЗавершитьРаботуСистемы(Ложь)");
	Исключение
	КонецПопытки;
КонецПроцедуры

ИзмеритьЗаписьАсинх();
