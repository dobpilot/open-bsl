Пробы = СтрРазделить("оба.mxl,оба.xlsx", ",");
Для Каждого Проба Из Пробы Цикл
	Попытка
		Д = Новый ТабличныйДокумент;
		Д.Прочитать("/tmp/open-bsl-xlsx/" + Проба);
		Строки = "";
		Для С = 1 По 3 Цикл
			Для К = 1 По 3 Цикл
				Строки = Строки + "[" + Д.Область(С, К, С, К).Текст + "]";
			КонецЦикла;
		КонецЦикла;
		Сообщить(Проба + "	" + Строка(Д.ВысотаТаблицы) + "x" + Строка(Д.ШиринаТаблицы) + "	" + Строки);
	Исключение
		Сообщить(Проба + "	ошибка	" + ОписаниеОшибки());
	КонецПопытки;
КонецЦикла;
Сообщить("КОНЕЦ");
